# AN-1: Бенчмарк AnyDoc (firecrawl) — локальный DOCX → Markdown (08.08.2026)

## Что сделано

Установлен `firecrawl-anydoc==0.1.7` (pip --break-system-packages). Импорт — `import anydoc`.
Проверен на реальных файлах грантовой заявки `ai-eggs/grant_start1/`.

## Результаты

| Файл | Размер вывода | Скорость | Таблицы |
|------|--------------|----------|---------|
| `GRANT_CHECKLIST.docx` | 3405 chars / 520 слов | **6 мс** | ✅ сохранил кавер-таблицу |
| `FULL_GRANT_APPLICATION.docx` | 6152 chars / 773 слова | **2 мс** | — |

**Качество:** камер-таблица из чек-листа восстановлена корректно (`| Требование | Статус | Нюансы |`), эмодзи/жирный курсив — OK. Никаких ошибок конвертации.

## MinerU сравнение

| Критерий | AnyDoc (этот тест) | MinerU (пред. тест 06.08) |
|----------|--------------------|----------------------------|
| Скорость | 2-6 мс (миллисекунды) | ~минуты (обл.) |
| Зависимости | нет (Rust бинарь в wheels) | модели 3GB + диск |
| Текстовые DOCX | ✅ отлично | избыточен |
| Сканы/сложные PDF | ❌ не файл | ✅ основное назначение |

## Вывод

**AnyDoc — ADOPT для локального офисного формата.** Подходит для Фемиды (DOCX-договоры, csv-чеклисты → Markdown за миллисекунды локально) и потенциально ai-scout (епisodes). MinerU остаётся для сканов/сложных PDF (неполные — диск не позволяет).

**Статус:** AN-1 закрыт. AN-2 (интеграция anydoc в пайплайн Фемиды) — отмечено как pending в реестре, но есть готовый модуль.