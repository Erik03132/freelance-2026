# PF-3: Аудит «функции над данными»

> Дата: 06.08.2026 · Источник: Prime Agent (RLM — чтение через код, а не инструменты).
> Цель: где агенты читают данные инструментами (grep/read целых файлов) вместо
> обработки кодом (скрипты, smart-explore, cached_read).

## Наблюдения

### 1. A2A-шина ai-eggs — образец «функций над данными» ✅
- `a2a_protocol.py`, `a2a_observability.py` — JSON-шина читается кодом
  (json.load, построчный парсинг), не агентом. 8 657 вызовов обработано кодом.

### 2. ⚠️ Найден спам-цикл echo (побочное открытие) — РАЗОБРАНО, НЕ СПАМ
Observability (traces.jsonl, 06.08): **8 640 из 8 657 вызовов (99.8%) — echo**.
Средняя латентность 2мс, 0 ошибок. **Вердикт:** это накопленная история traces.jsonl
с 15-16 июля (файл 2.8MB), не активный цикл. На VPS в логах только 6 записей
«test -> echo» — мои smoke-тесты. Активного спама нет.

### 3. Паттерны в наших агентах
| Место | Сейчас | Кандидат на «функции над данными» |
|---|---|---|
| smart-explore (claude-mem) | tree-sitter AST | ✅ уже функция |
| geekneural cached_read | дедуп-чтение | ✅ уже функция |
| hh-ai-agent ai_analyzer.py | Playwright-скрейп | ✅ уже функция |
| Фемида пайплайн | чтение PDF → MinerU → код | ✅ функция |
| webfetch больших страниц | весь HTML в контекст | → jina-reader/скрипт с extract_text кодом |

### 4. Рекомендации
1. ~~Найти источник спама echo~~ — спама нет (исторические traces с 16.07).
2. Для веб-контента: скрипт `extract_text.py` (BeautifulSoup/trafilatura) вместо
   webfetch целых страниц → токены только текста.
3. Замер после внедрения: токены чтения страниц до/после.
4. Опционально: ротация traces.jsonl (копит 2.8MB+ с июля) — логировать/архивировать
   по дате, чтобы observability-сводки не раздувались историей.
