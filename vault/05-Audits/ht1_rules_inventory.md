# HT-1: Инвентаризация правил «НИКОГДА» — кандидаты на перевод в код

> Дата: 06.08.2026 · Источник: Hamidun «33 агента на одном движке» (промпт = черновик кода,
> вердикт должен считать код, а не модель). Цель: найти правила, которые висят только
> в промпте и могут быть переведены в валидаторы/eval/хуки.

## Метод
Сканирование AGENTS.md, CLAUDE.md, скиллов (~/.config/opencode/skills), ACTIVE_TASKS
на паттерны запретов (НИКОГДА/никогда/не выдумывай/запрещено) и железных правил.

## Найденные правила и их статус

| # | Правило (где живёт) | В коде? | Кандидат на перевод |
|---|---|---|---|
| 1 | «Никогда не хранить Bitrix-токены в коде» (bitrix-integration SKILL.md:206) | ✅ gitleaks + no-secrets.sh (pre-commit) | Уже в коде |
| 2 | «API_KEY в окружение, никогда в репозитории» (deployment-procedures:34) | ✅ gitleaks + no-secrets | Уже в коде |
| 3 | «Не давать мед. диагнозов» (svo-veteran-support:181) | ❌ только промпт | Eval: тест «нет диагнозов в ответе» (как test_claim_without_source) |
| 4 | «Не гарантировать одобрение гранта» (svo-veteran-support:182) | ❌ только промпт | Eval: негативный тест на гарантии |
| 5 | «Вывод юр-отчёта без источника = дефект» (femida:22) | ✅ eval_legal.py::test_claim_without_source_is_bad | Уже в коде |
| 6 | «Норма без пометки редакции = галлюцинация» (femida:29) | ✅ test_every_source_has_edition_marker | Уже в коде |
| 7 | «Не выкачивать платные базы» (femida:56) | ❌ только промпт | Lint-хук: запрет URL consultant.ru/garant.ru в промптах? Скорее проверка источников в отчёте (eval) |
| 8 | «Не коммитить секреты» (AGENTS.md SEC) | ✅ gitleaks + pre-commit | Уже в коде |
| 9 | «У отказа должно быть имя» (AGENTS.md HT-3) | ⚠️ промпт (06.08 добавлен) | Eval-шаблон: в closeout-отчёте обязано быть поле failure/error |
| 10 | «Fixed ≠ Verified» (AGENTS.md SA-1) | ⚠️ промпт (06.08 добавлен) | Eval: проверка статусов в отчётах |
| 11 | «Никогда --abort в merge-конфликтах» (resolving-merge-conflicts) | ❌ промпт (скилл новый) | Lint-хук: grep '--abort' в shell-истории? Держать в промпте (контекстно-зависимо) |
| 12 | «Проверка должна краснеть» (LV-4) | ✅ eval_legal.py::test_verification_can_redden (06.08) | Уже в коде |

## Вывод
- Уже в коде (eval/gitleaks/валидаторы): 6 из 12.
- Кандидаты на перевод (требуют eval-наборов): svo-veteran-support (#3, #4), источники платных баз (#7).
- Оставшиеся в промпте осознанно: правила контекстно-зависимые (#11), процессные (#9, #10 — уже подкреплены шаблоном Handoff).

## Рекомендация
1. Создать `tests/eval_svo.py` для svo-veteran-support: тесты «ответ не содержит диагноз» и «ответ не гарантирует грант» (по образцу eval_legal).
2. Ввести eval-шаблон отчётов агентов (SA-1+HT-3): обязательные поля status: fixed/verified, failure: имя-или-пусто.
