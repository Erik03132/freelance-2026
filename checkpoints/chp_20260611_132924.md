# 📋 CHECKPOINT — 10.06.2026

## Что сделано сегодня

### ✅ #11 Mem0 — долгосрочная память клиентов (ГОТОВО)
- Создан `angel-backend/mem0_memory.py` — LLM-экстракция фактов из диалога + файловый store
- Интегрирован в `angelochka_core.py`: recall в промпт + remember после ответа
- Архитектура: SimpleMemoryStore (JSON-файл per client) + BM25-like search + дедупликация
- LLM-экстрактор: GPT-4o-mini через OpenRouter + regex-фоллбэк
- Деплой: `/root/antigravity/ai-eggs/agent/` (правильный путь!) + pm2 restart angela-bot
- Подтверждено в логах: `✅ Mem0 долгосрочная память: АКТИВНА`
- ⚠️ LLM-экстракция ВЫКЛ — нет OPENROUTER_API_KEY в .env VPS (добавить позже)

## ✅ #12 Agentic RAG — ЗАВЕРШЕНО (10.06.2026)
Источник: Google Research ixbt.com/news/2026/06/08/google-agentic-rag.html
Файл: `angel-backend/agentic_rag.py` (274 строки)
Архитектура: QueryRewriter → Retriever → ContextChecker → QueryRefiner → loop (max 3 iter)
LLM-агенты: ВЫКЛ на VPS (нет OPENROUTER_API_KEY → режим Naive с одной итерацией)
Подтверждено в логах: `✅ Agentic RAG: АКТИВЕН`

## Следующая сессия
Добавить OPENROUTER_API_KEY в .env VPS — активирует LLM-агенты для Mem0 и Agentic RAG.

## Бэклог (остаток P1)
- 📌 #13 — OPENROUTER_API_KEY в .env VPS (разблокирует LLM-режим Mem0 + Agentic RAG)

## Ключевые файлы изменены
- `angel-backend/mem0_memory.py` — НОВЫЙ модуль (Mem0-паттерн без Qdrant)
- `angel-backend/angelochka_core.py` — интеграция Mem0 (recall + remember)
- `.agent/rules/two-angelas-map.md` — mem0_memory.py добавлен в карту

## Состояние сервисов (VPS 72.56.38.19)
angela-bot — перезапущен с новым кодом (Mem0 активна)
angela-server, angela-listener, angela-vk-bot — без изменений

## Следующая сессия
Начать с `#12 Self-Reflective RAG` — заменить Naive RAG в `rag_lite.py`.
