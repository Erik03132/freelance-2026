## 🌞 2026-07-23 — HH-агент: генерация откликов + правила агента

**Статус:** Сессия завершена (finish-day).

### Сделано
1. **HH-агент — генерация 8 откликов на вакансии:** Petrovax AI PM, Vallettasoftware AI Builder, Синди (AI-экосистема beauty), Второй пилот (Fullstack + AI), Глобус-М (логистика AI), Braiden Consulting (AI-автоматизация), Selecty (Руководитель AI-внедрения), вода/очистка, и др. Ответы на вопросы заказчиков с Кворка (музыкальная дистрибуция, обзвон клиентов, контент).

2. **Правила агента (AGENTS.md) — 4 новых жёстких правила:**
   - Не зеркалить текст вакансии
   - GitHub-ссылка в конце каждого отклика
   - Хеджирование инструментов (не врать, альтернатива / готов освоить)
   - Модели: не акцентировать одну, через OpenRouter или не упоминать
   - Формат вывода: только текст, без обёрток

3. **candidate_profile.txt — обновлён:**
   - LLM API через OpenRouter (Claude, Gemini, DeepSeek, Kimi и др.)
   - Вайб-кодинг: OpenCode, Antigravity (не Claude Code/Cursor)
   - Профильное образование и сертификация по нейросетям
   - Генерация изображений: Leonardo AI (не MidJourney)

4. **KWORK_SILENT_AUTO.md — 2 новых правила:**
   - Анализировать количество откликов и % найма заказчика
   - Предлагать демо 30% работы бесплатно для выделения среди конкурентов

5. **Кворк-ответы:** 2 ответа с учётом стратегии (контент-наполнение, обзвон клиентов)

### Архитектурные решения
- Модели перечислять через OpenRouter без акцента на конкретную версию
- Отклики на Кворк — в формате AI-агентства (незаметная автоматизация)
- Демо 30% как основной инструмент конверсии на Кворке

### Файлы
- `projects/hh-ai-agent/AGENTS.md` (правила #8-11, формат вывода)
- `projects/hh-ai-agent/prompts/candidate_profile.txt` (стек, инструменты, образование)
- `projects/freelance-agent/.agent/rules/KWORK_SILENT_AUTO.md` (демо, анализ конкурентов)
8. **AGENTS.md обновлён** — каскадная система понижена до fallback, OmniRoute — основной режим.

### Архитектурные решения
- OmniRoute — единый AI-шлюз для всех проектов. Авто-роутинг, RTK+Caveman компрессия (~89%), TLS stealth.
- US прокси оставлен как резерв (если OmniRoute недоступен).
- BytePlus SeedSpeech — перспективный бэкенд для Levitan (дешевле OpenAI Realtime).

### Блокеры
- В дашборд OmniRoute не добавлены бесплатные провайдеры (Kimi, Groq и др.) — через UI.
- BytePlus — требуется тест русского TTS/ASR качество.

### Файлы
- `chp.md` — обновлён
- `~/.config/opencode/AGENTS.md` — обновлён (OmniRoute как шлюз, каскад — fallback)
- `.env` (root, freelance-agent, levitan, ai-levitan) — +OPENAI_BASE_URL
- `projects/levitan/docs/active_tasks.md` — +BytePlus, +OmniRoute
- VPS: `~/.omniroute/.env`, `~/.pm2/` (omniroute process)

---

## 🌞 2026-07-21 (вечер 2) — MacWhisper: переустановка + dictation fix

**Статус:** Сессия завершена (finish-day).

### Сделано
1. **MacWhisper — чистая переустановка:** удалён из `/Applications`, вычищены все данные (модели Small/Large, БД, кеши, префы, логи, `~/Downloads`). Установлен заново с macwhisper.com.
2. **Починка "The session is not running"** — ошибка dictation. Удалён автоскачанный Large V3 Turbo (700MB). Оставлена Tiny для диктовки, затем переключены на Small (лучший баланс скорость/качество). Dictation заработал.

### Файлы
- Системная задача (MacWhisper.app, не проект)

---

## 🌞 2026-07-21 (вечер) — Grok 4.5, slide_gen, brand_md, proxy testing, capture_outcome fix

**Статус:** Сессия завершена (finish-day).

### Сделано
1. **Grok 4.5 (Tier 2.5)**: модель `x-ai/grok-4.5` на OpenRouter — протестирована через HTTP-прокси (64468). $0.002/$0.006/1M токенов, контекст 500K. AGENTS.md обновлён.
2. **Proxy US**: HTTP 64468 работает для HTTPS через requests; SOCKS5 64469 — только raw TCP. AGENTS.md уточнён.
3. **slide_gen.py**: генератор React HTML-презентаций (Bolt Slides-стиль). CLI `--slides`. `output/slides.html` — 12765 байт.
4. **brand_md.py**: дизайн-токены по URL. CLI `--brand-url`. designmd.supply (meta-fallback при 403).
5. **capture_outcome bug fix**: `signal.py` — `capture_outcome()` подтягивает agent из start-записи по sid. Тесты 13 passed.
6. **llm/__init__.py**: `load_proxy()` — HTTP_PROXY из `.env` при каждом `call_llm()`.
7. **Angela cost**: 100 звонков/день = ~13 600 ₽/мес ($151). Mango телефония ~78%.
8. **Тесты**: 125 passed, 4 skipped.

### Архитектурные решения
- Grok 4.5 = Tier 2.5 (продвинутые задачи, дешевле Sonnet). Прокси HTTP 64468 проверен.
- designmd.supply: meta-fallback при 403 — работает, внедрён как есть.

### Файлы
- `agents/artemiy/slide_gen.py` · `brand_md.py` (новые); `cli.py` (обновлён)
- `agents/learning/signal.py` (capture_outcome fix)
- `agents/llm/__init__.py` (+load_proxy)
- `~/.config/opencode/AGENTS.md` (каскад Grok 4.5)
- `freelance-agent/.env` (прокси 64468)

---

## 🌞 2026-07-21 — Анжела в Битриксе: восстановление после миграции VPS

**Статус:** Сессия активна (НЕ finish-day). Анжела в Битриксе починена.

### Контекст
Пользователь поднял новый VPS (старый `72.56.38.19` отключён за неуплату, IP отдан чужому клиенту). Новый сервер `217.149.23.113` (Timeweb, Ubuntu 22.04, 1 vCPU/2GB). 9 PM2-процессов перенесены в `/opt/levitan/projects/ai-eggs`, но Анжела в чате Битрикса не отвечала.

### Корневая причина
`bitrix_bot.py` (бот чата Битрикса) **не был запущен** на новом VPS — на старом сервере работал через `nohup`, при миграции забыли поднять. Плюс 2 проблемы в конфигурации:
1. `BITRIX_WEBHOOK_URL` указывал на sandbox `b24-mjxvhq.bitrix24.ru`, а не на production `incubird.bitrix24.ru`
2. `HTTPS_PROXY=socks5h://localhost:1080` ломал все запросы к Битриксу (`Connection refused` — SOCKS на :1080 не запущен)

### Сделано
**1. Инфраструктура VPS (предыдущие шаги):**
- `.env`: все 6 вхождений `72.56.38.19` → `217.149.23.113`
- `sync_autodial_to_bitrix.py`: путь к `.env` сделан относительным
- SSH-ключ `.ssh_agent_key` добавлен в `authorized_keys` на VPS, вход без пароля
- `known_hosts` почищен от старого host-key `72.56.38.19`
- `setup_ssh_key.sh` + `deploy_to_vps.sh`: переписаны — читают конфиг из `.env` (безопасный парсер, не ломается на `&`/`!`), новые пути `/opt/levitan/projects/ai-eggs`, venv `.server_venv`, ecosystem `/opt/levitan/`

**2. Анжела в Битриксе (today):**
- `bitrix_bot.py`: читает `PRODUCTION_BITRIX_WEBHOOK_URL` (с fallback на `BITRIX_WEBHOOK_URL`); убирает прокси перед запросами к Битриксу
- `.env`: убран мёртвый `HTTPS_PROXY=socks5h://localhost:1080`; `HTTP/HTTPS/ALL_PROXY` → рабочий US-прокси `172.120.21.141:64469`; добавлен `NO_PROXY` для `bitrix24.ru`, `mango-office.ru`, `localhost`
- `bitrix-bot` (id 9) добавлен в PM2, online, `pm2 save` выполнен
- Проверено: `Bitrix URL: incubird.bitrix24.ru/rest/42020/...`, user 41624 = «Анжелочка Заботкина» (active), запросы проходят без ошибок

### Подтверждение
- `bitrix-bot` online, 0 рестартов, error-лог пустой
- Бот опрашивает чат каждые 15 сек, «Непрочитанных нет — чисто!»
- Ждём подтверждения пользователя: ответит ли Анжела на сообщение в чате Битрикса

### Нерешённое / Next Steps
- `faiss`, `rank_bm25` не установлены в `.server_venv` → векторный/BM25 поиск отключён (бот работает на FAQ + RAG Lite)
- `psycopg2` отсутствует → PersistentHistory/VectorDB (Neon) недоступны
- baresip не зарегистрирован на Mango SIP (голосовые звонки не ходят)
- Код `vezemcip.ru` (Laravel) не найден
- `deploy_to_vps.sh` не протестирован реальным деплоем (только `bash -n`)

---

## 🌞 2026-07-21 — Анжела в Битриксе: починена после миграции на новый VPS

**Статус:** Сессия активна (промежуточный итог). Блокер ботов снят.

### Контекст
Пользователь поднял новый VPS (старый `72.56.38.19` отключён за неуплату, IP отдан чужому клиенту). Данные перенесены, но Анжела в чате Битрикса не отвечала.

### Корневая причина
`bitrix_bot.py` (бот чата Битрикса, polling каждые 15 сек) **не был запущен** на новом VPS. На старом сервере работал через `nohup`, при миграции забыли поднять. Плюс 2 бага в конфигурации:
1. Читал `BITRIX_WEBHOOK_URL` (sandbox `b24-mjxvhq`) вместо production (`incubird.bitrix24.ru`)
2. `HTTPS_PROXY=socks5h://localhost:1080` ломал все запросы к Битриксу (`Connection refused` — SOCKS на :1080 не запущен)

### Сделано
**1. Инфраструктура VPS (новый `217.149.23.113`, Timeweb, Ubuntu 22.04):**
- Подтверждено: 9 PM2-процессов online, Bitrix API работает (66165 сделок, лиды пишутся сегодня)
- SSH-ключ `.ssh_agent_key` добавлен в `authorized_keys` → вход без пароля
- `known_hosts` почищен от старого host-key `72.56.38.19`

**2. `.env` (projects/ai-eggs):**
- Все 6 вхождений `72.56.38.19` → `217.149.23.113`
- Убран мёртвый `HTTPS_PROXY=socks5h://localhost:1080`
- `HTTP/HTTPS/ALL_PROXY` → рабочий US-прокси `172.120.21.141:64469`
- Добавлен `NO_PROXY` для `bitrix24.ru`, `mango-office.ru`, `localhost`

**3. Код:**
- `bitrix_bot.py`: читает `PRODUCTION_BITRIX_WEBHOOK_URL` (fallback `BITRIX_WEBHOOK_URL`) + убирает прокси перед Битрикс-запросами
- `sync_autodial_to_bitrix.py`: путь к `.env` относительный (работает и на VPS, и на маке)
- `setup_ssh_key.sh` + `deploy_to_vps.sh`: читают конфиг из `.env` (безопасный парсер, не ломается на `&`/`!`), новые пути `/opt/levitan/projects/ai-eggs`, venv `.server_venv`

**4. Запуск бота:**
- `bitrix-bot` (PM2 id 9) добавлен и запущен, `pm2 save` выполнен
- Логи чистые: `Bitrix URL: incubird.bitrix24.ru`, `User ID: 41624` (Анжелочка Заботкина), `Непрочитанных нет — чисто!`

### Архитектурные решения
- Битрикс и Mango из РФ доступны напрямую → прокси только для LLM (Gemini/OpenRouter). `NO_PROXY` + `os.environ.pop` в коде.
- Бот чата Битрикса (`bitrix_bot.py`) вынесен в PM2 (раньше `nohup` — терялся при миграции/ребуте).

### Нерешённое (не блокирует)
- `faiss`, `rank_bm25`, `psycopg2` не установлены в `.server_venv` → векторный/BM25 поиск и PersistentHistory отключены. Бот работает на FAQ + RAG Lite.
- baresip не зарегистрирован на Mango SIP (голосовые звонки не ходят).
- Код `vezemcip.ru` (Laravel) не найден.
- Захардкоженные пути `/root/antigravity/ai-eggs` встречаются ещё в ~9 файлах `agent/` (deploй-скрипты починены, остальные — по мере запуска).

### Следующие шаги
- Пользователь проверяет ответ Анжелы в чате Битрикса (написать сообщение, ждать 15-30 сек)
- (опц) Доставить `faiss`/`rank_bm25`/`psycopg2` в `.server_venv`
- (опц) Настроить baresip на Mango SIP

---

## 🌞 2026-07-20 (вечер) — Git-hygiene завершение + scaffold self-improvement (ADR-001)

**Статус:** Сессия завершена (finish-day).

### Сделано
**1. Git-hygiene: убрали `freelance-agent/dist` из индекса**
- `git rm -r --cached freelance-agent/dist` → 178 файлов скомпилированного TS (есть `src/` → пересобирается `npm run build`).
- `.gitignore` уже покрывает (`dist/`), проверено `git add -A --dry-run` → не возвращается.
- Commit `ca0a60b8`, пуш в `origin/main` (`31d73e27..ca0a60b8`). Репозиторий −49.5K мусорных файлов (с учётом node_modules `f8713698`).

**2. Разбор поста @vibecoding_tg/3513 (survey arXiv:2607.13104)**
- Тема: Self-Improvements in Modern Agentic Systems (Ren, Zhuge, Schmidhuber et al., 14 июл 2026).
- Фреймворк: agent = foundation model + operational scaffold (prompts/memory/tools/control logic); самоулучшение = update operator в параметры модели (Target A) ИЛИ scaffold (Target B).
- Наш вывод: Target A (файнтюн) недоступен (чужие API) → фокус на **Target B (scaffold-only)**.

**3. Применение к нашей системе (вне репозитория — `~/.config/opencode/`):**
- `docs/adr/ADR-001-scaffold-self-improvement.md` — формализовали петлю через модель survey; таблица сигнал → update target → действие.
- `docs/self_improve_log.md` — журнал «сигнал → апдейт → результат» (раньше отсутствовал).
- `AGENTS.md`: паттерн №10 (scaffold self-improvement) + триггер «регрессия eval = обязательный scaffold-апдейт + запись в журнал».
- Существующий `learning loop` (ADR-002 freelance-agent, сигналы accepted/edited/rejected) лёг как частный случай Target B. Дублирования нет.

### Архитектурные решения
- Самоулучшаем ТОЛЬКО scaffold (Target B); файнтюн модели исключён по экономике/доступу.
- Eval-регрессия повышена из «прогнать перед изменением» в обязательный триггер апдейта.

### Продолжение (WIP)
- Настройки агентов (`~/.config/opencode/`) вне git — отдельный контроль версий не заведён (пока не нужно).
- Полная очистка PII из git-истории (git filter-repo + force-push) — отложено.

### Следующие шаги
- (опц) Завести контроль версий для `~/.config/opencode/` (git init) — чтобы ADR/журналы не терялись.
- Восстановление VPS/SSH-ключей (блокер ботов) — не начато.

---

## 🌞 2026-07-20 — HH-агент: отклики работодателям (AI/финтех + LD LATTE)

**Статус:** Сессия завершена (finish-day).

### Сделано
**1. Ответы HH-агента на вопросы AI/финтех-работодателя**
- Работодатель (ИИ- и финтех-платформы, удалёнка, асинхрон, найм через собственную ИИ-систему по GitHub-коду) прислал 3 вопроса: опыт AI/ML + топ-3 фич, зарплата, готовность приступить.
- Собрал факты: опыт AI/ML ~2 года (1-3 по шкале), зарплата < $2000/мес, старт < 1 недели.
- Подтянул реальные публичные репозитории GitHub (Erik03132): `angel-sales` (AI-агент продаж/автодозвон, A2A, ~1068 файлов), `ai-scout-final` (AI-скайтинг, парсинг TG/YT, RAG), `freelance-2026` (Levitan turbo-FAQ голосовой агент, 210 триггеров, Яндекс TTS + local LLM-fallback).
- Подготовил честные ответы на 3 вопроса (без выдумки).

**2. Открытый отклик на LD LATTE (бренд женской одежды, WB/Ozon, 1 млрд ₽)**
- Первый черновик нарушал стандарт HR-агента (эмодзи, «буду рад(а) познакомству», нет подписи/тестового).
- Подтянул и применил реальный стандарт: `projects/hh-ai-agent/prompts/cover_letter_v3.txt` (HOOK→MATCH→PROOF, жёсткие запреты) + `candidate_profile.txt` (реальные кейсы/метрики) + скилл `brand-voice`.
- Переписал отклик по стандарту v3: зеркалил «ты» (культура бренда), убрал эмодзи/баны, добавил кейсы (A2A-шина −69% токенов, голосовая кампания 100+ звонков), подпись «С уважением, Игорь», готовность к тестовому.

### Архитектурные решения
- HR-агент = НЕ «с листа», а через наработанный стандарт v3 + профиль. Всегда подтягивать `hh-ai-agent/prompts/*` и `brand-voice` перед генерацией откликов.
- WB/Ozon в отклике LD LATTE честно захеджирован как «беру по тому же API-паттерну, что Avito/VK/CRM» (per candidate_profile line 5).

### Продолжение (WIP)
- Отклики не отправлены — ждут подтверждения пользователя / уточнения конкретной задачи LD LATTE.
- Сабмодули `ai-eggs`/`ai-scout`/`angel-backend`/`hh-ai-agent` — чужой WIP (не коммитим в рамках этой сессии).

### Следующие шаги
1. Отправить ответы AI/финтех-работодателю и отклик LD LATTE (после аппрува).
2. (опц) Обновить `candidate_profile.txt` метриками голосового агента Levitan (210 триггеров) при следующей серии откликов.

---

## 🌞 2026-07-15 — PII cleanup (Levitan) + ADR-002 latency budget

**Статус:** Сессия завершена.

### Сделано
**1. Чистка PII в Levitan (вариант 1)**
- `git rm --cached` для **26 PII-CSV** в `projects/levitan/data/campaigns/csv/` (колонки `Имя` + `Телефоны` реальных лидов). Файлы остаются на диске (47 шт.), удалены только из индекса.
- Подводный камень: `git diff --name-only` оборачивает non-ASCII пути в кавычки → `git rm` с `--ignore-unmatch` молча «успешно» не удалял. Фикс через `git ls-files -z | xargs -0`.
- В `projects/levitan/.gitignore` (line 43) добавлен комментарий: `PII: campaign CSVs contain real lead names + phones -> NEVER track`.
- Проверка: `git add -A --dry-run` по папке → пусто (gitignore корректно блокирует повторное добавление).
- Коммит `17d4a731` (fix levitan PII) → запушен в `main`.
- ⚠️ Старая PII остаётся в git-истории. Полная чистка (вариант 3, `git filter-repo` + force-push) — отдельная, более инвазивная задача, не делалась.

**2. ADR-002: бюджет задержки голосового агента**
- Синтез видео «I Made My AI Voice Agent 2x Faster» + свежий (2026) консенсус по voice latency (LiveKit/ElevenLabs/Vapi/OnCallClerk).
- Записан `projects/levitan/docs/adr/ADR-002-latency-budget.md` (Proposed): для realtime-архитектуры (Яндекс Realtime) ~60% pipeline-оптимизаций уже делает модель; рычаги на нашей стороне — byte-стабильный system prompt + prefix caching, SIP/кодек (ulaw_8000, колокация с Mango POP, тёплый пул WS), VAD ~300ms, filler + greeting pre-buffer вокруг `save_lead`, замеры p95 по стадиям.
- Коммит `dd49ef8c` → запушен в `main`.

### Архитектурные решения
- PII bulk-data не коммитим; трекаем только `call_results` (gitignore exception).
- Latency budget для Levitan целимся в p95 < 500 мс; динамику промпта — в конец (не ломать prefix cache).

### Продолжение (не закоммичено, WIP вне сессии)
- Сабмодули `ai-eggs`/`ai-scout`/`angel-backend`/`hh-ai-agent` — чужой/несвязанный WIP.
- `projects/levitan/scripts/extract_adygea_leads.py`, `ping_checker.py`, `reports/geo_monitor_launchd.err` (volatile лог) — не трогали.

### Следующие шаги (по желанию)
1. Реализовать ADR-002 в `deploy/levitan_realtime.py` + `greeting_bridge` (когда будет Яндекс Cloud ключ).
2. (опц) Полная очистка PII из истории — `git filter-repo` + force-push (обсудить отдельно).
3. Восстановить VPS / SSH-ключи (блокер ботов).

---

## 🧬 2026-07-14 — soul.md: самоуправляемая конституция агентов (Chen pattern)

**Статус:** Сессия завершена.

### Источник
Видео Шен Шон Чена (@vibecoding_tg/3475) о self-improving агентах. 5 механик: soul.md · smart RAG · self-terminating loop · self-healing prompt · memory compaction. Выбрана к внедрению **soul.md**.

### Сделано
**Новый модуль `soul/` (freelance-agent/.agent/agents/soul/)** — pure stdlib, graceful fallback:
- `store.py`: read/write/scaffold/replace_auto_zone. Формат soul.md = 2 зоны: `## Constitution` (human-owned) + `## Evolving Lessons` с маркерами `<!-- SOUL:AUTO:BEGIN/END -->` (machine-owned).
- `__init__.py`: `load_soul` · `soul_context(max_chars)` · `ensure_soul` (идемпотентно) · `evolve_soul(lessons)` (self-edit только AUTO-зоны).
- `tests/test_soul.py` — **10 тестов, 100% pass** (TDD): scaffold, идемпотентность, evolve замыкается только на AUTO-зону, замена прошлых lessons, no-op на пустое, max_chars.

**4 soul.md с распиленными конституциями:** artemiy · kulibin · sherl · rembrandt (Role + Hard rules из профилей).

**Проводка в CLI (4 агента):** wrapper над `build_learned_context` инжектит `soul_context` во ВСЕ call-sites без правки каждого; `ensure_soul` на старте; при `--feedback` accepted/edited/rejected → `evolve_soul` сворачивает свежие learned-lessons в soul.md (замкнут self-improving цикл: signals → learning → soul).

### Проверка
- soul: 10/10 · sherl: 10/10 · все 4 CLI smoke OK (`--list-*`).
- ⚠️ artemiy/kulibin/rembrandt tests виснут на pre-existing тесте `*_empty_api_key` (реальный сетевой вызов без таймаута) — НЕ связано с soul.

### Архитектурные решения
- soul.md = единый живой файл governance: статичная конституция (человек) + эволюционирующие уроки (машина). Заменяет разрозненность profile.md / memory.jsonl / learning.
- evolve_soul трогает только AUTO-зону — конституция человека неприкосновенна.

### Продолжение (та же сессия) — механики Чена #4, #5 + фикс тестов

**1. Фикс pre-existing сетевого хэнга в тестах**
- Корень: `key = api_key or load_openrouter_key()` — пустая строка `""` (тестовый «нет ключа») falsy → подхватывался реальный ключ из `.env` → реальный сетевой вызов 60с.
- Фикс во всех 4 llm_client.py: `key = api_key if api_key is not None else load_openrouter_key()` — `None` = взять из env, `""` = явно без ключа. Тесты стали герметичны.

**2. Механика #4 — self-healing prompt (`healing/`)**
- `run_with_healing(prompt, generate, validate, max_retries)` — detect error → patch prompt корректирующими директивами → re-run → self-terminate на чистой валидации ИЛИ по исчерпании бюджета (совмещает #4 + #3 self-terminating loop).
- `heal_prompt(prompt, issues)` + dataclass `HealResult(output, ok, attempts, issues)`. Валидатор-исключения контейнеризованы (не роняют цикл).
- Проводка в artemiy: `_validate_frontend` (пусто/лики/наличие markup) оборачивает генерацию компонента, max_retries=1.
- `tests/test_healing.py` — **7 тестов, 100% pass**.

**3. Механика #5 — memory compaction (`memory.compact`)**
- Дедуп идентичных фактов (norm-текст, хранит новейший ts) + обрезка до `keep` новейших; атомарная перезапись jsonl. Возвращает `{before, after, removed}`.
- Авто-триггер в feedback-хендлере всех 4 CLI (момент самооптимизации) с выводом `🗜️ Memory compacted`.
- `memory/tests/test_compact.py` — **5 тестов, 100% pass**.

### Итог по тестам
Полный прогон: **62 passed за 33s, без хэнга** (было 50). soul 10 + healing 7 + memory compact 5 + прежние 40.

**4. Механика #2 — smart RAG top-K (`memory.recall_scored`)**
- Найден баг: `enrich_context` вызывал `recall()` (возвращает строку), но итерировал как список dict'ов → память **никогда** не инжектилась.
- `recall_scored(agent, query, top_k, min_score)` — ранжированные dict'ы {fact, score, ...} по Jaccard-overlap, порог + top-K. `recall()` переписан поверх (backward-compat строка).
- `enrich_context` починен: реально инжектит top-K релевантных фактов.
- `memory/tests/test_smart_rag.py` — **8 тестов, 100% pass** (ранжирование, top-K, min_score, реальная инъекция в enrich_context).

### Самоулучшающийся цикл (замкнут)
generate → **validate/heal** (#4) → **smart-RAG recall** (#2) → signals → learning → **soul.md evolve** (#1) → **memory compact** (#5). Из 5 механик Чена внедрены **все**: #1, #2, #3 (внутри #4), #4, #5.

### Итог по тестам (финал)
Полный прогон: **70 passed за 27s, без хэнга** (было 50). +soul 10 +healing 7 +compact 5 +smart_rag 8.

---

## 🌞 2026-07-12 — VPS диагностика + обновление credentials

**Статус:** Сессия в процессе.

**Сделано:**
1. Диагностика VPS: 72.56.38.19 — пинг есть, SSH не пускает (host key changed, пароль не подходит). 185.39.206.145 — мёртв
2. Выяснена причина молчания @Angella26bot — VPS переустановлен, все PM2 процессы удалены
3. Найдены Timeweb credentials в .env (ai-eggs)
4. Обновлены Timeweb логин/пароль во всех проектах: ai-eggs, ai-levitan, levitan, angel-backend, agent-lab
5. Новые данные: лог `Vezemcip@yandex.com`, пасс `Azamat123`

**Блокеры:** ❌ VPS недоступен (ждём сброс пароля от Timeweb панели)

**План:**
- Зайти в my.timeweb.ru → сбросить root-пароль
- SSH → восстановить сервисы (Angela, боты, VezemCip)

---

## 🏁 2026-07-07 — CRM доработки (Levitan)

**Статус:** Сессия завершена. Исправлена логика «Сегодня» и счётчик звонков.

**Сделано в сессии:**
1. Исправлена логика страницы "📞 Сегодня": показывает напоминания с дедлайном сегодня/просроченные
2. Кнопка «На сегодня» создаёт напоминание на сегодняшнюю дату
3. Исправлен счётчик «Звонков сегодня» — считает по timestamp, а не по created_at
4. Обновлён шаблон today.html
5. Завершена сессия по протоколу finish-day

**Проблемы:** VPS недоступны, Whisper STT плохой, claude-mem 403 на projectId

**План на завтра:**
- Дождаться 25 июля для теста автопоявления напоминаний
- Восстановить VPS или сменить хостинг

---

## 🌙 2026-07-08 (ночь) — Global Session: Levitan + Agents Upgrade

**Статус:** Сессия завершена.

### Сделано

**1. Levitan — Ralph Phase-1 (Production Readiness) — ВСЁ ГОТОВО**
- US-001: ruff linting (445→0 ошибок)
- US-002: `normalize_phone()` в utils.py
- US-003: `crm_enricher.py` (provider pattern + MockCrmProvider)
- US-004: CRM enrichment в dialer_bot.py
- US-005: duration_sec + operator в save_to_crm
- US-006: ConfigDict замена (0 pydantic warnings)
- US-007: campaign_runner.py (100-call baseline)
- Тесты: 25 → 41, все проходят

**2. Поиск глобальных агентов (Antigravity)**
- Найдены в `freelance-agent/.agent/agents/`: Артемий, Ботмэн, Игорёк, Кулибин, Маркетолог, Рембрандт, Шекспир, Шерлок
- Skills: `~/.agents/skills/sherlock-deduction/`, `sherlock-bidding/`, `brigadier-router/`
- Фелини — только в gemini brain, отдельного профиля нет

**3. Rembrandt (ai-eggs) — апгрейд до Universal Designer Agent**
- Modules: brand_system, image_generator, design_generator, component_generator
- CLI: `python3 agent/rembrandt.py --design/--component/--prompt`
- Agent profile `rembrandt-designer.md` обновлён
- IncuBird brand JSON добавлен
- 7 коммитов в angel-sales repo

**4. HH.ru** — Ralph setup создан, НО deprioritized (пользователь выбрал Levitan как приоритет)

### Архитектурные решения
- OpenCode (не Claude Code CLI) — пользователь через OpenRouter в IDE
- Ralph default tool → `opencode`
- Levitan приоритетнее HH.ru

### Проблемы
- claude-mem binary недоступен (создан SESSION_SUMMARY markdown вместо)
- ai-levitan repo accidentally staged docs/superpowers from root — исправлено

### План на завтра
- Запустить реальный 100-call обзвон через campaign_runner (Levitan)
- Проанализировать метрики
- Прокачать других агентов (Артемий, Кулибин, Шерлок) по аналогии с Рембрандтом
- Рассмотреть Refero MCP интеграцию для Рембрандта

---

## 🌞 2026-07-09 — Agent Ecosystem: Full upgrade (Memory + Model Routing + Tests)

**Статус:** Сессия завершена.

### Сделано

**1. Починка TG бота @Angella26bot** — заблокированный токен заменён в 6 файлах, бот работает.

**2. Agent Ecosystem — финальный апгрейд всех 4 агентов**
- `rembrandt/component_generator.py` + `design_generator.py`: `requests.post` → `call_llm` (model routing, graceful fallback, cleaned unused `_find_env`/`import os`/`import requests`)
- `sherl/llm_client.py`: добавлен `ROUTE` dict (simple→deepseek, complex→sonnet) + fenced block stripping + `_compress`
- `memory/enrich_context()`: обёртка `recall + merge` в `memory/__init__.py`
- Все 4 CLI (`artemiy`, `kulibin`, `sherl`, `rembrandt`): `import memory` с graceful fallback + `enrich_context()` во все LLM-ветки
- Исправлен `DESIGN_SYSTEM_PROMPT` (экранированы JSON-фигурные скобки для `.format()`)
- Docstrings в копиях `llm_client.py` исправлены (кулибин/рембрандт)

**3. Тесты для всех агентов (40 total, 100% pass)**
- `artemiy/tests/test_component_gen.py` — 8 тестов (типы, фреймворки, fallback без ключа, invalid type)
- `kulibin/tests/test_basic.py` — 11 тестов (языки, анализатор, scout/evaluate без ключа, security)
- `sherl/tests/test_sherl.py` — 10 тестов (провайдеры, geo scan, research без ключа, форматирование)
- `rembrandt/tests/` — 11 тестов (были до, все проходят)

**4. Профили агентов + Handoff**
- `artemiy-frontend.md`, `kulibin-engineer.md`, `sherl-research.md`, `rembrandt-designer.md`: добавлены секции Memory Tree + Model Routing
- `docs/handoff_session_final.json` — финальная передача (контекст, файлы, next steps)
- `docs/adr/ADR-001..004` — архитектурные решения (LLM-CLI, Self-Learning, Security, Memory)

### План на завтра
- Levitan: запустить 100-call обзвон
- Angela: eval для Generator (LLM-as-judge, HD5)
- LLM Wiki: перестроить KB Angela по паттерну Карпатого (HD1)
- Agent Ecosystem: протестировать model routing с реальным ключом, кросс-агентный recall

---

## 🌞 2026-07-09 (2-я сессия) — Levitan Realtime Migration + Global Commands

**Статус:** Сессия завершена.

### Сделано

**Levitan — Real-time миграция (планирование + DRAFT)**
- Habr-кейс 1057176: Яндекс Realtime побеждает для РФ (330мс, родной русский, без VPN, рубли).
- ADR-001 (`docs/adr/ADR-001-yandex-realtime.md`): архитектура прокси (baresip→FastAPI WS→Яндекс Realtime WS), 5 этапов миграции, риски.
- `deploy/levitan_turnbased.py`: класс `RealtimeDialog` (DRAFT, GA-форма сессии, `gpt-realtime-2.1-mini`).
- `docs/active_tasks.md` — обновлено с блокомером Яндекс Cloud ключа.
- `docs/adr/ADR-001-yandex-realtime.md` + `checkpoints/chp_20260709_1006.md`.

**Global AGENTS.md — новые команды**
- `/goal` — декларативный цикл до измеримого результата (метрика+target→baseline→итерировать TDD→стоп/эскалация→`memory_add kind=decision`).
- `/code-review` — выделенный Two-axis Review: `git status/diff`→полное чтение файлов+вызовы→Standards+Spec→список рисков→не коммитить без подтверждения→`memory_add kind=bugfix` при блокере.

### Блокеры
- **Яндекс Cloud аккаунт/ключ** — сервисный аккаунт с 4 ролями (`ai.speechkit-stt.user`, `ai.speechkit-tts.user`, `ai.languageModels.user`, `ai.models.user`) + `YC_API_KEY`/`YC_FOLDER_ID`. Нет аккаунта — задача на след. сессию.

### План на завтра
1. Создать Яндекс Cloud аккаунт + сервисный аккаунт с 4 ролями → выдать ключ → в `.env`.
2. `deploy/levitan_realtime.py` (FastAPI WS-прокси, TDD) + `tests/test_realtime_proxy.py`.
3. `greeting_bridge` baresip: PCM FIFO + `auresamp` 8k↔24k + `echoCancellation`.
4. `SYSTEM_PROMPT` под голос (убрать JSON, роль/факты/цены/запреты, tool `save_lead`).
5. Function calling `save_lead` → CRM API.
6. Тест: latency <500мс, barge-in, эхо, лид в CRM. Трёхзвенка — fallback.

---

## 🌞 2026-07-09 (3-я сессия) — Habr Digests + Evals + GPT-Live vs Realtime

**Статус:** Сессия завершена.

### Сделано

**1. Обработка дайджестов**
- 🔑 **TG токен @Angella26bot**: найден заблокированный (401), заменён на рабочий в 6 файлах (`~/.env`, `watchdog.py`, `send_report_today.py`, `projects/ai-eggs/.env`, `projects/agent-lab/.env`, `projects/angel-backend/.env`). Бот ожил.
- 📡 **ABR INTELLIGENCE 02.07**: SkillSpector (NVIDIA, 12K stars) — `uv tool install skillspector`. Просканированы 14 скиллов — 0 находок. Добавлен в архитектурные заметки ACTIVE_TASKS.md.
- 📖 **HABR DIGEST 01.07**: LLM Wiki (Карпатый) — перестроить KB Angela по паттерну raw/wiki/index/ingest/query/lint. Добавлен в план (HD1).
- 📖 **Статья "Claude-агент без LangChain"** — сравнить с архитектурой Angela (ABR4).

**2. Статья "RAG на кончиках пальцев" (22.06 → `angela_agents.py`)**
- `faq_synonyms.json` — 27 negative synonyms (бройлеры ↔ утки, корм ↔ аптечка, москва ↔ краснодар и др.)
- `faq_aliases.json` — 40 записей морфологических алиасов (утка/уток/уточки, справка/справки и др.)
- `lookup_faq()` переписан: fingerprint + aliases + negative synonyms с penalty 2x при score ≥ 0.83
- Починены Router keywords: `опрос`→pro, `пород`→breeds, `налич`→availability, `функционал`→dev, `сколько сто`→pricing
- Шумовые слова: `расскажи`, `привезите` добавлены в noise filter

**3. Статья "Evals" (23.06 → глобальный CI-слой)**
- `tests/eval_angela.py` — 47 тестов: Router regression (9) + capability (11) + FAQ точность (7) + aliases (12) + negative (3) + nomatch (4). 100% pass.
- Pre-commit hook — авто-прогон при изменении `angela_agents.py` или `faq_*`
- `scripts/install-eval-hook.sh` — универсальный установщик для любого проекта
- `AGENTS.md` (глобальный): пункт 9 — Eval-driven development (каждый проект → eval suite, прогон перед изменениями)
- AGENTS.md ai-eggs: добавлена строка про eval suite в блок LLM каскада

**4. Сравнение GPT-Live vs Яндекс Realtime для Levitan**
- GPT-Live (gpt-realtime-2.1): $4/мин audio → ~$240/час. Из РФ — через VPN. Качество диалога выше.
- Яндекс Realtime: бесплатные кредиты, РФ без прокси, <500ms, родной русский.
- **Вердикт:** Яндекс Realtime — единственный реалистичный вариант для Levitan сейчас. GPT-Live рассматривать только после починки VPS и стабильной инфраструктуры.

### Архитектурные решения
- Eval-driven development — для всех проектов, текущих и будущих
- FAQ matching: fingerprint + aliases + negative synonyms (статья IgMamont)
- Eval suite как CI-слой AI-систем (статья artarasov)

### Блокеры
- VPS 185.39.206.145 — не отвечает (100% packet loss)
- SIP audio на Mac — тишина (Mango employee 22 timeout ~0 сек)
- Яндекс Cloud ключ — нет аккаунта для Realtime

### План на завтра
1. Создать Яндекс Cloud аккаунт + сервисный аккаунт с ролями STT/TTS/LLM → ключ в `.env`
2. `deploy/levitan_realtime.py` (FastAPI WS-прокси, TDD)
3. Angela: eval для Generator (LLM-as-judge, HD5)
4. LLM Wiki: перестроить KB Angela по паттерну Карпатого (HD1)

---

## 🔧 2026-07-12 — P1 cleanup + GeekNeural token-dedup

**Статус:** Сессия завершена.

### Сделано

**1. P1 — приведение кода в порядок (4 сабмодуля + root)**
- `ai-grant-portal-temp` (`685aad3`): AGENTS.md, config, лендинг.
- `ai-scout` (`133ab1e`): скилл `extruct-api` → `project-skills/`, +content-pipeline, video_clipper.
- `ai-eggs` (`76f828a`): удалены 4 генератора + тесты; +eval suite (**47/47 passed**).
- `angel-backend` (`72d35f8`): agentic RAG, mem0, autodial, call-quality monitor; `.gitignore` пополнен артефактами.
- root (`4aa79c7e`,`a3415565`,`5387c294`): bump указателей, chp.md 07-09, extract/enrich adygea leads, gitignore PII.
- Безопасность: секреты не попали (`.env` игнорируются; `adygea_leads/` PII → gitignore). Дубликат-чекпоинт удалён.

**2. GeekNeural — движок дедупликации контекста (4 уровня)**
- Одно и то же содержимое в сессии передаётся модели **один раз**; повтор → короткая ссылка `gn:<hash>`.
- Единое ядро `tools/geekneural/core/dedup.py` (чистый stdlib, без зависимостей).
- Уровни:
  - 1. shell-hook `shell/hook.sh` (`gn read|cat|stats|clear`, bash+zsh) — **работает**;
  - 2. MCP-сервер `mcp_server/server.py` (stdio JSON-RPC, чистый stdlib) — **зарегистрирован в `opencode.jsonc`**;
  - 3. браузерное расширение MV3 `browser/extension/` (дедуп вставок в ChatGPT/Claude/Gemini);
  - 4. IDE-плагин VS Code `ide/vscode/` (команда «копировать файл в чат (дедуп)»).
- volatile/real-time (`*.log`, `/tmp/`, runtime-JSON) сознательно **не дедуплицируются**.
- Тесты: unit (6) + MCP-интеграция — оба green.
- Коммит: `5b4d3273`. ADR-001 + README.

### Демо (реальные цифры)
- 5 чтений 11 КБ-файла → **79.9%** экономии (~11 059 токенов).
- Сценарий агента (3 файла проекта + 2 перечитывания): 257 630 байт → 132 024, **48.8%** (~31 401 токенов).
- На стабильном контексте — до **~92%**.

### Архитектурные решения
- MCP на чистом stdlib JSON-RPC (без `mcp` SDK) — переносимо, без сети/установки.
- Кеш сессии в `~/.geekneural/cache.db` (локально, **без телеметрии**).
- Дедуп через content-addressed sha256; статистика персистентна в БД между процессами.

### Блокеры (ждут пользователя)
- VPS `185.39.206.145` лежит (100% packet loss) → боты оффлайн.
- Ключ SSH отвергнут на `72.56.38.19` → добавить `id_ed25519.pub` в `authorized_keys`.

### План на завтра
1. **Перезапустить OpenCode** → подхватит MCP `geekneural` (`cached_read`).
2. Восстановить VPS / сменить хостинг; добавить SSH-ключ на `72.56.38.19`.
3. Задеплоить накопленные изменения сабмодулей на живой VPS.
4. (опц.) Загрузить браузерное расширение и VS Code-плагин.

---

## 🏁 finish-day — 2026-07-14 (глобальная сессия завершена)

### Итог сессии
- **Механики Чена (все 5) внедрены** в экосистему агентов `freelance-agent/.agent/agents/`: #1 soul.md · #2 smart RAG top-K · #3 self-terminating loop · #4 self-healing prompt · #5 memory compaction. Замкнут self-improving цикл: `generate → heal → smart-RAG recall → soul evolve → memory compact`.
- **Фикс pre-existing бага:** `enrich_context` никогда не инжектил память (итерация строки как dict'ов) + сетевой хэнг в тестах (`api_key=None` vs `""`).
- **Тесты:** 70 passed за 27s (было 50). +soul 10 +healing 7 +compact 5 +smart_rag 8.
- **Коммит:** `c41613be` (feat agents self-improving loop) → запушен в `main`.

### Чекпоинт
- Скопирован: `checkpoints/chp_20260714_XXXX.md`.

### НЕ закоммичено (вне сегодняшней сессии — ждёт пользователя)
- `projects/levitan/data/campaigns/csv/adygea_grain_*.csv` — содержат PII (телефоны лидов); игнорируются только для новых файлов, но уже отслеживаются → `git add -A` перезапишет PII. НЕ трогать без подтверждения.
- `projects/ai-scout` (untracked), `projects/angel-backend` (submodule pointer) — чужой WIP, не из этой сессии.
- `reports/geo_monitor_launchd.err` — лог рантайма (volatile).

### Следующие шаги (по желанию)
1. Решить судьбу levitan CSV: либо `git rm --cached` + gitignore, либо отдельный коммит без PII-полей.
2. Перезапуск OpenCode для MCP geekneural.
3. Восстановить VPS / SSH-ключи.

---

## 📝 Session 2026-07-15

### Контекст
Сессия в двух фокусах: (1) прокачка экосистемы агентов `freelance-2026` — превращение разрозненных проектов в связанный «цифровой конвейер»; (2) подготовка откликов на вакансии AI-направления.

### Что сделано
**Накопление знаний (claude-mem, проект `ai-bureau`):**
- `t.me/dailyprompts/8939` (Cloudflare Drop — публикация статик-сайтов без аккаунта) → заложено в arsenal.
- PDF Google Cloud «AI agent trends 2026: CX» (5 трендов) → разложено по зонам (срочно/наполовину/отложить) + применимость к Levitan; заложено в knowledge library.
- Глобальный аудит всех агентов/систем → выявлены top-3 приоритета; главный — **связность агентов (п.1)**, а не фичи отдельных проектов.

**П.1 — A2A-шина (проснули «спящий» протокол в `projects/ai-eggs/agent/`):**
- `a2a_protocol.call_agent()` — синхронный запрос с ожиданием ответа (A2A inbox).
- `a2a_registry.py` — реестр агентов + декоратор `@agent`.
- `a2a_agents.py` — зарегистрированы `echo`, `scanner` (bitrix_scan), `reporter` (daily_report).
- `a2a_dispatcher.py` — активный цикл (poll-loop), флаг `--traces`.
- `a2a_smoke.py` — тест: ✅ «ШИНА ЖИВАЯ».

**Gemini Enterprise (анализ страницы):** не парадигмальный сдвиг, подтверждает наш курс; выявлены пробелы у нас — Governance, Observability, Long-running agents.

**Governance + Observability (достроили):**
- `a2a_governance.py` — HMAC-подпись, `scan_injection`, `check_message` (warn-mode, `A2A_GOVERNANCE_ENFORCE=0`).
- `a2a_observability.py` — трассы (`traces.jsonl`) + `summarize()`.
- dispatcher/protocol пропатчены; `a2a_gov_smoke.py` — ✅ 3 сценария (легит/инъекция/подделка).

**pm2 + кросс-проектные агенты:**
- pm2 установлен локально (`~/.npm-global/bin/pm2`); `ecosystem.a2a.config.cjs` → демон `a2a-dispatcher` (online, 0 restart).
- В реестр добавлены `levitan` (Voice Dialer, health-probe через свой venv) и `hh` (HH.ru Agent, health-probe через свой venv); кросс-проектный `call_agent` проверен.

**Autopilot подключён к шине (оркестратор):**
- `autopilot.py` переписан в v4 (публикует задачи через `delegate_task`, health-check всех агентов, TG-отчёты); убрана зависимость `schedule` (datetime-loop).
- `ecosystem.autopilot.config.cjs` → демон `a2a-autopilot`; `autopilot` добавлен в `A2A_TRUSTED_SENDERS`.
- Стартовая рутина: 5/5 агентов alive; `pm2 save` сделан.

**Отклики на вакансии (3 письма):**
- «Руководитель службы заботы о клиентах» (PETSEE) — упор на управление 5–15 людьми 2+ г. и EdTech.
- «Специалист по внедрению искусственного интеллекта» (городской портал).
- «AI-специалист» (офис м. Бабушкинская, 5/2) — упор на обучение сотрудников, внутренний консалтинг, регламенты/тренды, измеримый результат.

### Блокеры
- Нет. Техническая часть поставлена на паузу по просьбе пользователя.

### Статус pm2 (на момент паузы)
- `a2a-dispatcher` — online; `a2a-autopilot` — online; агенты: echo, scanner, reporter, levitan, hh = 5/5 alive.

### Следующие шаги (по желанию)
1. Расширить обработчики `levitan`/`hh` с health-probe на реальные действия (обзвон / отклики).
2. Замкнуть agent→agent loop: scanner находит забытую сделку → делегирует autopilot → TG владельцу.
3. Прод-режим Governance: прописать `A2A_SECRET` + `A2A_GOVERNANCE_ENFORCE=1` в `.env` (ai-eggs).
4. Отправить подготовленные отклики на вакансии.

---

## 🌙 2026-07-15 (вечер) — HH-агент: серия follow-up Q&A + 2 отклика

**Статус:** Сессия завершена (finish-day).

### Контекст
Режим ручного HH-агента: пользователь задаёт уточняющие вопросы для анкет/переписки + вставляет новые вакансии. Все ответы в v3 стандарте (`prompts/cover_letter_v3.txt`).

### Сделано (follow-up Q&A — аргументированные ответы для анкет)
1. **Tilda/HTML лендинги по промпту** — делал на HTML (не Tilda); контент-пайплайн + макеты в Google AI Studio/Stitch.
2. **n8n для объединения сервисов** — n8n не использовал; аналог (A2A-шина, Python/pm2) связывает Bitrix24/Mango/Avito/VK/Google Sheets.
3. **Иная работа с API** — Bitrix24 REST/webhooks, Mango, Avito/VK, Google Sheets, 1С/МойСклад; AI для генерации/чинки интеграций.
4. **Вайбкодинг сервисов (php/js/python)** — Python (основной), JS/HTML, PHP; промпт→код→pm2/cron→observability.
5. **Вайбкодинг Telegram apps** — боты (aiogram), Mini App (HTML/JS+FastAPI), фронт к A2A-шине.
6. **CRM Битрикс/Амо** — Битрик24 автономно (сканер сделок), Амо по тому же паттерну.
7. **Аналитика через выгрузки/таблицы/сбор** — Google Таблицы (API, автоотчёты), выгрузки CRM, сбор с площадок, LLM-выводы.
8. **Аналитика в спецсервисах** — Langfuse (observability LLM), Mango (STT/классификация звонков), Битрик24, Avito/VK.
9. **Проектирование интерфейсов / Axure** — слоистые макеты в Stitch/Google AI Studio; Axure не использовал (зона роста Figma); прототипы на коде.
10. **Технические задания** — десятки за 2+ года (интеграции, AI-агенты, боты, лендинги, автоматизация, стандарты); приёмка по критериям ТЗ.
11. **Языки/фреймворки (аргументированно)** — Python, JS/HTML, PHP, SQL, prompt-инжиниринг; FastAPI/Flask, aiogram, ≈LangChain/LangGraph, ≈Langfuse, Docker/Linux/pm2; честно: Figma/SPA — не core.
12. **CMS** — интегратор Битрик24 (API), базово WordPress (PHP/webhook), основной — самописный headless на Python/HTML; Tilda не использую.
13. **+1ч рабочего дня** — уточнить контекст/компенсацию, автоматизировать рутину через ИИ, перепроверить приоритеты, зафиксировать в трекере, ТК РФ при сверхнорме.
14. **Случайная печать «аштшыр»** — проигнорирована как шум.

### Отклики (v3)
- **«Философт и Железно» — ИИ-внедренец внутри компании** — упор на A2A-шину, MVP на коде, интеграции (Bitrix/Mango/Avito/1С), RAG, боты, обучение+безопасность; метрика −69% токенов.
- **«Сикса» (азиатские товары) — контент-менеджер/SMM** — контент-конвейер, ИИ для текста/визуала (Stitch/нейросети), презентации, контент-план+автоотчётность.

### Статус
- HH-агент в ручном режиме; `main.py cover` доступен, не использовался.
- Кандидат-профиль (`candidate_profile.txt`) актуален: Avito/VK раздельно, Stitch, Figma-зона роста.

### Следующие шаги (по желанию)
1. Вернуться к тех-задачам A2A (расширить обработчики levitan/hh, замкнуть loop).
2. Отправить подготовленные отклики; при необходимости адаптировать под конкретную анкету.
3. (опц) Записать серию follow-up Q&A в кандидат-профиль как FAQ-блок.

---

## 🌞 2026-07-18 — VPS восстановление (Timeweb wy07319) + runbook

**Статус:** Сессия завершена (finish-day). Доступ к VPS в процессе восстановления поддержкой.

### Контекст
Пользователь зашёл в ЛК Timeweb и обнаружил: сервер `72.56.38.19` (где живёт `vezemcip.ru`) **отключён за неуплату**. IP уже переиспользован другим клиентом (поэтому пинг/HTTP/200 шли — но это чужой сервер, не наш). Наш сервер лежит в аккаунте `wy07319`, отдельном от `Vezemcip@yandex.com`.

### Сделано
- **Найден правильный аккаунт Timeweb Cloud:** `wy07319` / `Incubator2026` (записан в `projects/ai-eggs/.env` → `TIMEWEB_CLOUD_LOGIN` / `TIMEWEB_CLOUD_PASS`).
- **Доказано через whois:** `72.56.38.19` = `netname: TW-Cloud`, `org-name: JSC "TIMEWEB"`, `abuse@timeweb.ru`. Сервер точно Timeweb Cloud, но в другом аккаунте.
- **Диагностика доступа:** старый root-пароль `zE4qDJb-+Y+rv+` и SSH-ключ `antigravity_agent` НЕ подходят к нашему (выключенному) серверу. Порты ботов `8085`/`8086` закрыты; сайт vezemcip.ru отдаётся чужим nginx.
- **Подготовлен RUNBOOK восстановления:** `projects/ai-eggs/docs/RECOVERY_RUNBOOK.md` — Шаг 0 (доступ через панель) → Шаг 1 (сайт Laravel/nginx) → Шаг 2 (PM2-агенты через `resurrect.sh`) → Шаг 3 (порты 8085/8086) → Шаг 4 (чек-лист) → fallback (новый сервер + DNS).
- Найден SMTP-пароль `incubird@yandex.ru` = `Cipcip2025` (app-pass, из `.env`).

### Архитектурные решения
- `vezemcip.ru` = отдельный Laravel «sk_sokol» на nginx (НЕ PM2-агент). Восстанавливается отдельно от ботов.
- Доступ к восстановленному серверу возвращаем ТОЛЬКО через панель `wy07319` (сброс пароля root ИЛИ вставка SSH-ключа `antigravity_agent`).
- При первом подключении — обновить `known_hosts` (`ssh-keygen -R 72.56.38.19`).

### Блокеры
- ❌ VPS `72.56.38.19` наш — выключен. Ждём ответ поддержки Timeweb (ticket открыт; деньги на оплату есть). После восстановления — прогнать RECOVERY_RUNBOOK.

### Следующие шаги (по желанию)
1. Как поддержка поднимет сервер — Шаг 0→4 из `RECOVERY_RUNBOOK.md`.
2. Добавить SSH-ключ `antigravity_agent` в панель, проверить порты 8085/8086.
3. Запустить `resurrect.sh` и убедиться, что pm2-процессы online.

---

## 🌞 2026-07-22 — RAG: платные эмбеддинги (OpenRouter) + тестирование всех функций

**Статус:** Сессия завершена (finish-day).

### Сделано
1. **vector_db.py — OpenRouter embedding (text-embedding-3-small, 1536d)** вместо локального fastembed (384d). Качество поиска: 0.630 → **0.803**.
2. **fastembed оставлен как fallback** (384→1536 padding) при недоступности OpenRouter.
3. **DB schema пересоздана** с vector(384) → vector(1536). 139 документов реиндексированы.
4. **Комплексное тестирование всех функций:**
   - vector_db: ✅ health=True, search=0.803, exact match first
   - faster-whisper (tiny): ✅ транскрипция 2s → текст
   - call_transcriber/call_analyzer: ✅ импорт без Gemini
   - habr_intelligence: ✅ Gemma-4 429 → Nemotron-3 200 → TG
   - daily_report: ✅ Gemma-4 429 → Nemotron-3 200 → TG
   - scheduler (PM2): ✅ 3 процесса online
   - crontab (night audit): ✅ 02:00 MSK
   - night audit (Grok 4.5): ✅ отчёт 5.7KB

### Архитектурные решения
- RAG (ответственный компонент) — платные эмбеддинги OpenAI `text-embedding-3-small` через OpenRouter ($0.02/1M токенов). Бесплатный каскад — для неответственных задач (daily_report, habr_intelligence).
- Fallback fastembed (384→1536 padding) для аварийного режима.

### Файлы
- `agent/vector_db.py` — OpenRouter primary + fastembed fallback

---

## 🌞 2026-07-22 — Freelance Agent: восстановление + отклики на Kwork

**Статус:** Сессия завершена (finish-day).

### Сделано
1. **Freelance Agent — восстановлен с нуля** (был архивный скелет с утерянными исходниками):
   - Создана Python-структура: `mcp_servers/proposal_engine/` (ScoutAgent + генератор)
   - ScoutAgent: категоризация задач (10 типов), скилл-матчинг (0-1), clarity score (0-1), оценка выполнимости
   - ProposalGenerator: отклик с 30% работы + дорожная карта, через LLM (DeepSeek Chat на OpenRouter)
   - CLI: `python main.py propose` и `python main.py analyze` с поддержкой --file, --title, --budget
   - Копирование профиля кандидата из HH-агента

2. **Настройка под Kwork**:
   - Формат: чистый текст без markdown, столбик для дорожной карты
   - Ценообразование: 1500 руб/ч, подстраивается под бюджет
   - "Вы получите" вместо "Что получит заказчик"
   - Только текст отклика, без примечаний

3. **Сгенерировано 3 отклика**:
   - Автоматизация CRM Bitrix24 (уведомления в мессенджеры)
   - План интернет-рекламы (Яндекс Директ, VK, OK, Дзен, МАКС, Avito)
   - Рерайт текстов 10 000 знаков, уникальность 95%

### Файлы
- `projects/freelance-agent/main.py` — CLI точка входа
- `projects/freelance-agent/mcp_servers/proposal_engine/scout.py` — ScoutAgent
- `projects/freelance-agent/mcp_servers/proposal_engine/generator.py` — генератор откликов
- `projects/freelance-agent/config/settings.py` — настройки
- `projects/freelance-agent/config/profile.json` — профиль навыков
- `projects/freelance-agent/prompts/candidate_profile.txt` — профиль кандидата

### Архитектурные решения
- Python вместо TypeScript (как HH-агент, единый стек)
- MCP-архитектура для модульности (proposal-engine, platform-adapters, learning-engine)
- Ценообразование: 1500 руб/ч базовая ставка, подстраивается под бюджет заказчика
