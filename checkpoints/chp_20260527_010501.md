# 🏁 ЧЕКПОИНТ: 2026-05-27 00:50 MSK

## 🔧 Главная работа: Автодозвон — починка отчёта в ТГ

### Что починено (3 слоя багов)

#### 1. TG-бот (tg_bot.py) — отчёт не приходил / был пустой
- **Timezone mismatch**: VPS в UTC, Mac в MSK (+3) → CSV timestamps парсились со сдвигом 3ч → все результаты отбрасывались как "старые". Фикс: `datetime.replace(tzinfo=timezone.utc)`
- **Дубли CSV**: `cat` 3 файлов → двойные заголовки ломали DictReader. Фикс: читаем только `/opt/data/mango/call_results.csv`
- **SSH без ключа**: добавлен `-i .ssh_agent_key`
- **Poll логика**: `unclear` больше НЕ считается финальным ответом — бот ждёт `confirmed`/`cancelled` или таймаут 5 мин
- **Отчёт 4 категории**: ✅ Подтвердили | ❌ Отказали | ❓ Неразборчиво | ⚠️ Нет ответа

#### 2. STT Monitor v2 (dtmf_monitor.py на VPS) — STT слышал мусор
- Whisper **tiny → base** (лучше на телефонном аудио 8kHz)
- **Crop последние 8 сек** (убираем IVR-проигрыш, оставляем ответ человека)
- **Resample 8kHz → 16kHz** (нативный для Whisper)
- **Fuzzy Levenshtein классификатор**: ловит "подквердая" → "подтверждаю" (dist≤4)
- Тесты: 11/11 ✅ на реальных примерах из логов
- Задеплоен и рестартован на VPS

#### 3. Конфликт двух ботов
- angela-bot на VPS конфликтовал с локальным тестовым ботом
- VPS angela-bot остановлен для тестирования

### Статус
- **Бот запущен локально** (PID в /tmp/tg_bot_test.log)
- **VPS angela-bot остановлен** (pm2 stop) — ВКЛЮЧИТЬ после тестов!
- **dtmf-monitor v2 на VPS** — работает (pm2, base модель)
- **Тест не завершён** — нужен финальный прогон CSV → звонок → DTMF/STT → отчёт

### Файлы изменены
| Файл | Что |
|------|-----|
| `ai-eggs/agent/tg_bot.py` | Timezone fix, single CSV, poll logic, 4 categories |
| `ai-eggs/agent/dtmf_monitor_v2.py` | Локальная копия нового монитора |
| `/opt/dtmf_monitor.py` (VPS) | Задеплоен v2: base + crop + fuzzy |
| `/opt/dtmf_monitor.py.bak.*` (VPS) | Бэкап старой версии |

### ⚠️ НЕ ЗАБЫТЬ
1. **Финальный тест** автодозвона: CSV → звонок → подтверждение → отчёт ✅/❌
2. **Включить angela-bot на VPS**: `pm2 start angela-bot`
3. Claude-mem — плагин для Claude Code, не для Antigravity. Qwen MEMORY.md есть в проекте.

## 📜 Сессия
- Время: 23:28 — 00:50 MSK (1ч 22мин)
- Хроника: `chronicles/chronicle_2026-05-26.md`
