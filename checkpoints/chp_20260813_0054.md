# Context — последняя сессия

## 📖 2026-08-12 — Внешние материалы → AGENTS.md п.13 + ACTIVE_TASKS (HB/NB/OB/HG/SM)

**Статус:** Сессия завершена (finish-day).

### Сделано (Fixed + Verified)

1. **AGENTS.md п.13** — «Верификация перед внедрением внешних фич (анти-авторитет)»: не принимать утверждения авторов видео/статей/постов на веру; проверять в открытых источниках (gh search/web/docs/issues) ДО записи «к внедрению»; статусы «предложено автором → проверено (src) → принято/отклонено». Запись в `self_improve_log.md` (2026-08-13, сигнал feedback).
2. **Habr 1069180** → HB-1..4: GEO zero-click, ИИ-агент отзывов (LLM-кластеризация), карта смыслов (3 источника), статусная аналитика (ClientID→CRM→offline).
3. **TG neurobussines/3011** → NB-1..4: Skill-Guide валидирует HT-2 (реальные вызовы скиллов), «80% дешевле» валидирует cost-cascade, OpenMontage (ref video-clipper), бригада субагентов-монтажёров.
4. **YouTube 3afX6vZ3zaM** → OB-1..3: Obsidian как БЗ агентов. OB-1 (YAML frontmatter) ВЕРИФИЦИРОВАН (James Croft/Nick Liu/OKF/OSS mcp-obsidian/mcpvault) → ПРИНЯТО с оговоркой.
5. **Habr 1069372** → HG-1..5 (GEO-плейбук, ВСЁ проверено по п.13): llms.txt МАРГИНАЛЕН (Ahrefs 97% не читаются, SE Ranking 0 корреляция) → НЕ приоритет; Schema.org JSON-LD ПРИНЯТО; факты/цифры (Princeton +32%/+41%) ПРИНЯТО; brand mentions ПРИНЯТО; методика измерения GEO ПРИНЯТО.
6. **Habr 1069782 (simpleone)** → SM-1..3: агент-критик «создатель+критик» ВЕРИФИЦИРОВАН (PairCoder ACL 2026: 91% pass@1, +20.3%, −40-70% токенов; DebateCoder ACL 2025) → лечь на /code-review/Fable 5/validation-layer/MH. Low-code-питч erStudio отклонён как вендорская реклама.
7. Ревизия MCP-тулов (MA-1.1): ~105 тулов ≈ 10K токенов/запрос; firecrawl+notebooklm → `enabled:false` (MA-1.2), остались claude-mem+geekneural+transcribe. MA-4: `tools/context_bridge.py` (CLI, не MCP; тест заблокирован: OmniRoute 503 / OpenRouter 403).

### Осталось (следующая сессия)
1. Рестарт opencode — активация MA-1.2 / skills-lib / AGENTS.md п.13 (hot-reload отсутствует).
2. MM-2/MM-3: DASHSCOPE/SERPER ключи (блокеры), MM-1 core ждёт vision-задачи.
3. MH-1..4 selective voting / HR-1..3 Herdr — только при реальной потребности (worktree-изоляция обязательна).
4. OB-2: vault/00-Inbox; SM-2/3 сверка ролей и стека.

---

## 🛡️ 2026-08-09 (ночь) — OmniRoute: лимиты, каскад, модели OpenCode

**Статус:** Сессия завершена (finish-day).

### Сделано (Fixed + Verified)

1. **Диагностика слива $10/20мин в OpenRouter**: комбо `auto/best-coding` имело weight1 = `openrouter/anthropic/claude-sonnet-5`. Механика OmniRoute (из исходников `handleComboChat` + `promoteSuccessfulComboModel`): запрос → модель по весу, при ошибке maxRetries:2 → fallback на следующую; при УСПЕХЕ модель промотируется на #1 и залипает. 40 запросов за 16 мин (18:29–18:45) = 4.78M входных токенов на Sonnet-5.
2. **Комбо `auto/best-coding` перестроено** (free → дешёвые платные → дорогие последними): w1 oc/qwen3.6-plus-free, w2 oc/deepseek-v4-flash-free, w3 blackbox/.../deepseek-v4-pro, w4 blackbox/.../gpt-5.5-pro, w5 openrouter/deepseek-v4-pro, w6 claude-sonnet-5, w7 gpt-5.6-terra-pro.
3. **Лимит $5/день** на ключ Open code (`api_keys.daily_usage_limit_usd=5`, `usage_limit_enabled=1`).
4. **Default model → `omni-auto/auto/free-coding`** (small_model тоже). `auto/best-coding` больше не дефолт.
5. **5 прямых (НЕ комбо) моделей в меню** по нарастающей: Code 1 DeepSeek V4 Pro (openrouter) → Code 2 DeepSeek V4 Pro (blackbox) → Code 3 GPT-5.5 Pro (blackbox) → Code 4 Claude Sonnet 5 → Code 5 Claude Opus 5.
6. **Ling 3.0** (`openrouter/inclusionai/ling-3.0-tiny:free`) → weight 1 в комбо `auto/free-coding` (заменил голову каскада).
7. **Чистка меню OpenCode**: убрана сломанная `auto` (таймаут HTTP 000, нет в каталоге). Все ID сверены с `/v1/models` (1273) + живой запрос. Проверено: free-coding → Ling-3.0, best-coding → deepseek-v4-flash-free.

### Выяснено
- OmniRoute CLI (`omniroute combo`) сломан: `Cannot find package '@/lib'`. Правки комбо/ключей — напрямую SQLite (WAL, сервер живо).
- «Иссякнут» в каскаде priority = все модели дали ошибку/лимит/timeout → 502/503 или global fallback. Переход между моделями — только по отказам, НЕ по сложности.
- Смена модели в OpenCode (`/model`) НЕ теряет контекст сессии.

### Файлы
- `~/.config/opencode/opencode.jsonc` (изменён, вне репо)
- OmniRoute `~/.omniroute/storage.sqlite`: combos + api_keys (вне репо)

### Осталось
1. Перезапустить OpenCode → проверить новое меню моделей (6 пунктов).
2. Проверить утренний TG-отчёт ночного читателя (Фаза 4 NotebookLM).
3. Закоммитить изменения 08-09 (night_reader.sh, notebooklm_scan.py, book_digest.py, nas_*.sh) — висят с прошлого дня.

## 🧠 2026-08-09 (день) — NotebookLM MCP + ночное сканирование + NAS + чистка диска

**Статус:** Сессия прервана пользователем («вернёмся позже сегодня»). Изменения НЕ закоммичены.

### Сделано (Fixed + Verified)

1. **NotebookLM MCP подключён** (jacob-bd/notebooklm-mcp-cli): `uv tool install`, блок в `opencode.jsonc`, авторизация `nlm login --provider openclaw --cdp-url http://127.0.0.1:18800` (Chrome с US-прокси, 54 куки, erik03132@gmail.com). 17 блокнотов, 321 источник.
2. **Прокси-схема решена**: US-прокси 172.120.21.141 режет РФ-IP → локальный форвард `ssh -L 127.0.0.1:64468:172.120.21.141:64468` через VPS (launchd com.antigravity.usproxy-tunnel, KeepAlive). Chrome НЕ умеет креды в `--proxy-server` (ERR_NO_SUPPORTED_PROXIES) → CDP-режим. ssh-tunnel-app (elliotgao) оценён и отклонён.
3. **Ночное сканирование NotebookLM**: `tools/ops/notebooklm_scan.py` (state-файл, 321 источник зафиксирован — с сегодня читаются только НОВЫЕ изменения), `book_digest.py` + US-прокси (free-модели без прокси дают 403), night_reader.sh Фаза 4. Verified E2E.
4. **VPS fail2ban-бан**: частые ssh из РФ → бан :22 (~10-30 мин), KeepAlive-туннели переживают сами. Прямой US-прокси работает при включённом VPN.
5. **Диск: 207Mi → 6.9Gi свободно** (uv cache 4.9G, Genspark 4.2G удалён, кеши Chrome/Edge).
6. **NAS Synology (192.168.0.102)**: шара home → `~/nas_home` (пароль в Keychain, launchd com.antigravity.nas-mount), структура Archive/Video/Backup, перенесены видео + Downloads, ночной бэкап vault+freelance-2026 (03:15, com.antigravity.nas-backup). ВАЖНО: ~/Documents синхронизируется с NAS 2-сторонне (не переносить оттуда вручную).

### Осталось (при возврате)
1. Перезапустить opencode/MCP → проверить `notebooklm_notebook_list` через MCP (конфиг уже обновлён на локальный прокси)
2. Проверить утренний TG-отчёт ночного читателя (02:30) — Фаза 4 NotebookLM (новые источники)
3. Закоммитить изменения (night_reader.sh, notebooklm_scan.py, book_digest.py, tg_bot.py, nas_*.sh, reports/)

---

## 📚 2026-08-08 (вечер) — Obsidian-волт + ночной читатель + VPS-мост библиотеки

**Статус:** Сессия завершена.

### Сделано (Fixed + Verified)

1. **Obsidian-волт построен** (`~/freelance-2026/vault/`): 00-Inbox, 01-Projects, 02-Knowledge, 03-Lessons, 04-Decisions, 05-Audits, 06-Library (fiction/non-fiction/media/papers/books), 10-Daily, _templates, _index/_catalog. Obsidian 1.13.4 установлен, `.obsidian/` минимальный. Перенос: adr→04-Decisions, solutions→03-Lessons, audits→05-Audits (15 файлов, git-rename), ACTIVE_TASKS пути исправлены, lesson_capture.py → vault/03-Lessons.
2. **obsidian_mirror.py** — claude-mem → волт (kind→папки: session-summary→10-Daily, decision→04-Decisions, bugfix/lesson→03-Lessons, audit→05-Audits), инкрементально, --force.
3. **book_digest.py** — книги → digest.md (free-каскад nemotron-3-super-120b → ultra-550b → gpt-oss-20b → gemma-4-31b), push claude-mem kind=discovery (lesson невалиден), E2E проверен.
4. **night_reader.sh** — Фаза 0 rsync-инбокс, 1 конвертация (агентский+личный инбокс), 1b раскладка Входящих по имени (NFC-нормализация, рецепты→Кухня и т.д., мусор удаляется), 2 чтение книг (агентские+личные в КНИГИ/<slug>/), 3 каталог+TG-отчёт. Первый прогон: 33 заметки разложены. launchd 02:30 (com.antigravity.nightreader).
5. **Реорганизация Obsidian**: «Клей Obsidian» закрыт → Личное/Проекты/Клей (архив)/, вытащено не-клеевое (E409, Вьетнам, Тату, Знания/, КНИГИ/AI BOOK и др.), битые имена починены, мусор удалён.
6. **VPS-мост (msk-1-vm-si6t, 217.149.23.113)**: задеплоены fetch_url.py (html→md/file→bin/video→summary → /opt/vault_inbox) + tg_bot.py с handle_inbox_link (URL-ссылки из TG → библиотека ночью). angela-bot починен (crash loop 1034 × из-за устаревшего bot.lock). sync_inbox.sh + night_reader Фаза 0: туннель 127.0.0.1:22001 → fallback прямой хост. E2E: файл с VPS в vault/00-Inbox ✅.
7. **Туннель стабилен**: launchd com.antigravity.vps-tunnel (KeepAlive, ssh_tunnel.py через US-прокси CONNECT → VPS:22). Переживает рестарты/перезагрузку.
8. **OmniRoute O1-O8 закрыты** (старый блок ACTIVE_TASKS): локальный :8123 жив (15 free-моделей, vps_alive=true), VPS :20128 pm2 online, watchdog/комбо-модели готовы.
9. **igorek-core.md** — компетенция «Библиотекарь»; AGENTS.md правило 0.1 «агенты читают только md-версии»; корпус books-ai-agents создан.

### Выяснено
- SSH к VPS идёт ТОЛЬКО через US-прокси (172.120.21.141:64468 HTTP CONNECT / 64469 SOCKS5), прямые порты закрыты.
- В 19:30 прокси временно лёг (CONNECT timeout на оба порта) — SSH-доступ к VPS лежит до оживления прокси; локальный OmniRoute от этого не зависит.
- claude-mem: kind=lesson не проходит валидацию — для книг kind=discovery.

### Осталось (следующая сессия)
1. Патчи OMNIROUTE_PATCHES.md на VPS (contextFilterMode lenient + handoff sessionId) + проверить oc из DEGRADED (ждёт прокси)
2. Проверить ночной прогон night_reader (02:30) — ожидаем digest первой книги
3. AN-2: интеграция anydoc в пайплайн Фемиды
4. YU-6: 5 спорных пунктов кросс-чека → Фемиде (Роспатент/депонирование, ст.1298, GPL)

### Приоритеты на следующую сессию
- Ночной прогон ночного читателя (02:30) — проверить утром
- Оживление прокси → VPS-патчи OmniRoute
- AN-2 (anydoc в Фемиду)

---

## 🛡️ 2026-08-08 — OmniRoute: watchdog + live-monitor + модели OpenCode

**Статус:** Сессия завершена.

### Сделано (Fixed + Verified)

1. **watchdog.py** (+ вшит в `omniroute-recover.sh` как systemd-сервис) — мониторинг VPS OmniRoute 217.149.23.113:20128. Эскалация: pm2 restart → hard restart → SQLite reset → reboot. Telegram-уведомления. VPS сейчас недоступен (ping OK, порты 22/20128 закрыты) — watchdog поднимет при оживлении.

2. **server.py (omni-auto-router, :8123)** — 15 бесплатных моделей в TIER_CHAINS[0], VPS health-кеш (60с, иначе 30с таймаут на каждый таргет), исправлен Ollama default (gemma3:4b → qwen2.5:7b), исправлен OMNIR_VPS_URL default (был openrouter.ai — VPS вообще не использовался). `/stats` + `/v1/models` расширены. Проверено: 18 моделей, free-only отвечает.

3. **OMNIROUTE_PATCHES.md** — решение проблем contextFilterMode (обнулять requiredContextTokens в lenient-режиме, код `comboStructure.ts`) и handoff для priority (генерация sessionId из hash сообщений, код `contextHandoff.ts`).

4. **opencode.jsonc** — очищен список моделей: только `auto/free-coding`, `auto/best-coding`, `auto`. Убраны 10 нерабочих (best-fast/vision/chat/reasoning, pro-chat, model/claude-*). Default model → `auto/best-coding`. Verified: обе комбо отвечают (best-coding → blackboxai/deepseek-v4-pro, free-coding → oc/deepseek-v4-flash-free).

5. **omniroute-live.py** — живой монитор реальных моделей (polling `/api/usage/call-logs`). Показывает активные запросы (модель/провайдер/RUN), последние завершённые, сводку. Проверено: виден живой запрос + диагностика (gpt-5.5-pro → 400).

### Выяснено

- **OpenCode в статус-баре показывает только выбранное комбо** (`auto/best-coding`), не реальную модель фолбэка. Реальная модель записывается в message-данные сессии (`{"providerID":"opencode","modelID":"deepseek-v4-flash-free"}`).
- **oc (opencode) провайдер был в DEGRADED** (11 ошибок) — сброшен через `/api/resilience/reset`. Без сброса бесплатные fallback-модели обоих комбо были отрезаны.
- **blackbox gpt-5.5-pro падает с 400** — поэтому best-coding фолбэчится на blackboxai/deepseek-v4-pro.
- **Баланс OpenRouter = $0** — платные модели best-coding (Sonnet 5, GPT-5.6 Terra Pro, DeepSeek V4 Pro) отваливаются с 402, комбо доходит до blackbox + free. Пользователь решил не чистить (пусть фолбэчатся).

### Файлы
- `tools/omni-auto-router/watchdog.py` (новый)
- `tools/omni-auto-router/omniroute-live.py` (новый)
- `tools/omni-auto-router/OMNIROUTE_PATCHES.md` (новый)
- `tools/omni-auto-router/server.py`, `omniroute-recover.sh`, `.env.example` (изменены)
- `~/.config/opencode/opencode.jsonc` (изменён, вне репо)

### Осталось (при оживлении VPS)
1. Применить патчи OMNIROUTE_PATCHES.md на VPS (contextFilterMode lenient + handoff sessionId для priority)
2. Задеплоить watchdog на VPS (omniroute-recover.sh, уже готов)
3. Проверить, вернулся ли oc из DEGRADED после стресс-тестов

### Приоритеты на следующую сессию
- VPS 217.149.23.113 (уже >24ч недоступен)
- Патчи OmniRoute на VPS
