# ACTIVE_TASKS.md — сводка (переформирована 22.08.2026)

## 🔴 P0-блок (finish-day 2026-08-29 вечер) — инфраструктура OmniRoute/Hermes

> Итог аудита: инфра подтверждена рабочей. Блокеров нет.

- [x] **HTG-001** — Фикс автостарта VPS: `omniroute.service` (failed→active), `hermes-dashboard.service` (crash-loop→active). Все 6 systemd-юнитов active+enabled. Verified.
- [x] **HTG-002** — Найдена путаница портов: `:8890`=самописный (не OmniRoute), `:20128/dashboard`=родной OmniRoute. SSoT зафиксирован.
- [x] **HTG-003** — Hermes Desktop добавлен в Mac Login Items (автооткрытие GUI).
- [x] **HTG-004** — Веб `:9120` = админка (не чат); `/chat`=активация сессии. Общение → Desktop/ТГ.
- [x] **HTG-005** — SSoT-док создан: `~/freelance-2026/INFRA_SSOT_OMNIROUTE_HERMES.md`.
- [ ] **HFM-001** (P1) — Уточнить учётку входа в родной OmniRoute `/login` (что просит форма).
- [ ] **HFM-002** (P2) — Подключить провайдеров в OmniRoute (OpenRouter/Nous API-ключи) — опц.
- [ ] **HFM-003** (P2) — Улучшить кастомный дашборд `:8890` (модели/провайдеры) — опц.

> SSoT активных задач. Старое (июльские планы, HABR-дайджесты, ротация
> секретов, Angela ГОТОВО) вычищено — лежит в `ACTIVE_TASKS.md.bak-20260822`.

## 🏗 АРХИТЕКТУРА (ARCH) — обсуждение, перенесено из VPS-копии 28.08

> Источник: рабочая копия, которую ТГ-бот (профиль femida) записал на VPS
> 25.08, пока Mac-SSoT был недоступен с VPS. Влито в SSoT 28.08. Решение НЕ
> принято — вынести в отдельный урок/созвон.

### ID: ARCH-001 — Архитектура ночной работы агентов и общей базы знаний
- **Статус:** требует обсуждения (не реализовано)
- **Инициатор:** Игорь В. · **Зафиксировано:** 2026-08-25 (Telegram, Hermes)

**Суть:** ночная автономная работа агентов (Шерлок, Батрак, Айболит и др.)
упирается в то, что (1) каждый профиль держит знания изолированно
(`~/.hermes/profiles/<name>/`); (2) результаты ночной работы не попадают в
общую базу автоматически; (3) нет механизма «доучивания» общих скилов/правил.

**Повестка:** где лежит глобальная база знаний (Obsidian? общий репо?
центральный skill-хаб?); правила записи (без конфликтов/перезаписи); роутинг
профилей в Telegram (chat_id 176203333 жёстко на femida → переключение на
batrak не срабатывает, нужна матрица «чат→профиль» или on-demand switch);
ночной конвейер Шерлок→Батрак→SSoT+утреннее уведомление; контроль качества
(Айболит ≠ диагноз/лекарства).

### ID: ARCH-002 — Синхронизация базы знаний Mac ↔ VPS
- **Статус:** требует обсуждения (не реализовано)
- **Инициатор:** Игорь В. · **Зафиксировано:** 2026-08-25 (Telegram, Hermes)
- **Связь:** продолжение ARCH-001, добавляет измерение «два хоста»

**Суть:** агенты живут в двух местах — Mac (день, десктоп) и VPS (ночь,
Telegram-шлюз). Сейчас (1) профили изолированы и на Mac, и на VPS это
**разные копии**; (2) ночью VPS доучивает скилы, утром на Mac — старая версия
(и наоборот) → рассинхрон; (3) роутинг в ТГ жёстко на femida.

**Повестка:** единый источник скилов/правил/памяти (общий репо или центральный
хаб, а не дубли на два хоста); двусторонняя синхронизация Mac↔VPS (cron-хук
push/pull при подъёме/отбое Mac либо общий volume); правило записи (ночная
работа VPS не перезаписывает дневные правки Mac); on-demand switch профиля в
ТГ. **Итог:** проснулся на Mac → вижу ночные наработки VPS; лёг → VPS с теми же
свежими скилами. Без повтора контекста.

> ✅ 28.08: файл задач `ACTIVE_TASKS.md` **добавлен в скрипт синхронизации**
> (`tools/ops/sync_hermes_knowledge.sh`, функция sync_statefiles) — см.
> ARCH-002-fix ниже. Больше не выпадает из моста Mac↔VPS.

## 🔥 ЗАДАЧИ НА СЕГОДНЯ (28.08) — из сессии в Telegram

> Перенесено из вчерашнего обсуждения в ТГ (фрагмент от Игоря). TG-токены для
> всех агентов Игорь получил и передаст позже — шаги, завязанные на токены,
> помечены ⏳ (ждут токены).

### T-01 · Навык живого перевода EN→RU — `translate-to-russian` ✅ СДЕЛАНО
- **Суть:** перевод «по-человечески», без канцелярита/дослова (бесило
  «конвой/вокансии/хабитер»). Был лимит описания навыка ≤60 символов — падал.
- **Сделано:** навык записан файлом `~/.hermes/skills/translate-to-russian.md`
  (description 52 символа). Триггер: «переведи по-человечески / по-русски
  нормально».

### T-02 · Переключение модели на бесплатную ✅ ПРОВЕРЕНО
- **Суть:** Игорь хотел free-модель Hermes. Ранее ставили `hermes-llama-3.1-70b`
  в default/config.yaml (правка одобрена). Сейчас глобально стоит
  `provider: omniroute, default: auto/free-coding-full`.
- **Проверено 28.08:** OmniRoute локально жив (127.0.0.1:20128), free-каскады
  `auto/free-coding-full` и `auto/best-coding` в списке моделей и реально
  отвечают. Paid fallback не требуется. ✅

### T-03 · Регистратор — маршрутизация задач специалистам ✅ ПРАВИЛО ЗАФИКСИРОВАНО
- **Суть:** Игорь называет спеца (Шерлок/Батрак/Айболит) + задачу → Hermes
  распределяет. Шерлок=поиск вакансий (hh.ru, свежие 3 дня), Батрак=отклики
  (Кворк/hh.ru), Айболит=здоровье (без диагнозов/лекарств).
- **Сделано:** правило в `USER.md` профиля chief + в память агента.
- **План:** при первом вызове «регистратор Шерлок найди вакансии» — отработать
  маршрутизацию через навыки sherlock-hh-kwork / batrak-agent.

### T-04 · Айболит + статины — ЗАКРЫТО (вне правил профиля)
- **Суть:** Игорь поручал Айболиту собрать дозировки статинов после шунтирования.
- **Вердикт:** навык `aibolit.md` жёстко «не назначает, не отменяет лечение».
  Задача **против правил профиля** — выполнять нельзя. Перенаправить к врачу
  (генерировать список вопросов к кардиологу, не дозировки).

### T-05 · Главная боль: TG не знает агентов из десктопа ✅ ЧАСТИЧНО (ARCH-001/002)
- **Суть:** контекст не переносится между сеансами TG↔десктоп.
- **Сделано 28.08:** файлы состояния (ACTIVE_TASKS.md и др.) добавлены в
  `tools/ops/sync_hermes_knowledge.sh` → едут Mac↔VPS через `hermes-knowledge-sync`.
- **План:** запушить (нужно «да» Игоря), авто-push на Маке (LaunchAgent), авто-pull
  на VPS (cron, когда VPS поднимем).

### T-06 · Профиль-маршрут chat 176203333 → femida ⏸ НЕ ТРОГАТЬ
- **Суть:** чат жёстко на femida; меню смены профиля сюда не применяется.
- **Решение Игоря:** конфиг не править. Зафиксировано в память + USER.md.

### T-07 · Авто-push синхронизации на Маке ⏳ ФАЙЛ ГОТОВ, ЖДЁТ ПУШ В РЕПО
- **План:** LaunchAgent `com.user.hermes-sync.plist` — `sync_hermes_knowledge.sh push`
  раз в 15 мин. Файл создан (`~/Library/LaunchAgents/`), **загружу только после
  первого ручного пуша** (чтобы репо не приняло пустой/незавершённый коммит).

### T-08 · Поднять VPS (FWaaS блок) 🔴 БЛОК (нужен Игорь)
- **Суть:** VPS 217.149.23.113 недоступен — TimeWeb FWaaS блокирует IP Мака.
- **План:** Игорь добавляет текущий IP (172.120.21.141) в Allow через веб-консоль
  TimeWeb → я поднимаю SSH/туннель и ставлю авто-pull на VPS.

### T-09 · Влить TG-токены для всех агентов ⏳ ЖДЁТ ТОКЕНЫ
- **План:** когда Игорь передаст токены — прописать в systemd override на VPS
  (`TELEGRAM_ALLOWED_USERS` / per-profile tokens), не в логи/чат.

## 🔴 БЕЗОПАСНОСТЬ (постоянно)

- **Не коммитить секреты** (.env, ключи) — правило в AGENTS.md.
- **Write-protect скиллов** (ES-22): `tools/security/lock_skills.sh` —
  активные скиллы read-only (chmod 0444 + git-track). Правки только через `skill_manage`.
- **ai-defender**: летальное трио (ES-25) — агент с секретами ≠ парсящий веб/почту.
  Runtime guardrail (ES-26) + непрерывный red-teaming (ES-27) — в очереди.
- **stealth/ox-alpha** (ES-28): LOW-TRUST модель в OmniRoute. ТОЛЬКО публичный
  код/несекретные задачи. ЗАПРЕТ: клиентский код, Фемида, приватные данные.

## 🤖 Экспертная группа (Hermes профили) — ГОТОВО 22.08

6 профилей (`hermes profile list`): `sherlock` (поиск), `femida` (юр),
`defender` (безопасность), `marketer` (маркетинг), `financier` (финансы),
`health` (Айболит). Маршрутизация в `~/.hermes/skills/expert-council.md`
(авто-раскидывание по теме). Тест: ELI5 Anthropic → Council-вердикт (не берём
как есть, адаптировали как `eli5-explain`).

## 🔴 P0 25.08 — Hermes Telegram profiles + бесплатные каскады

- [ ] **HTG-1.** End-to-end меню `@hermes03132_bot`: `/profiles` → `/sherlock`
  → `Кто ты?` → `/chief` → `Кто ты?`; проверить SOUL/route, не только подпись.
- [ ] **HTG-2.** Проверить journal: `set_my_commands OK`, Telegram connected,
  нет `Conflict`, `duplicate_credential`, `No inference provider`.
- [ ] **HFM-1.** Снять cost baseline через `hermes insights`; сохранить отчёт.
- [ ] **HFM-2.** Починить primary auth локального OmniRoute в профиле `bridge`;
  smoke-test `auto/free-chat`, `auto/free-coding`, `auto/free-reasoning` без
  незаметного ухода в fallback.
- [ ] **HFM-3.** Создать `profile-model-policy.yaml` + идемпотентный CLI
  `--dry-run/apply/verify/rollback`; запретить автоматические paid fallback.
- [ ] **HFM-4.** По одному перевести 11 профилей на free-first матрицу:
  Nous `tencent/hy3:free` → OpenCode (`laguna`/`x-preview`) → профильный
  OmniRoute free-combo; после каждого role/model smoke-test, stop-on-regression.
- [ ] **HFM-5.** VPS: не использовать Mac-local `127.0.0.1:20128`; до проверки
  серверного Nous/OmniRoute оставить Verified OpenCode baseline.
- [x] **HTG-5.** Синхронизация знаний Мак → VPS: приватный репо `hermes-knowledge-sync` (git, secrets excluded), скрипт `sync_hermes_knowledge.sh`. Pull на VPS применён; проверка с VPS подтвердила OK_FLAT, EXPERT_OK, 1-в-1 навыки, gateway active. Репо возвращено в private.
- [ ] **HTG-4.** Добавить тихий daily healthcheck + cost guard; alert только при
  gateway/model failure или появлении paid model в автоматической цепочке.

**Политика:** GPT/Claude и другие платные модели — только ручное включение по
явной просьбе Игоря. Автоматические primary/fallback — только проверенные free.

## 🩺 Айболит (health) — v0.2 ГОТОВО 22.08

- Папка `~/health/`: `knowledge/cardio/` (30 PubMed-абстрактов), `notes/`, `private/`.
- Скилл `eli5-explain` адаптирован под Hermes + протестирован (холестерин).
- Граница: справочник, НЕ врач. Не диагноз, не лекарства.
- **Статин-симптомы** (усталость, боли в мышцах/суставах): записаны в базу,
  ждём похода к врачу — тогда соберу дневник + список вопросов. БЕЗ назначений.
- **ЕМИАС**: выгрузка медкарты — Вариант А (ты скачиваешь, я структурирую).
  Браузерная автоматизация = ToS-риск, отложено.

## 🛠 Установленные инструменты (проверено 22.08)

- **CodeGraph** v1.5.0 (`~/.local/bin/codegraph`) — граф кода, ADR-003 закрыт.
  Пилот на `agent-lab/` успешен. MCP-интеграция в OpenCode — НЕ сделана (интерактив).
- **Defuddle / Caveman / Humanizer** — скиллы в `~/.hermes/skills/`.
- **OmniRoute** — провайдер моделей (localhost:20128). Модели: auto/free-coding,
  DeepSeek V4 Pro, GPT-5.5 Pro, Claude Sonnet/Opus 5, Nous Hermes 4 70B/405B.
- **Ox Alpha Free** — включена в OpenCode Desktop (бесплатная, unlimited).

## 📚 Библиотека кейсов Hermes (`docs/cases/`)

- `INDEX.md` + кейсы: `codegraph.md`, `ivan-ilyich.md` (ЕМИАС), `razbor-servisa.md`,
  `watermarks-remover.md` (в `docs/solutions/`).
- Cron `hermes-cases-weekly`: пн 9:00 МСК — разбор 3-5 свежих кейсов.

## 📌 Отложено / в очереди

- **ES-8d Codebase Memory MCP**, **ES-8e Marketing Skills** — ждут задачу.
- **ES-13 Ornith-1.5**, **ES-14 Qwen-uncensored**, **ES-15 Vercel fx** — локальные
  LLM-рантаймы, кандидаты, не реализуем сразу.
- **ES-12 (21.08) VAD/роутер/ack-фразы/ТТС** — реализовано в `src/levitan/` (детали
  в коде, не дублируем).
- **ES-16..21, 23..27** — паттерны для Фемиды/SVO/Sinergy/RAG — в очереди как PR.

## ❌ Отклонено (не возвращаться)

- youtube-dl-wpf (только Windows), Hermes Bot Mode (дубль), ES-8f..j (Agent Reach,
  Composio, OpenMontage, Oh My Claude Code, Superpowers целиком).
- Privacy Filter (Claude skill) — unlicensed, не локально.
- **YaY Fullstack Engineer (#03, yay-tech.ru)** — тяжёлый тест (5 блоков, голосовой
  разбор, end-to-end под ключ) + замороченный процесс. Игорь: «пропускаем». (25.08)
- GigaAM-v3, AEC-зона, «фабрика знаний», «letальное трио» (CVE-примеры).

## ✅ ИНФРА (проверено 22.08)

- **Time Machine** на NAS DS720 (`Time Mashine`) — работает автоматом (03:00).
- **Прокси**: SSH-туннель `127.0.0.1:64468` (US) + NO_PROXY для РФ-сервисов.
  ExpressVPN, Rewind — выгружены из автозагрузки (оптимизация).
- **Проекты Hermes**: freelance-2026, health, Личное (Obsidian) — привязаны.
- **Локальная LLM (Ollama)**: обновлён до 0.20.3, удалён `qwen2.5:7b` (тормозил 8GB),
  оставлены `qwen2.5:1.5b` (986MB, 7.2сек, основная) + `qwen2.5:0.5b` (397MB, мгновенно).
  Добавлен в `fallback_providers` с `no_proxy` — при отключении интернета Hermes
  **автоматически переключается на локалку** (проверено: отвечает без сети).
- **Skill Router** (`~/.hermes/skills/skill-router.md`): ленивая загрузка скиллов
  по контексту (не preload всё в промпт). Реализация паттерна из Claude Skills API.
- **FreeToken** (arXiv 2608.16157): edge-native MoE serving — кандидат для Ornith-1.5,
  но пока не берём (Ollama + OmniRoute хватает).

## 🧠 ЭКСПЕРТ-СЕССИЯ (идеи, не реализованы)

### ES-N: Сообщество экспертных ботов (ТГ-канал + ВК)
**Суть:** 6 экспертных профилей Hermes (`sherlock`/`femida`/`defender`/`marketer`/
`financier`/`health`) → 6 отдельных Telegram-ботов (через `hermes gateway`,
токены @BotFather). Боты сидят в одном ТГ-канале/группе (+ ВК-сообщество) и
дискутируют между собой (`@mention` агента штатно поддерживается в коде),
вовлекая реальных людей в спор. VPS = 24/7, не зависит от Mac (ложится в
цель «закрыл крышку → агенты работают»).
**Что нужно:** 6 бот-токенов; привязка профиль→бот в gateway; логика дискуссии;
ВК-коннектор (whook/`hermes send`). **РИСКИ:** 152-ФЗ (перс. данные людей в
сообществе), спам-лимиты ТГ на активных ботов, расход токенов (через
OmniRoute/бесплатные — терпимо). **СТАТУС:** идея зафиксирована, реализация
ПОСЛЕ стабилизации базы (агент на VPS + туннель + гейтвеи Mustay).

### ES-N+1: Sinergy → Экспертная группа (доводка идей)
**Суть:** проект `sinergy` (`projects/sinergy/`, projectId `sinergy`) ищет
стартапы и через Blender генерит **новые идеи**. Цель: каждая сгенерённая
идея автоматом прогоняется через экспертную группу Hermes
(`sherlock`/`femida`/`defender`/`marketer`/`financier`/`health`) → на выходе
**готовый практический вариант применения**, а не сырая задумка.
**Где складывать:** (1) резюме идеи + итог экспертизы — прямо в проект
`projects/sinergy/` (напр. `sinergy/ideas/<id>/brief.md` + `verdict.md`);
(2) дублируть короткое резюме в эту экспертную группу (наш чат / chief-сводка).
**Архитектура состыковки:** Sinergy генерит идею → пишет `brief.md` в
`projects/sinergy/ideas/<id>/` → триггер (cron/скрипт/hook) кидает резюме в
экспертный совет через `@mention` всех 6 профилей (шина агентов штатно
поддерживает mention) → каждый даёт verdict в своём ключе → chief-агент
сводит в `verdict.md` → результат виден в `sinergy/` и в чате.
**Что нужно:** hook генерации Sinergy→brief; роутинг в expert-council.md
(добавить `sinergy` как источник); шаблон `brief.md`/`verdict.md`.
**СТАТУС:** идея зафиксирована, архитектура набросана, реализация ПОСЛЕ
стабилизации базы (агент на VPS + туннель + гейтвеи + починка Telegram).

### ES-N+2: Топ-5 GitHub-репозиториев (vibecoding_tg/3736)
**Суть:** пост «Вайб-кодинг» (22.08.2026) — 5 тулз для ИИ-агентов. Council-разбор
(4 роли, факты проверены по GitHub API). Факты: computer 8.6k★/MIT, OpenViking
32.9k★/**AGPL-3.0**, omarchy 29.9k★/MIT, needle 8.9k★/Apache-2.0, semantica 10.6k★/MIT.
**Стек-матч:** computer ≈ наш computer-use скилл (дубль/конкурент); OpenViking ≈
smart-rag+ADR-004 (дубль + AGPL-риск); semantica ≈ графовый слой ADR-003
(кандидат на интеграцию); omarchy/needle — вне фокуса агент-стека.
**Вердикт совета:** брать частично — проверить **computer** (A/B с нашим
computer-use) и **semantica** (прототип в графовый слой); **OpenViking — НЕТ**
(AGPL-3.0 + дубль smart-rag, нужен юр-чек Фемиды); omarchy/needle — out of scope.
**Риски:** OpenViking AGPL при сетевом use обязывает открыть исходники; computer
(Cloudflare) может нести vendor-lock на Anthropic/Workers (против ADR-002).
**СТАТУС:** зафиксировано, НЕ реализовано (ждёт «закругляемся»). При реализации —
сначала юр-чек лицензии OpenViking через Фемиду.

## 🔭 AI-SCOUT: экспертная группа на каждой новости + Habr + мерж с Angela

**Цель (22.08.2026):** к каждой новости в ai-scout прикрутить разбор нашей
экспертной группы (6 профилей). Плюс объединить дублирующий сбор новостей из
Angela_bot (ai-eggs) и добавить источник Habr.

**Реализовано (24.08):**
- **Ф0 — БД:** миграция `20260823_add_expert_analysis.sql` — колонки
  `expert_analysis JSONB` + `expert_analyzed`, расширен `source` CHECK на
  `'Habr'`, новая таблица `habr_feeds` (seed: ИИ/ML/нейросети/Python хабы).
- **Ф1 — Экспертная группа в бэкенде:**
  - `supabase/functions/shared/experts.ts` — SSoT 6 профилей
    (sherlock/femida/defender/marketer/financier/aibolit) + `buildExpertPrompt`.
  - `analyze-post` расширен: после основного анализа делает 2-й LLM-вызов
    (Gemini→OpenRouter каскад) за мульти-ролевым разбором, пишет в
    `expert_analysis`. Smoke-тест пройден (6 ролей, промпт, safety aibolit).
- **Ф2 — Habr-источник:** `supabase/functions/fetch-habr/index.ts` (парсит RSS
  ленты, пишет `posts` со `source='Habr'`, триггерит analyze-post).
- **Ф3 — Фронт:** тип `Post` расширен (`expert_analysis`, `source='Habr'`);
  `PostDetailModal` рендерит карточку «🧭 Разбор экспертной группы» (6 секций
  с бейджами актуально/не по теме). `npm run type-check` — ОК (без ошибок по
  новому коду; pre-existing unused-imports в App.tsx не трогал).
- **ADR-002** — решение зафиксировано (`docs/adr/002-expert-group-analysis.md`).

**Точка деплоя:** `supabase functions deploy` (analyze-post, fetch-habr) +
применить миграцию + деплой фронта (Vercel).

**Ф4 — мерж Angela (ОТДЕЛЬНАЯ ФАЗА, не трогал):** Angela (ai-eggs) сейчас
дублирует сбор Хабра (`agent/habr_digest.py`) и шлёт свой дайджест
(`daily_report.py`/`morning_post.py`). План: ai-scout становится единым SSoT
сбора+анализа (уже с Habr), Angela переключается на **чтение готовых `posts`**
из Supabase ai-scout вместо собственного сбора. Нужен доступ к ai-eggs —
делаем отдельной задачей.

**Риски:** расход токенов ×2 на пост (основной + экспертный анализ) — терпимо
через Gemini/OpenRouter free-квоты. Habr RSS без ключа (User-Agent достаточно).

**⛔ СТАТУС ДЕПЛОЯ (вечер 23.08): ЗАБЛОКИРОВАН.** VPS `217.149.23.113`
полностью недоступен извне (ping 100% loss, порты 22/80/443 closed) с
любого канала (домашний + мобильный). Сервер внутри сети Timeweb жив
(дашборд «В сети»), блок на уровне провайдера (антибрутфорс). Firewall
whitelist-igор в панели не помог. Утром — тикет в support Timeweb.
Готовый `scripts/deploy.sh` собран, запуск после разбана.

## 🔴 P0 25.08 — ИТОГ сессии 25.08 (вечер)

**СДЕЛАНО:**
- ✅ Синхронизация навыков: `batrak/SOUL.md` (спец по откликам) + `chief/SOUL.md`
  (знает 6 спецов: sherlock/femida/defender/marketer/financier/health) доставлены
  на VPS scp (git push в GitHub с Мака не проходит — канал до GitHub недоступен).
- ✅ Профиль `chief` создан (`hermes profile create --clone-from personal`),
  добавлен в `multiplex_profile_allowlist` gateway на VPS.
- ✅ **Free-first каскад P0 ЗАКРЫТ:** `config.yaml` VPS = `provider: omniroute,
  default: hy3-free` + `fallback_providers` (opencode-zen: hy3-free / claude-sonnet-4-5
  / claude-opus-4-8). Живой тест `hermes -p batrak -z "Кто ты?"` вернул ПЕРСОНУ
  Батрака (SOUL подхватился, каскад живой). Твой TG-тест подтвердил:
  `Switched to fallback model: hy3-free via opencode-zen`.
- ✅ Ключи из Keychain прописаны на VPS: `OPENCODE_ZEN_API_KEY` + `OMNI_API_KEY`
  (drop-in `20-hermes03132.conf` + `/root/.hermes/.env`). `OPENROUTER_API_KEY`
  (len 73) из `/opt/levitan/projects/levitan/.env` записан в
  `/root/.omniroute/provider-credentials.json`.

**ДОБИТО (сессия 26.08, Hermes Agent):**
- ✅ **Туннель Мак↔VPS — вечный, прямой, БЕЗ us-прокси.** Поставлен `autossh`
  + LaunchAgent `~/Library/LaunchAgents/com.igorvasin.vps-tunnel.plist`
  (run `ssh root@217.149.23.113 -p 22`, `-o ProxyCommand=none`, `ServerAlive*`,
  `ControlMaster=auto`, forward `127.0.0.1:20128→VPS:20128` под OmniRoute,
  `KeepAlive`/`AUTOSSH_GATETIME=0`). Самопереподключается при дропах VPS.
  `com.antigravity.vps-tunnel` (его `ssh_tunnel.py 22001`) НЕ трогал — его
  инфра. Примечание: порт 2222 с Мака сейчас недоступен (антибрут VPS/IPtables
  policy DROP) — ходим на 22.
- ✅ **chief на VPS:** создан `profiles/chief/config.yaml` (копия рабочего
  batrak — `provider: opencode-zen, default: hy3-free`) + `chief` добавлен в
  `multiplex_profile_allowlist` (гейтвея). Ранее был только SOUL → generic.
- ✅ **sherlock 401 — ROOT CAUSE найден и устранён.** Двойная причина:
  (а) встроенный провайдер `opencode-zen` в Hermes v0.20.5 шлёт на endpoint
  `https://opencode.ai/zen/go/v1` — тот отдаёт 401 с ЛЮБЫМ ключом (проверено
  curl'ом обоими ключами). Рабочий endpoint — `https://opencode.ai/zen/v1`
  (curl вернул "Pong!" на hy3-free с OPENCODE_ZEN_API_KEY).
  (б) В профилях стоял `key_env: OMNI_API_KEY` (Nous key, len 40) вместо
  `OPENCODE_ZEN_API_KEY` (len 67) — opencode.ai его отвергает.
  **РЕШЕНИЕ:** все 9 профилей переведены на провайдер `custom`
  (`base_url: https://opencode.ai/zen/v1`, api_key = реальное значение
  ключа из env gateway; `${VAR}` в yaml НЕ раскрывается — только литерал).
  Плюс из root config.yaml удалён вредный блок `profiles:` (навешивал всем
  спец-профилям мёртвый `omniroute/auto/free-coding`).
- ✅ **TG-gateway switch:** механизм = `hermes-profile-menu` плагин + `profile_routes`
  (дока `docs/profile-routing.md`). Switch не применял SOUL из-за двух причин:
  (1) `chief` не был в allowlist → `/chief` отклонялся на default; (2) блок
  `profiles:` ломал модель sherlock/др. на мёртвый OmniRoute. Обе устранены;
  gateway перезапущен (`systemctl restart hermes-gateway` через setsid вне
  дерева гейтвея). Живой тест CLI: **sherlock, chief, batrak, femida отвечают
  ПЕРСОНАМИ** («Я — Шерлок, поисковый разведчик…», «Я — Chief, маршрутизатор…»,
  «Я — Батрак, эксперт по откликам…», «Я — Femida, юридический агент…»).

**ОСТАЛОСЬ (после добивки, проверка):**
- [x] **TG-персона живьём ПОДТВЕРЖДЕНА (25.08 вечер):** Фемида в
  `@hermes03132_bot` ответила персоной юриста. Механизм: статический роут в
  root `config.yaml` → `profile_routes: [telegram chat 176203333 → femida]`
  + `systemctl restart hermes-gateway`. P0 ЗАКРЫТ.
- [ ] **Починить меню-свитчер** (`/batrak`/`/chief`/... в TG): плагин
  `hermes-profile-menu` молча не срабатывает — его `_session_origin()` не
  видит `HERMES_SESSION_PLATFORM/CHAT_ID` в окружении команды (та же причина
  старого «переключает профиль только внутри Telegram-чата»). Пока
  переключение = правка `profile_routes` в конфиге (2 мин, через Chief).
  Механизм известен до строчки — починка ~20 мин.

**АРХИТЕКТУРА (зафиксировано от Игоря):**
- VPS в РФ — НЕ принимает трафик из US. Us-прокси нужен ТОЛЬКО для зарубежных
  ресурсов (OpenRouter/Nous) ВНУТРИ VPS, не в туннеле до VPS.
- OmniRoute на VPS = локальный хаб провайдеров (копия твоей версии на Маке,
  `/Users/igorvasin/.omniroute/`). Агенты → `omniroute/auto/free-coding` (primary).
  OmniRoute СЕЙЧАС МЁРТВ (веб-UI не поднялся, API пусто, init зависает).
  Отдельная задача: `OMNIROUTE_VPS_TASK.md`.

**СЛЕДУЮЩАЯ СЕССИЯ:** P0 закрыт 25.08 (туннель + профили персоны + TG-роут).
Осталось: починить меню-свитчер в TG-боте (~20 мин, см. ОСТАЛОСЬ). OmniRoute —
отдельной сессией по `OMNIROUTE_VPS_TASK.md`.

## 🟢 КВОРК-ПРОЕКТ: Упаковка сообщества ВК (socialmos) — ЭТАП 1

**Дата старта обсуждения:** 16–22.08.2026. **Встреча с заказчиком:** 22.08 19:00 МСК.
**Личность исполнителя на бирже:** erik0313233 (Игорь = Hermes как саб-агент-водитель).
**Проект на бирже:** kwork.ru/projects/3027955 — «Упаковка, контентная стратегия и продакт-менеджмент в ВК» (бюджет до 27 000₽).
**Отклик принят на 500₽** = Этап 1 «Концепция и прототипирование» (минимальная ставка платформы).

### СУТЬ ПРОЕКТА (два этапа)
- **Этап 1 (500₽, СЕЙЧАС):** отбор кандидата. Концептуальные артефакты в СВОЁМ демо-сообществе «Примерка» (результат заказчику НЕ передаётся, НЕ заливается в его аккаунт). Оценка — логика и мышление, не оформление.
- **Этап 2 (отдельная оплата, ПОСЛЕ):** реальная упаковка и автоматизация в аккаунте заказчика — 4 инфопродукта, бот VK+TG, рассылки, мини-приложение ВК.

### ТЗ ЭТАПА 1 (ровно 4 артефакта, КОНЦЕПТ, не код/не рабочая заливка)
1. **PDF-аудит** текущего сообщества (сильные/слабые стороны, отвлекающие элементы, что скрыть/упростить) + 3–5 рекомендаций + скриншоты.
2. **Визуальный концепт первого шага** («ВК-витрина»): шапка + сегментация + меню + карточки продукта «Настройка меню в ВК» = **2 обложки + пример кнопок меню** (статичный мокап, единый стиль).
3. **Мини-цепочка рассылки** (1–2 письма) для лид-магнита «5 ошибок, мешающих росту сообщества».
4. **Сценарий одной ветки бота:** приветствие + сегментация (блогер/бизнес/компании) + тексты/кнопки + логика перехода к первому продукту.

**P.S. обязательные гейты (ПРОВЕРЕНО технически 22.08, ВСЕ ЗАКРЫТЫ):**
- «Своё сообщество Примерка» — есть админ-доступ к 3 сообществам: «Своё Подворье» (club238230663), «#ВеземЦып» (club238316002), «Топ товаров 2025» (best_2025). Разворачиваем любое как демо-витрину (`groups.create` через API не прошёл — scope, создадим вручную в аккаунте).
- Граф. редакторы — Pillow + генерация обложек (FAL активна по подписке Nous).
- Конструкторы автоматизации — `TG_BOT_TOKEN`, `VK_USER_TOKEN`, `VK_SERVICE_TOKEN` в `ai-eggs/.env` (живые, проверены).

### ЧТО УЖЕ СОГЛАСОВАНО С ЗАКАЗЧИКОМ (итоги переписки)
- Тема проекта: **«Практическое внедрение цифровых инструментов»** (ВК для бизнеса/блогеров).
- 4 инфопродукта (ступенчатая воронка 1→2→3→4): **Аудит сообщества → Настройка меню → Настройка рассыльщика → Настройка бота**. Каждый = DIY («прошёл → внедрил → получил результат»), без консультаций.
- Формат обучения: **НЕ PDF-учебник**, а **мини-приложение ВК как премиальный визуальный интерфейс**; основной контент — короткие видеоскринкасты + чек-лист/конспект как вспомогательное; аудио — опционально, не обязательно. Интерактивный PDF — отвергнут как основной (плохо на мобильных).
- Сегментация на входе: **блогер / бизнес / компания** (ветвит примеры/шаблоны, базовый каркас общий).
- Бот: **VK + Telegram**. После оплаты — персональная кнопка доступа (не общая ссылка), минимум действий на шаг, сохранение прогресса чек-листа.
- Воронка линейная (1→2→3→4), но «перешагивать» уровни нельзя свободно (теряется user story); для опытных — отдельная кнопка «уже настроено» с контрольными вопросами.
- Обучение внутри ВК (не выходя за пределы).

### РЕАЛЬНЫЙ АУДИТ edmarketingpro (снято через VK API 22.08, факт)
- id 219701436, «Эдуард Интернет-маркетолог», 93 участника, Москва, открытое.
- Магазин ВК включён (advanced), но **товаров 0** (витрина пустая).
- Кастомного меню с кнопками **нет** (только авто-раздел «Услуги»).
- 37 постов про продажи/контент — **без привязки к воронке**.
- Описание — общий консалт-текст + ссылка на Telegram. Одна кнопка «Написать Эдуарду».
- Вывод: сейчас это визитка эксперта, не DIY-система → ровно то, под что делается прототип.

### РИСКИ / УРОКИ (зафиксировать)
- ⚠️ erik0313233 в переписке допустил ошибки: отвечал мимо вопроса, шутил, говорил про OpenCode вместо инструментов, соглашался на всё. Заказчик проверял внимательность к ТЗ.
- ⚠️ НЕ обещать рабочее мини-приложение на этапе 1 (это этап 2). Этап 1 = концепт.
- ✅ Границы: этап 1 — только 4 концепт-артефакта в демо. Всё остальное задания Кворка — только за отдельную оплату этапа 2.

### ПЛАН НА ВСТРЕЧУ 19:00 (от заказчика)
Подвести итог, сформировать план действий, оформить заказ на этап 1 «не для галочки, а как часть реальной системы».
**Действие Hermes:** вести erik0313233 прямо по ТЗ (4 артефакта), не уходить в фантазии про рабочий код; подтвердить контур этапа 2 (объём + оплата) до старта работы.

### СТАТУС
- [ ] Встреча 19:00 проведена, заказ этапа 1 оформлен.
- [ ] Артефакт 1: PDF-аудит edmarketingpro.
- [ ] Артефакт 2: визуальный концепт (2 обложки + кнопки меню).
- [ ] Артефакт 3: цепочка рассылки (1–2 письма).
- [ ] Артефакт 4: сценарий ветки бота.
- [ ] Демо-сообщество «Примерка» развёрнуто.

## 🌙 НОЧНОЙ КОНВЕЙЕР КВОРКОВ (Sherlock→Batrak→Chief, 25.08)

**Цель:** ночью агенты ищут подходящие кворки, пишут отклики в стандарте v3;
утром Игорь копирует готовое и отправляет вручную (анти-бан HH/Kwork).
SSoT: `docs/adr/007-nightly-kwork-pipeline.md`, план `.hermes/plans/nightly-kwork-pipeline.md`.

- [x] **Ф0 аудит:** старый `freelance-agent` НЕ содержит парсера Kwork (`src/index.ts`
  отсутствует, только MCP Avito/Bitrix + design-агент). Строим почти с нуля.
  Kwork с Мака ходит напрямую (200 OK), но SPA — карточки грузятся JS.
- [x] **ADR-007 + план** записаны (роли, фильтр калибровки, якорь 1500₽/ч,
  триаж A/B, «рыбка» ≤30 мин, дедуп, free-only guard, доставка 9:00).
- [x] **Ф1 код:** `cron_jobs/kwork_finder.py` написан (Playwright headless, фильтр
  калибровки, дедуп через sqlite, запись в outbox + leads). `--dry-run` готов.
  Фильтр проверен на 5 тестах: транскрибация→A, бот+Bitrix→A, игра→X,
  Senior Backend→X, парсер 800₽→B. ✅
- [ ] **Ф1 тест:** прогнать `kwork_finder.py --dry-run` на реальном Kwork (ждём
  завершения `playwright install chromium` в фоне, pid 72152).
- [ ] **Ф2:** Batrak v3-промпт (HOOK→MATCH→PROOF) подключить в конвейер.
- [ ] **Ф3:** «рыбка» proof-budget (≤30 мин, локально, без секретов).
- [ ] **Ф4:** cron 0 3 * * * → outbox + leads; TG-дайджест — после починки gateway.
- [ ] **Заметка Firecrawl:** `sk-nous` (Nous Inference) НЕ авторизует api.firecrawl.dev
  (401). Firecrawl по подписке Nous требует отдельного эндпоинта/ключа — не найдено
  в доках; временно используем Playwright headless (локально, без ключей).

## 🔴 ЖЁСТКИЙ УРОК 25.08 (Игорь закрыл сессию — НЕ повторять)

Агент облажался на подборе кворков. Зафиксировано как обязательное правило:

- [ ] **ФИЛЬТР ПО РЕАЛЬНЫМ КОМПЕТЕНЦИЯМ, не по regex-ключам.** Наш профиль СТРОГО:
  боты (aiogram)+Bitrix24/Mango CRM, RAG/эмбеддинги, парсеры, транскрибация
  (faster-whisper — реально есть), лендинги/сайты под ключ.
  **ЖЁСТКО ОТСЕКАТЬ:** видео-синтез/face-swap, игры (Roblox/Unity/движки),
  чистый SEO/GEO-маркетинг, Hardcore Senior/Enterprise. Агент зацепился на
  «ИИ/видео/игра» и предложил не наши задачи — БРАК.
- [ ] **ПЕРЕД ОТКЛИКОМ ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ТЗ заказчика** (описание + вложения,
  часто Word-док на странице кворка). Писать отклик ПО ЗАГОЛОВКУ = брак
  (было на кворке Bitrix24 — отклик вслепую, ТЗ не читал).
- [ ] **Игорь сам бракует лишнее** — спрашивать, что вычеркнуть, не угадывать.
- [ ] **ФОРМАТ ВЫВОДА:** Игорь НЕ может открывать/кликать файлы в Hermes.
  Печатать каждый кворк как «ссылка → готовый отклик» ПРЯМО В ЧАТ, не в outbox/.
- [ ] **VOICE:** писать как исполнитель «Готов разработать…», НИКОГДА не
  парафразить ТЗ «Нужно разработать…» (старый баг вставлял текст заказчика в наше письмо).
- [ ] **АНТИ-БАН Kwork:** Qrator банит IP после частых заходов (ERR_CONNECTION_REFUSED/
  Timeout). Один медленный прогон: задержки 30–60с, ротация UA, пауза 60мин между
  прогонами. Не долбить в цикле.
- [ ] Код `cron_jobs/kwork_finder.py` требует переписать фильтр (сейчас regex-ключи →
  не те предложения). См. ADR-007 + план `.hermes/plans/nightly-kwork-pipeline.md`.

## 🔗 СИНХРОНИЗАЦИЯ Mac ↔ VPS (ТГ-бот) — SSoT общения (решено 29.08)

> ПРАВИЛО: два «главных» агента (десктоп на Маке + ТГ-бот, оба профиль femida)
не видят общих файлов напрямую. Чтобы не было раздвоения — ВСЁ пишется сюда,
в ACTIVE_TASKS.md, по единому шаблону. Кто бы ни исполнял (Мак или бот на VPS),
результат дописывается В ТУ ЖЕ строку.

### Формат задачи (обязателен для ТГ-бота и для Мака)
```
### T-NN · <название> · [⏳|🔧|✅|❌]
- Поручил: <ТГ|Мак> · <дата/время>
- Задача: <что сделать, кратко>
- Результат: <вывод команды / ссылка / краткий итог>   ← заполняет ИСПОЛНИТЕЛЬ
```
- Большая задача → отдельный `tasks/T-NN.md` с полным логом; здесь — ссылка + статус.
- Игорь спрашивает «что по T-NN?» → бот/Мак цитирует строку из этого файла.

### Каналы синхронизации (приоритет)
1. **ДОМА** → VPS через СТАТИЧЕСКИЙ IP VOSNET (подключить ЛК/офис +7(496)441-11-62).
   IP вписать в TimeWeb FWaaS + ufw (порт 22) → туннель `~/.vps-tunnel.sh` живёт.
   Тогда `ACTIVE_TASKS.md` синкается scp/rsync по SSH (GitHub с Мака недоступен).
2. **МОБАЙЛ** → через ТГ-бота (уже работает, входящий IP не нужен). Бот пишет в
   свой `ACTIVE_TASKS.md` на VPS; Игорь спрашивает бота «что по T-NN?» → цитирует.
3. **RU-SOCKS5-прокси ОТМЕНЁН** (поддержка: только белые списки РФ, возврат средств).

### Инструкция ТГ-боту (профиль femida на VPS)
При любом поручении от Игоря: создать строку `T-NN` по формату выше (статус ⏳),
после выполнения дописать `Результат:`, сохранить в `/root/freelance-2026/ACTIVE_TASKS.md`
(копия на VPS). Не держать контекст только в чате — всё в файл.

### ДЕЖУРНЫЙ ЦИКЛ (выходные, динамический IP VOSNET ещё не куплен)
Туннель падает при смене домашнего IP. Оживить БЕЗ туннеля (через ТГ-бота, он жив):
1. `bash ~/freelance-2026/my_ip.sh` → узнать текущий IP.
2. В ТГ главному боту: «открой <IP> в ufw на VPS (порт 22)».
3. LaunchAgent сам ретраит туннель (~10с), порты 9119/8742 оживают.
АВТО-вариант (после статического IP в понедельник): cron на VPS смотрит лог sshd
и сам добавляет новые IP в ufw → туннель оживает без участия Игоря.

### ES-AUTO-COMPANY (31.08.2026) — адаптация Auto-Company (MaxMiksa, 2563★)
**Суть:** репозиторий `MaxMiksa/Auto-Company` — полностью автономная AI-компания 24/7 на 14 агентах (Bezos/Vogels/Munger/Norman/DHH/Bach/Hightower/Godin/Graham/Ross/Campbell/Thompson) + daemons (macOS launchd / WSL systemd) + dashboard + 30+ навушков. **Статус:** кандидат на адаптацию как набор паттернов, промптов и скиллов, а не как скопированный продукт. Snapog отдельно — готовый к ship продукт (Cloudflare Workers).

#### Пилот: 3 файла для адаптации под Шерлока, Маркетолога, Дефендера, Финансиста
| # | Файл из Auto-Company | Адаптируем под | Что берём | Конкретная правка |
|---|---|---|---|---|
| **1** | `.claude/skills/team/SKILL.md` | Шерлок / Маркетолог / Дефендер / Финансист (все) | Шаблон команды: spawn 3–5 субагентов, `docs/<role>/` outputs, consensus baton, `team/SKILL.md` | **✅ ДОСТАВЛЕН** `skills/expert-council-adapter/SKILL.md` + `~/.hermes/skills/expert-council-adapter/SKILL.md` + `skill-router.md`. Шаблон spawn с нашими профилями (`sherlock/femida/defender/marketer/financier/health`) вместо `.claude/agents/*`. Китайский текст убран, формат `SKILL.md` по канону Hermes. |
|| **2** | `.claude/skills/deep-research/SKILL.md` + `.claude/skills/competitive-intelligence-analyst/SKILL.md` | Шерлок | 8-фазный исследовательский пайплайн (Scope→Plan→Retrieve→Triangulate→Synthesize→Critique→Refine→Package) + citation tracking + feature-comparison matrix | **✅ ДОСТАВЛЕН** `skills/shrelock-research/SKILL.md` + `references/methodology.md` + `scripts/validate_report.py/citation_manager.py/source_evaluator.py` + `~/.hermes/skills/shrelock-research/SKILL.md` + `~/.hermes/profiles/chief/skill-bundles/shrelock-research.yaml`. Методология 8-фазная + competitive-intelligence framework. Китайский убран, источники заменены на наши (HH, Kwork, Supabase, GitHub, arXiv). `validate_report.py` адаптирован под `brief.md`. |
|| **3** | `.claude/skills/financial-unit-economics/SKILL.md` + `.claude/agents/cfo-campbell.md` | Финансист + Маркетолог | Unit-economics framework (LTV/CAC ≥3:1, payback <12мес, contribution margin) + pricing-strategy playbook + `resources/template.md` | **✅ ДОСТАВЛЕН** `skills/financier-unit-economics/SKILL.md` + `references/cfo-campbell.md` + `references/financial-unit-economics-original.md` + `~/.hermes/skills/financier-unit-economics/SKILL.md` + `~/.hermes/profiles/chief/skill-bundles/financier-unit-economics.yaml`. Китайский убран, формулы + benchmarks + decision framework + шаблоны под Snapog/Kwork. |

#### Зачем именно эти 3
- **#1 (team/SKILL.md)** — универсальный мультиагент: покрывает Шерлока (поиск вакансий), Маркетолога (контент-план), Дефендера (security audit), Финансиста (unit-economics review) одним паттерном spawn. Главный ROI — не дублировать логику «набрать команду» в каждом профиле.
- **#2 (deep-research + competitive-intelligence)** — мозг Шерлока: для кворков нужны исследования рынка, competitor-матрицы, pricing intel. Auto-Company даёт готовую 8-фазную методологию с валидацией через `scripts/validate_report.py`.
- **#3 (financial-unit-economics)** — мозг Финансиста + Маркетолога: pricing-стратегия + unit-economics — то, что нужно для Kwork-воронки (этап 2 socialmos) и для любых прод-продуктов. `cfo-campbell.md` — готовый промпт, который можно подрезать под наши задачи.

#### Что НЕ берём в пилоте (оставим для отдельного решения)
- `scripts/core/auto-loop.sh` — мощный daemon loop, но привязан к Claude Code/Codex CLI + `bypassPermissions`. Наш stack — OmniRoute + gateway. Адаптировать после стабилизации базы.
- `dashboard/server.py` — Python stdlib dashboard. Нам нужен дашборд expert-council, но свой, под Supabase + ВК. Взять шаблон TDD из `tests/test_dashboard_server.py`, не сам сервер.
- `projects/snapog` — Cloudflare-воркер. Не мигрируем на TimeWeb; использовать как демонстрацию OG-изображений для клиентов Kwork.
- `.claude/agents/ceo-bezos.md` / `critic-munger.md` — сильные промпты, но для нашей архитектуры (6 fixed-профилей) избыточны. Возьмём паттерн из них, не файлы.

**Что я НЕ буду делать без явного «да»:**
- Клонировать/форкать репо локально и запускать `make start` / `make install` — требует quota Claude/Codex, запускает daemon; репо сам по себе не знает о нашем «авто=бан».
- Редактировать `.claude/agents/*.md` или `CLAUDE.md` в оригинальном репо (запись в чужой репо без ветки/PR — стирает историю).
- Устанавливать `@anthropic-ai/claude-code`, `@openai/codex`, `wrangler` глобально — это подписки/квоты, должны быть явно утверждены.
- Привязывать `projects/snapog` к нашему VPS без переписывания на наш стек (Cloudflare Workers ≠ TimeWeb).
- Адаптировать `bypassPermissions` и autonomous-loop философию под нашу инфраструктуру без установки наших guardrails (нет auto-approve на destructive operations).
- Писать новые скиллы по паттерну репо без проверки на вес — `.claude/skills/deep-research/SKILL.md` = 33 KB, один скилл съест много контекста агента.

**Статус:** кандидат на адаптацию. Файл #1 (`team/SKILL.md`) ✅ ДОСТАВЛЕН. Файлы #2 (`deep-research`) и #3 (`financial-unit-economics`) — ожидают «делай».

#### Связь с существующими задачами
- T-03 (регистратор — маршрутизация спецам) — `expert-council-adapter.md` может стать «мозгом» регистратора, маршрутизирующим задачу в команду из 3–5 профилей.
- ES-N (экспертные боты в ТГ) — `team/SKILL.md` паттерн напрямую ложится на многоагентную дискуссию ботов.
- AI-Scout expert analysis — 8-фазный research pipeline из файла #2 можно адаптировать как fallback для `expert_analysis` поля.

**ОБЯЗАТЕЛЬНО:** перед пилотом — добавить LICENSE в копию репо или зафиксировать решение «MIT без файла — договорённость с мейнтейнером».

> ⚠️ ВАЖНО (зафиксировано в memory): VPS-конфиги = `/srv/hermes/.hermes`, НЕ `/root/.hermes`. Перед правкой профилей на VPS всегда править `/srv/hermes/.hermes/{config.yaml,profiles/*/config.yaml}`. Туннель: 2222→VPS:22 (SSH), 2223→VPS:20128 (VPS-OmniRoute).


