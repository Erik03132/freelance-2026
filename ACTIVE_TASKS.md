# Текущий статус проекта (на момент паузы 26.03.2026)

## 📌 Последние достижения (Роботяга)
1. **GitHub MCP Server успешно подключен** (статус: Enabled). Теперь агенты могут читать чужие репозитории и делать Pull Requests.
2. **Установлен OrbStack (Docker)** — контейнеры локально работают, MCP могут запускаться.
3. В `gemini.ts` внедрена поддержка **DeepSeek V3** (через OpenRouter) и расчет сроков выполнения задач в днях (`effortDays`).
4. **✅ ВНЕДРЕН Perplexity API:** В `dashboard/src/lib/gemini.ts` добавлена функция `perplexitySearch()` (модель `sonar-reasoning`) для глубокого реалтайм-анализа данных из интернета агентом Шерлоком.

## 🤖 Наша ИИ-Команда (Роли)
- **Босс (Антигравити / Я)** — Архитектор системы, стратег, пишу ядро интеграций.
- **Роботяга (Я, ваш текущий ассистент)** — Выполняет тяжелые рутинные задачи, парсинг, интеграцию API и реализует код.
- **Шерлок (Scout/Analyst)** — Ищет и оценивает заказы (теперь имеет доступ к Perplexity).
- **Рембрандт (Designer)** — Генерирует дизайн-токены, UI-компоненты.
- **Кулибин (Engineer)** — Пишет код, работает с БД и деплоит на Vercel.

## 🚀 На чем мы ЗАВИСЛИ / Ближайшие задачи (После перерыва)
1. **✅ Подключен Шерлок к Perplexity:** Логика в `scout.ts` и `specialists/base.ts` обновлена. Теперь `Deep Deduction` результаты передаются специалистам и включаются в оффер.
2. **🟡 Активация MCP-сервисов в спецах:** В `DesignAgent` и `WebDevAgent` добавлена поддержка `mcpActions` для Stitch и Neon.
3. **Парсинг откликов (`proposals_count`):** Парсеры Kwork и FL.ru всё ещё не вытаскивают количество конкурентов. Нужно обновить селекторы, чтобы Шерлок точнее считал скоринг задачи.
4. **Дожим Авто-отправки (Auto-bid):** Скрипт готов, но требует боевых кредов в `.env` и тестов через Playwright.
5. **Резервный поиск (Tavily/Brave):** Интеграция вторым эшелоном для RAG-задач, если Perplexity ляжет.
6. **🚀 NAS Offloading (Infrastructure):** Настроить автоматический перенос тяжелых бэкапов и папки `antigravity-brain` на внешний NAS для освобождения места на SSD макбука.

## 💾 Как продолжить работу
Шеф, как вернешься из перезагрузки (вместе с Антигравити), просто напиши Роботяге: *"Продолжаем! Давай прикрутим Perplexity к Шерлоку"* или *"Погнали настраивать парсинг конкурентов"*. 

Отдыхай, система сохранена! 🛠️

---

## 🤖 ТЕМА: Эволюция агентов Antigravity

> Зафиксировано: 2026-04-30. Приоритет: Средний. Детали → `AGENT_EVOLUTION_ROADMAP.md`

### Этап 1 — Унификация коммуникации (быстрый старт)
- [ ] **1.1** Создать `utils/agentClient.js` — единая функция `callAgent(agentId, action, payload)` для обращения к любому агенту
- [ ] **1.2** Ввести файл-очередь `shared/event_log.jsonl` как простой брокер событий между агентами
- [ ] **1.3** Определить схему сообщений `config/message_schema.yaml` (`agentId`, `action`, `payload`, `ts`, `id`)
- [ ] **1.4** Создать `config/agents.yaml` — реестр всех агентов с их эндпоинтами и правами доступа

### Этап 2 — Автоматизация рутины
- [ ] **2.1** Добавить cron-задачу для `finish-day` (`0 23 * * * ~/freelance-2026/tools/finish_day.sh`)
- [ ] **2.2** Настроить автоматический бэкап `antigravity-brain` на NAS через `systemd`-таймер
- [ ] **2.3** Автоматизировать линт и проверку кода агентов через GitHub Actions (`SafeToAutoRun: true`)
- [ ] **2.4** Перенести существующие скрипты (`finish_day.sh`, `deploy.sh`) в задачи очереди

### Этап 3 — CLI-интерфейс агентов
- [ ] **3.1** Создать CLI-утилиту `agctl` (`tools/agctl.sh`) — быстрая отправка команд агентам из терминала
- [ ] **3.2** Пример: `agctl chat --agent igorek-core --msg "Обнови расписание"`
- [ ] **3.3** Подключить `agctl` к `agentClient.js`

### Этап 4 — Мониторинг и алертинг
- [ ] **4.1** Запустить `logAgent.js` — централизованный JSON-лог запросов и ответов в `./logs/agent_*.log`
- [ ] **4.2** Подключить Prometheus-метрики (количество запросов, latency агентов)
- [ ] **4.3** Настроить webhook-алерты при сбоях агентов (отправка в Telegram или Slack)

### Этап 5 — Модульная архитектура навыков
- [ ] **5.1** Создать `skillLoader.js` — автоматическое сканирование `./skills/` и регистрация навыков
- [ ] **5.2** Каждый навык должен экспортировать `metadata.json` (имя, описание, версия, права)
- [ ] **5.3** Открыть RPC-интерфейс для каждого навыка (вызов через `agentClient`)

### Этап 6 — Внешние интеграции
- [ ] **6.1** Telegram → агенты: webhook принимает сообщения, отправляет задачи в очередь
- [ ] **6.2** Google Calendar → планировщик задач: API ставит задачи агентам по расписанию
- [ ] **6.3** REST-webhooks для вызова агентов из внешних систем (CI/CD, Slack, Bitrix24)
