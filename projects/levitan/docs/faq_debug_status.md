# Levitan — статус после отладки FAQ (15.08.2026)

## Сделано (Fixed)
1. **`_word_match` ужесточён** (funnel.py): подстрока ИЛИ корень ≥5 ИЛИ SequenceMatcher ≥0.8.
   Убраны ложные созвучия: «беспокоит~бесплатная», «цыпленок~цыплята», «поставщика~доставка».
   Сохранена морфология: «гарантия~гарантии», «стоит~стоят», «бройлеры~броилер».
2. **`_FAQ_STOP` расширен**: даите/дайте, контакты, вашего, поставщика(ов), корм(а/ов),
   примут/примите, возврат, вернуть, сдохнет(ут), падеж — такие слова больше не участвуют
   в keyword-матчинге FAQ.
3. **Сервисный гейт в `_faq_reply`**: норма «возврат|примут|сдохн|дохн|вернут|падеж|замен»
   пропускает keyword-слой FAQ → вопрос уходит в LLM (юридические/сервисные запросы вне FAQ).

## Верификация (Verified)
- `tests/test_funnel.py`: **23 passed** (до этого было 3 failed).
- `tests/` в `.venv`: **27 passed** (включая test_call_session.py с faiss).
- Emulate: faq_real_questions — 7/8 fast (resp_lat ~0ms), ложные «дайте контакты поставщика кормов»
  и «примут ли возврат если цыплёнок сдохнет» → LLM.
- Ruff: agent/funnel.py — All checks passed.

## Остаточная неточность (не блокер)
- «какие породы есть» → «Базовая цена 90 руб.» — перехват price-интента (паттерн «породы»
  в funnel_config.json). Закреплено тестом `test_porody_known_intent` (ждёт упоминание «90 рублей»).
  Чтобы отдавать точный FAQ (породы Кобб/Росс), надо убрать «породы»/«ассортимент» из price-паттернов
  — но это сломает существующий тест. Требует решения.

## Ulterior
- Эмулятор: `tests/emulate_call.py --scenario faq_real_questions --dry` (работает без звонков).
- Следующий шаг: контрольный звонок на Mango для проверки реальной латенции.