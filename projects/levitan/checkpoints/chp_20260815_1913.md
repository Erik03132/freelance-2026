# Levitan — Session Log

## 2026-08-15 (вечер) — OmniRoute-кроличья нора + откат; delivery-фикс задеплоен; NEG-фикс локально, НЕ задеплоен

### Честный итог дня (HT-3): день ушёл почти целиком в OmniRoute, который агента НЕ касается
- **Не-скриптовые реплики агента НЕ идут через OmniRoute** — агент ходит на **Gemini** (levitan_agent.py: LLM_BASE/LLM_MODEL). OmniRoute на VPS (combo free-cascade, провайдер opencode-zen и т.п.) агент НЕ использует. Поэтому Boot день ковыряния OmniRoute = «коту под хвост» по влиянию на звонки.
- **Откат к началу сессии:** ufw ВЫКЛЮЧЕН (включал его я — он мог резать входящие порты) → звонки снова проходят. OmniRoute-БД VPS: тег провайдера остался `opencode-zen` (sqlite-команда отката была порвана вставкой, НЕ применилась) — НЕ влияет на агента, тронуть при доступе.
- Мой IP сменился `95.154.153.47` → **`193.56.253.30`** (динамический). SSH к VPS закрыт Security Group провайдера (даже при открытом ufw: `Connection refused` до хоста) — открыть порт 22 для нового IP (или временно `0.0.0.0/0`) в панели провайдера.
- Дашборда OmniRoute на VPS НЕТ (headless, `/` и `/omniroute/` → 404). Порт 20128 наружу не доступен (провайдер режет).

### Реальный звонок (VERIFIED): работает, но нашёлся баг
- Звонок дошёл, диалог состоялся, приветствие ок. **Баг:** на «нет, не интересно» (отказ про цыплят) агент СНОВА поздоровался и СНОВА предложил — составной отказ не ловится NEG-детектом → уходит в LLM, LLM повторяет воронку.

### Фиксы (локально: agent/funnel.py, agent/levitan_agent.py, tests/test_funnel.py — 24 passed)
1. **Delivery-change canned reply** (задеплоено на VPS): после «Место доставки прежнее?» ЛЮБАЯ не-подтверждающая фраза → `DELIVERY_CHANGED_REPLY` = «Сообщите менеджеру новое место доставки, он с вами свяжется в ближайшее время, всего хорошего!» (было `return None` → LLM → тишина). Промпт step 5 обновлён той же фразой.
2. **NEG_COMPOUND_RE** (НЕ задеплоено): «нет не интересно» / «не надо» / «не нужно» / «не хочу» / «отказ» → полноценный отказ → «Спасибо за внимание, всего хорошего!» + авто-завершение (маркер «всего хорошего» в _FAREWELL_MARKERS).

### Деплой (VPS)
- ✅ Пункт 1 задеплоен: байтовый патч (base64, 16 printf-строк в веб-консоль) → `funnel.py patched`, `levitan_agent.py prompt patched`, `systemctl restart levitan-agent` → **active**. Голосом НЕ проверен (в контрольном звонке клиент отказал ДО вопроса доставки).
- ⚠️ Пункт 2 (NEG-фикс) — **только локально, на VPS НЕ задеплоен**. Завтра первым делом.

### Открыто / на завтра
1. **Задеплоить NEG-фикс на VPS** (патч `funnel.py` тем же механизмом → restart levitan-agent), контрольный звонок: «нет не интересно» → «Спасибо за внимание…» без повтора оффера.
2. Проверить голосом delivery-canned reply (вопрос «Место доставки прежнее?» → «нет» → canned-ответ). НЕ Verified.
3. OmniRoute больше НЕ трогать (агент на Gemini). Если откроется SSH — вернуть тег `opencode-go` (Unverified, не влияет).
4. Мой IP динамический → для SSH из агента открывать 193.56.253.30 в Security Group провайдера.

### Полезное: тест-звонок из VPS-консоли (Mango callback)
```bash
cd /opt/pipecat-agent && python3 - <<'PY'
import json,hashlib,uuid,urllib.request,urllib.parse
E={}
for l in open(".env"):
    l=l.strip()
    if "=" in l and not l.startswith("#"): k,v=l.split("=",1); E[k]=v
K=E.get("MANGO_VPBX_API_KEY",""); S=E.get("MANGO_VPBX_API_SALT","")
EXT=E.get("MANGO_FROM_EXTENSION","22"); PH=E.get("TEST_PHONE","79859234644")
p={"command_id":"lv_"+uuid.uuid4().hex[:8],"from":{"extension":EXT},"to_number":PH}
j=json.dumps(p,separators=(",",":"),ensure_ascii=False)
s=hashlib.sha256((K+j+S).encode()).hexdigest()
d=urllib.parse.urlencode({"vpbx_api_key":K,"json":j,"sign":s}).encode()
r=urllib.request.urlopen("https://app.mango-office.ru/vpbx/commands/callback",data=d,timeout=25)
print(r.status, r.read().decode()[:200])
PY
```

### Файлы (локально изменены, НЕ закоммичено)
- `agent/funnel.py` (DELIVERY_CHANGED_REPLY + NEG_COMPOUND_RE), `agent/levitan_agent.py` (промпт step 5), `tests/test_funnel.py` (24 passed).

## 2026-08-15 — FAQ-слой: убраны ложные срабатывания keyword-матчинга (Fixed + Verified)

### Что сделано (agent/funnel.py)
1. **`_word_match` ужесточён**: матч ТОЛЬКО если подстрока (морфология) ИЛИ общий корень >=5 ИЛИ SequenceMatcher >=0.8. Убраны ложные созвучия «беспокоит~бесплатная», «цыпленок~цыплята», «поставщика~доставка», «другая~другие». Сохранена морфология: «гарантия~гарантии», «стоит~стоят», «бройлеры~броилер».
2. **`_FAQ_STOP` расширен** сервисными словами: даите/дайте, контакты, вашего, поставщика(ов), кормов, примут/примите, возврат, вернуть, сдохнет(ут), падеж — не участвуют в keyword-матчинге FAQ.
3. **Сервисный гейт в `_faq_reply`**: regex `(возврат|примут|сдохн|дохн|вернут|падеж|замен)` → пропустить keyword-слой FAQ → вопрос уходит в LLM (юридические/сервисные запросы вне FAQ).

### Верификация (Verified)
- tests/test_funnel.py: **23 passed** (до этого 3 failed: дайте-контакты, цыплёнок-сдохнет, greeting-first-turn).
- tests/ в `.venv`: **27 passed** (включая test_call_session.py — faiss только в .venv).
- emulate_call.py faq_real_questions: **7/8 FAST ~0ms**; «дайте контакты поставщика кормов», «примут ли возврат если цыплёнок сдохнет», «соедините меня с директором», «это совсем другая история» → LLM.
- ruff agent/funnel.py: All checks passed.

### Уроки
- prefix-4 ловит случайные созвучия; рабочий баланс = substring + корень>=5 + ratio>0.8.
- bash-вывод python-проб режется RTK-сжатием → писать результат в /tmp/*.txt и читать через Read.
- Валидный вызов `_fast_path_reply` = `(chat_ctx, llm_obj)` с `ctx.items[].role/content`, не строка.

### Остаточная неточность (не блокер)
- «какие породы есть» → generic-цена 90 руб. (паттерн «породы» в price-интентах funnel_config.json, закреплён тестом test_porody_known_intent). Для FAQ-ответа про породы (Кобб/Росс) убрать «породы»/«ассортимент» из price-паттернов — сломает тест, требует решения.

### Следующий шаг
- Контрольный звонок на Mango для проверки реальной латенции (эмулятор логику подтвердил).

---

## 2026-08-14 (16:xx) — Fast-path ДА/НЕТ в слое LLM + 3-я ветка доставки (Fixed + Verified НЕТ)

### Корень решения (архитектура)
- Дубль «Сколько голов?» (было через `on_user_turn_completed`+`session.say`) устранён переносом fast-path ВНУТРЬ `DebugLLMStream`. Теперь ровно ОДИН LLM-стрим на ход → дубль структурно невозможен.
- `skip_reply` живёт на `_EndOfTurnInfo`, НЕ доступен из `on_user_turn_completed` → `session.say()` там НЕ подавляет auto-LLM (источник дубля).

### ЗАФИКСИРОВАННЫЕ настройки (рабочие, voice-stability обновлён)
1. **Fast-path ДА/НЕТ + количество в слое LLM** (`_fast_path_reply` + `DebugLLMStream`):
   - НЕТ (any) → «Спасибо за внимание, всего хорошего!» (мгновенно)
   - ДА до количества → «Отлично! Сколько голов вам нужно?» (мгновенно)
   - КОЛИЧЕСТВО (число/словами) → «Для N голов цена P рублей за голову. Место доставки цыплят прежнее?» — **цена по шкале считается локально** (`_price_for_qty`: ≤100→90, 101-300→85, 301-999→80, ≥1000→75), без LLM.
   - ДА после «Место доставки прежнее?» → «С вами свяжется менеджер для уточнения заказа, всего хорошего!» + `save_lead` в фоне
   - НЕТ на вопросе доставки → отдаётся LLM (клиент меняет адрес)
   - Детектор: позитив `^да`/«ну да»/«да ладно»; негатив `^нет`/`отказ`; количество — `_qty_from_text` (цифры + `_text_to_digits`). Флаги `_asked_quantity`/`_asked_delivery` на `DebugLLM`.
2. **Streaming TTS** через `StreamAdapter(tts=YandexTTS(streaming=False), sentence_tokenizer=tokenize.basic.SentenceTokenizer(retain_format=True))` — первое предложение стартует до конца генерации LLM.
3. **endpointing** `min_delay=0.2 / max_delay=1.2` (было 3.0).
4. **LLM primary** `opencode-go/minimax-m3`, **fallback** `oc/deepseek-v4-flash-free`.
5. `voice-stability` бэкап (`/opt/pipecat-agent/levitan_agent.py.voice-stability`) и служба обновлены; commits `2828bcae` (ДА/НЕТ+доставка), `3420d52b` (количество→цена).

### Результат теста (14.08, голос, VERIFIED)
- Полный продающий диалог прошёл по fast-path БЕЗ единого обращения к LLM:
  «да»→«Сколько голов?» (3.2s) → «78 голов»→«Для 78 голов цена 90 рублей… Место доставки прежнее?» (6.6s, из них STT+endpointing) → «да»→«С вами свяжется менеджер…» (4.8s).
- `[METRICS] summary: turns=3 resp_lat=[3.2, 6.6, 4.8] avg_resp_lat=4.8` — **было 8.4s baseline**. Дублей нет.
- Остаточная задержка ходов = STT (Deepgram) + endpointing `max_delay=1.2` + TTFT (только если попал в LLM). Следующий рычаг: **endpointing 1.2→0.4 (#2)** и **укорочение SYSTEM_PROMPT (#3)**.

### Доп. оптимизации (14.08, VERIFIED, коммиты c337042a / 908196a1)
- **#2 endpointing `max_delay` 1.2→0.4**: без регресса, но выигрыш маржинален — в основной воронке ответы уже мгновенные (fast-path), `resp_lat` упирается в время речи клиента + STT. `avg_resp_lat` стабильно ~4.9.
- **#3 SYSTEM_PROMPT сжат ~2k→~450 токенов** (убран длинный список городов и лишнее описание пород; цены/воронка/стиль сохранены).
- **#4 `max_completion_tokens` 2500→600** (короче генерация на LLM-ходах).
- Edge-case «Нет, другое» (смена города) проверен голосом: LLM (minimax) TTFT ~2s, логика сохранена (спросил новый адрес → телефон). Поведение после trim не сломалось.
- **Итог по пунктам 1-4**: основная воронка ДА→количество→доставка→ДА теперь полностью мгновенная (без LLM), `avg_resp_lat` снизился 8.4→4.9s. Остаток задержки на LLM-ходах = STT + ~2s TTFT minimax. Дальше упираемся в STT и сеть (OmniRoute/прокси) — рычаги #5 (preemptive, риск тишины) / #6 (локальный LLM) / #7 (realtime S2S).

---

## 2026-08-13 (19:13-19:49) — ПОБЕДА: приветствие НЕ обрезается (Fixed + Verified)

### Корень обрезания приветствия
Mango принимает SIP-плечо ПОКА телефон ещё звонит (`inviteToAcceptMs: 931` в логе) и НЕ передаёт аудио агента абоненту до ответа. Агент начинал говорить во время гудков → первые ~1-2с приветствия терялись. Тишина/тон до ответа тоже режутся (silence suppression / early-media не форвардится).

### Рабочие настройки (ЗАФИКСИРОВАНЫ, 19:48)
1. **5-секундная фикс-задержка перед приветствием** — в `on_enter`: после `media bridge settled` `await asyncio.sleep(5.0)` → затем `session.say(GREETING)`. Гарантирует, что абонент уже ответил. Проверено звонком 19:49: `5s pre-greeting delay done, saying greeting` в 19:49:07 (на 5с после моста 19:49:02), приветствие сыграло ЦЕЛИКОМ.
2. **Тон-префикс 0.3s @20% 700Hz** перед приветствием (страховка синхронизации SIP-моста, огибающая 40ms).
3. **`TTS_LEAD_SILENCE_SEC=2.5`** в `/opt/pipecat-agent/.env` (совпадает с дефолтом кода; было 0.3 — режется Mango).
4. **Уникальные комнаты на каждый звонок** — SIP dispatch rule `SDR_yZuSLajiYrJF` (`levitan-inbound`): `no_randomness` ВЫКЛ (default False) → рандомный суффикс `levitan-sip-_<номер>_<rand>`. Исправило коллизию повторных звонков (зомби-участник от 19:13 блокировал звонок 19:22). Персист в redis.

### Что НЕ сработало (история, для справки)
- `TTS_LEAD_SILENCE_SEC` 0.3→2.5 — тишина режется Mango.
- lead-TONE (0.4s@7%, 1.5s@20%) — режется, играет до ответа.
- answer-detection через `track.on("frame_received")` — `RemoteAudioTrack has no attribute 'on'` (LiveKit Agents SDK, не pipecat).

### Статус
- VERIFIED (звонок 19:49: пользователь подтвердил «ОТЛИЧНО, приветствие полное»).
- Файл `levitan_agent.py` (750 строк) забран локально в `agent/levitan_agent.py` и закоммичен.

---

## 2026-08-13 (20:08) — Stage 1: per-call METRICS (Fixed + Verified)

### Что сделано
- Добавлен модульный `_CALL_METRICS` + сбор в `on_enter`/`_on_transcribed`/`_on_item`/`_terminate_after_speech`: `greet_dur`, `turns`, `resp_lat[]` (user_final→assistant_item), `lead_queued`, `call_dur`, `avg_resp_lat`. Итог печатается строкой `[METRICS] {...}` при shutdown.
- Цель: сделать этапы улучшений измеримыми (база для Stage 2/3).

### Тест (звонок 20:08, VERIFIED)
- `[METRICS] {'greet_dur': 11.61, 'turns': 3, 'resp_lat': [5.12, 9.0, 11.14], 'lead_queued': 0, 'call_dur': 53.95, 'avg_resp_lat': 8.42}`
- Приветствие НЕ обрезано. Звонок штатно завершён.
- **Инсайт:** avg response latency ~8.4s (5–11s) — это «хвост» генерации LLM. Главный левер улучшения = стриминг TTS (Stage 3), а не платные модели.

### Статус
- VERIFIED. Закоммичено, `voice-stability` обновлён до этой версии.

---

## 2026-08-13 (20:1x-20:2x) — Stage 2: answer-detection — ОТКАТ (нестабильно)

### Идея
- Заменить слепую 5с задержку на детект ответа абонента через `sip.callStatus`
  (LiveKit SIP participant attr: `ringing` -> активное). Probe подтвердил:
  начальное значение `sip.callStatus: 'ringing'`.
- Корректный API: `room.on("participant_attributes_changed", cb)` с сигнатурой
  `cb(changed_attributes, participant)` (НЕ `participant.on`, которого нет).

### Результат: ХУЖЕ -> ОТКАТ к voice-stability
- Ложный ранний сигнал (`sip.callStatus` уже !=ringing на момент аттача) ->
  приветствие играло ДО ответа -> срез «Здравствуйте» (ещё сильнее).
- При таймауте 8с задержка > 5с -> «задержки больше».
- Пользователь: «уже хуже, приветствие обрезано, задержки больше» -> ОТКАТ.

### Вывод (HT-3: отказ назван)
- Answer-detection через `sip.callStatus` ненадёжен: сигнал «не ringing» приходит
  ДО реального ответа абонента (Mango ставит active раньше, чем пересылает
  аудио). Слепая 5с задержка — пока единственный устойчивый вариант.
- `voice-stability` (5с) возвращён на сервер и активен.

### Побочный баг, исправленный в этом же проходе
- Агент читал номер `79859234644` как «семь миллиардов…» (LLM видит одно число).
- FIX: `_fmt_phone_spoken()` форматирует номер поцифрово
  (`7 9 8 5 9 2 3 4 6 4 4`) для `{caller_phone_spoken}` в промпте; в CRM
  (`save_lead`) остаётся сырые цифры. Низкий риск, задеплоено на voice-stability.

---

## 2026-08-11 (18:30-19:00, вечер) — ФИНАЛ: обрезание хвоста прощания + закрытие звонка shutdown (Fixed, не Verified)

### Старт: контрольный звонок 13:29 (с 13:29 по 18:38 сделано 5 звонков)
- Модели: `LLM_MODEL=oc/deepseek-v4-flash-free`, `LLM_FALLBACK_MODEL=opencode-go/minimax-m3` (юнит), служба active.
- Диалог 13:29 работал, но: финальная фраза «всего хор...» обрезалась, mixer restarts=44, mux_gaps=3.

### Проблемы (диагностика по журналу звонка 13:29)
1. **Обрезание финальной фразы**: `end_call` (tool) вызывал `sess.terminate()` — метода НЕ существует → AttributeError, terminate молча не срабатывал. + livekit режет фразу при tool call.
2. **Хвост LLM**: deepseek-v4-flash-free — быстрый first chunk (1-3с), но медленная генерация ПОЛНОГО текста (5-7с) → фраза до livekit доходила неполной. minimax-m3 — полный текст за 1-2с.
3. **Приветствие обрезано** — НЕ текст (TTS полный 71+74), а **аудио: mixer.restarts=88 за 2мин** (~1 рестарт/1.4с) — джиттер/дрейф SIP (sip_rx_ppm большой). mux_gaps=2 мелкие.

### Сделано (Fixed на VPS, 18:52, служба active PID 232724)
1. **end_call переписан**: ждёт `agent_state_changed` (speaking→listening) с timeout 15с + sleep 0.5 → `sess.shutdown()` (шutdown СИНХРОННЫЙ, не await — TypeError падал). Вынесено в общий helper `_terminate_after_speech(reason)`.
2. **Детект финальной фразы по маркеру** в `_on_item` (маркеры: «всего хорошего», «свяжется менеджер», «до свидания»...) → авто-shutdown. Больше НЕ зависим от tool end_call (livekit рвал фразу на tool).
3. **Промпт**: save_lead вызывать ОТДЕЛЬНЫМ вызовом БЕЗ текста, финальная фраза — отдельным ответом ПОЛНОЙ (чтобы livekit не резал на tool call).
4. **Warmup LLM** в on_enter (обе модели, фоновая задача): deepseek ok in 4.1s, minimax ok in 1.9s — первый TTFT после приветствия упал с 5с до 1-3с.
5. `sess.terminate()` → `sess.shutdown()` (последний баг: звонок 18:41 не завершался программно, только клиент клал трубку).

### Результаты контрольных звонков (прогресс)
- 18:36-18:37: диалог чистый, TTFT 1-3с, НО финал: «...всего хоро» обрезано (deepseek хвост) + дубль фразы из-за tool-call. terminate не сработал (AttributeError).
- 18:38-18:39: warmup заработал (минимакс выигрывал гонку, ответы мгновенные).
- 18:39-18:41 (полный сценарий 5: новый телефон/город/дата → save_lead → прощание): все ответы TTFT 1-3с, финальная фраза озвучена ПОЛНОЙ («...всего хорошего!») через переспрос после tool. shutdown упал с TypeError (await None) — звонок закрыл клиент.
- Пользователь (18:40): «уже значительно лучше, но сильно обрезано приветствие и в конце всего хора. Близко к нормальному формату».

### НЕ сделано / на следующую сессию (честно, HT-3)
- **shutdown-фикс и промпт (save_lead отдельно) залиты, но НЕ Verified** — контрольный звонок не сделан (пользователь завершил сессию).
- **Приветствие обрезано = mixer.restarts=88** — не чинилось. Гипотеза: джиттер/дрейф частоты SIP (PCMA 8000 vs 48k), jitterBuf=false. Попробовать: включить jitter buffer в SIP-транке livekit-sip, сравнить ppm в RTP stats.
- «всего хора» в конце — остаточный хвост deepseek + tool-call реза. Промпт-фикс (фраза отдельно) может решить, проверить звонком.
- `_first_or_fallback`: asyncio warning "Task exception was never retrieved StopAsyncIteration" для проигравших стримов — добавить `t.cancel()` перед `_st.aclose()`.

### Файлы/бэкапы на VPS
- `/opt/pipecat-agent/levitan_agent.py` (активный, все фиксы)
- `/opt/pipecat-agent/levitan_agent.py.bak_terminate_fix` (до end_call/warmup фиксов)
- Локально: `docs/handoff_20260811_1144_audio_greeting.md` (утренний handoff)

---

## 2026-08-10 (поздний вечер, 19:30-22:50) — 🚨 КОРЕНЬ ПАУЗ НАЙДЕН + ВОЗВРАТ ГОНКИ (bak_winner)

### Главные открытия (диагностика TTFT через livekit SDK на VPS)
- **Все замеры «TTFT 2с» из chp.md были обманчивы**: это время до первого ЛЮБОГО чанка (пустого/role/reasoning). Реальный **первый ТЕКСТ** через `openai.AsyncClient`→OmniRoute: gpt-oss 5.2с, mimo 7.5с, ling-tiny 4.5с, nemotron 7.5с, deepseek-chat-v3 6.2с, ling-flash 15.9с. LiveKit-путь идентичен SDK.
- **gemini через OmniRoute → комбо `free-cascade`** (паттерны `*gemini*`, `gemini-2.5-flash` в `model_combo_mappings` → каскад 5 моделей opencode-zen, deepseek-v4-flash первый, 9-16с). Восстановил маппинги после откат-теста (gemini напрямую даёт 404 rate-limit free).
- **gpt-oss в реальном звонке НИКОГДА не отвечает за 4с** — таймаутит на каждой реплике впустую (в изолированном тесте «2.1с» = первый пустой chunk).
- **Путь 1 (последовательный перебор, LLM_FIRST_TIMEOUT=4.0/LLM_FALLBACK_TIMEOUT=10.0) УХУДШИЛ ситуацию**: пауза = 4с (gpt-oss) + 2-5с (gemini) = **6-9с стабильно**. Гонка (baseline 18:19) давала 2-3с.

### Тест-звонок 19:49-19:50 (79859234644, последовательный режим)
| Реплика клиента | gpt-oss | gemini | Пауза до TTS |
|---|---|---|---|
| «Да, интересно» | timeout 4с | OK +2с | **5с** |
| «Пятьдесят пять» | timeout 4с | OK +2с | **7с** |
| «Все прежнее» | timeout 4с | OK +5с | **9с** |
| финальная | timeout 4с x2 | OK +2с | **6с** |

Приветствие обрезано (пользователь подтвердил), задержки 10с+.

### Сделано (Fixed)
- **Восстановлен `bak_winner`** (состояние с ГОНКОЙ, лучший диалог за 2 недели) → `levitan_agent.py`, `py_compile` OK, `systemctl restart levitan-agent` → active. Служба сейчас на гонке.
- **OmniRoute диагностика**: таблицы `combos`/`model_combo_mappings` в `/root/.omniroute/storage.sqlite`. Отключал маппинги gemini (free-cascade → напрямую), но gemini напрямую 404 (rate-limit) → **ВЕРНУЛ enabled=1** (не ломать другие сервисы).
- OmniRoute нестабилен: pm2 `omniroute` перезапускался 99 раз за 6ч (↺ 99).

### Вывод для следующей сессии
- Каскадный путь (STT→LLM→TTS) имеет потолок ~5-6с паузы даже на лучшей модели (ling-tiny 4.5с + TTS 1-2с). **Для «ответ вылетает мгновенно при звуке» нужен realtime S2S (Путь 2: OpenAI Realtime через уже установленный `RealtimeModel` в LiveKit 1.6.6, <500мс)**.
- Завтра оптимизировать И технику, И скрипт ответов (промпт): требование пользователя — «как только звук произошёл, ответ должен вылетать, опережающе».

### Бенчмарк-скрипты (VPS /opt/pipecat-agent/, временные, можно удалить)
`bench_models.py`, `bench_mimo_livekit.py`, `bench_text_ttft.py`, `bench_chunks.py`, `bench_firsttext.py`, `bench_mct.py`, `bench_sdk.py`, `bench_sdk2.py`, `bench_sweep.py`, `bench_usage.py`, `bench_headers.py`, `bench_debug.py`, `bench_latency.py`.

---

## 2026-08-10 (вечер) — ✅ BASELINE ЗАФИКСИРОВАН (лучший диалог за 2 недели)

### Снапшот (железобетонное состояние, от него пляшем)
- **VPS:** `/root/baseline_levitan_agent_20260810.py` (sha256 `2e334869d88a9d9734934bd4a048df474d13265685450a84f4d17114f6d30e3e`), `/root/baseline_env_20260810`, `/root/baseline_levitan-agent.service_20260810`
- **Локально:** `projects/levitan/checkpoints/baseline_20260810/` (3 файла)

### Ключевые настройки baseline
| Параметр | Значение |
|----------|----------|
| LLM_MODEL | `openrouter/gpt-oss-20b:free` (deepseek УБРАН — деградировал, TTFT 15с) |
| LLM_FALLBACK_MODEL | `openrouter/google/gemini-2.5-flash-lite:free` |
| LLM_BASE | `http://127.0.0.1:20128/v1` (OmniRoute на VPS) |
| max_completion_tokens | 2500 |
| temperature | 0.3 |
| LLM_MAX_HISTORY | 6 (truncate chat_ctx в DebugLLM.chat) |
| first-chunk gate / __anext__ | 10с / 15с |
| reasoning_effort=none | только для "deepseek" в имени (gpt-oss → 400) |
| TTS_LEAD_SILENCE_SEC | 0.3 (было 5.0) |
| on_enter sleep | 1.0 (было 3.5) |
| endpointing | min_delay=0.2 / max_delay=1.5 |
| STT / TTS | deepgram nova-3 ru / Yandex alena |
| Промпт | правило 7: «сначала текст, потом end_call» |

### Итог тест-звонка (18:19-18:20, 79859234644)
Полный продающий диалог: приветствие → «Да, интересно» → «Сколько голов?» → «Пятьдесят пять» → цена → «Номер/место прежние?» → «Да, всё прежнее» → end_call order_confirmed. **Лучший диалог за 2 недели.**

### Осталось (для следующей сессии)
- Паузы на 2-4 реплике 7-10с: TTFT моделей поодиночке 2с, но в гонке параллельных free-запросов rate-limit → замедление. Кандидаты: платная быстрая модель (gpt-5-nano), отказ от гонки, предзагрузка.

### История фиксов дня (levitan_agent.py.bak_* на VPS)
1. `bak_aexit` — NoneType в `__aexit__`
2. `bak_reasoning` — reasoning_effort=none для deepseek
3. `bak_winner` — закрытие победителя гонки вместо проигравших
4. `bak_pauses`/`bak_timeouts`/`bak_endpointing` — паузы: lead-silence, sleep, truncate, таймауты

---

## 2026-08-10 (день) — ИТОГИ СЕССИИ: start-day + LLM-провайдеры + тест-звонок. 🚨 ГЛАВНАЯ ПРОБЛЕМА: баг `DebugLLMStream.__aexit__` (NoneType) — агент падает на LLM-инференсе

### 🚨 ГЛАВНАЯ ПРОБЛЕМА (для следующей сессии, Priority 1)
**`levitan_agent.py:368` — `DebugLLMStream.__aexit__` падает с `AttributeError: 'NoneType' object has no attribute '__aexit__'`** (лог 12:29, комната `levitan-sip-_79859234644`):
```
File "/opt/pipecat-agent/levitan_agent.py", line 368, in __aexit__
    await self._s.__aexit__(*a)
AttributeError: 'NoneType' object has no attribute '__aexit__'
```
- Когда все конкурирующие стримы (`_first_or_fallback`) закрыты/зафейлены, `self._s` = None, и `__aexit__` падает без проверки.
- Это регресс после правок 09.08 (там чинили `__aenter__` и `_first_or_fallback`, но не `__aexit__`).
- **Фикс:** добавить `if self._s:` перед `await self._s.__aexit__(*a)` (и аналогично в `__aenter__` проверить None). Файл `/opt/pipecat-agent/levitan_agent.py`, строки ~360-370.

### Тест-звонок (13:0x)
- Mango принял callback: `result 3103` (команда `test_levitan_z9y8x7w6`, номер 79859234644, from ext 22).
- **Мой звонок НЕ проявился** в событиях вебхука за последние 5 мин. Видны только звонки коллеги: 79781207028 (13:08:48 Appeared → 13:09:08 Connected → 13:12:48 Disconnected, диалог ~4 мин) и 79859234644 в 12:59 (cmd=test_1786366775).
- До агента звонки доходят штатно (есть Connected + комната levitan-sip-*), но LLM-инференс падает на баге выше → **диалог может молчать/падать**.

### Что сделано (Fixed)
- **Локально**: восстановлены зависимости (faiss-cpu, sentence-transformers, pydantic-settings, pytest-asyncio), 41/41 тестов проходят, ruff — 0 ошибок (убраны неиспользуемые total/since в call_cost.py).
- **Диагностика LLM**: денег на OpenRouter нет (ключ 63bf в минусе). Коллега нашёл провайдера OpenCode Zen (opencode.ai/zen/v1) — есть бесплатные модели, включая **deepseek-v4-flash-free** (подтверждено: 200, ответ «привет», 91+30 токенов). Каскад OmniRoute работает: deepseek 429 → ling-3.0-flash-free ответил «Привет! 😊».
- **OmniRoute на VPS обновлён 3.7.9 → 3.8.49** (причина: 3.7.9 не знает провайдеров oc/pollinations/ddgw/blackbox/kilocode, только opencode-zen; локальная 3.8.48 знает все). Установка через swap (OOM без него — npm жрёт 644MB при 1GB RAM). БД мигрировала (130 миграций), провайдеры opencode-zen-001 + openrouter сохранены. deepseek-v4-flash-free через OmniRoute работает (200).
- **NO_PROXY**: в /root/start-omniroute.sh добавлен opencode.ai (прокси 64468 резал SSE/стриминг — «Stream ended before producing useful content»).
- **systemd-дубль omniroute** остановлен и задизейблен (падал EADDRINUSE, pm2 — основной).
- **Swap** включён на VPS: `swapon /swapfile` + запись в /etc/fstab (иначе npm/Node OOM).

### НЕ ЗАВЕРШЕНО / ⚠️ ДЛЯ КОЛЛЕГИ
- Каскад «как на локалке» (комбо auto/free-coding: openrouter free → oc → pollinations → ddgw) **НЕ создан** на VPS. На VPS сейчас только combo `free-cascade` (5 моделей opencode-zen). Локально: OmniRoute 3.8.48 + провайдеры [kilocode, opencode(oc), pollinations, openrouter, blackbox, deepseek] + комбо auto/free-coding (20+ моделей).
- Free-тир OpenCode Zen даёт **429 rate limit** после серии запросов (суточный лимит). Локальный ключ sk-WU8Lh... на маке.
- STORAGE_ENCRYPTION_KEY warning в логах omniroute на VPS (БД существует, ключа в env нет — добавить в /root/.omniroute/.env).

### Блокеры
- SSH на 217.149.23.113:22 периодически refused; туннель 22001 через прокси как запасной (сейчас основной рабочий).
- Сеть VPS нестабильна: SSH рвётся на длинных командах (npm install, curl>40с).
- OpenRouter 63bf в минусе; free-модели OpenRouter с VPS дают 403 (без прокси-обхода).

### Следующие шаги (приоритет)
1. **Починить `__aexit__` NoneType** в levitan_agent.py:368 (главный баг, блокирует диалог).
2. Перезапустить агент → тест-звонок на 79859234644 → проверить полный диалог + save_lead.
3. Создать на VPS комбо auto/free-coding (копия локального) или использовать встроенное `auto/coding:free`.
4. Добавить STORAGE_ENCRYPTION_KEY в .env omniroute (убрать warning).

## 2026-08-09 (вечер) — НАЙДЕН И ИСПРАВЛЕН БАГ ТИШИНЫ

### Главное (Fixed)
- **Исправлен баг тишины после «Да»** в /opt/pipecat-agent/levitan_agent.py (3 правки):
  1. `DebugLLMStream.__aenter__` — теперь no-op. Раньше создавал ТРЕТИЙ стрим основной модели (leak), который никогда не закрывался → лишние конкурентные соединения через прокси.
  2. `_first_or_fallback` — теперь НЕ отменяет живые стримы при падении первого: цикл `while remaining` продолжает ждать следующий (deepseek упал 402 → gpt-oss:free выиграл). Раньше `await asyncio.wait(FIRST_COMPLETED)` + отмена всех = при быстром падении одного убивал второго.
  3. В гонку передаётся `conn_options=APIConnectOptions(max_retry=0, timeout=20)` вместо `max_retry=3, timeout=10` из kwargs → убраны скрытые внутренние ретраи (~34с на модель).
- **Verified в бою (звонок 19:14)**: deepseek 402 → цикл дошёл до gpt-oss → `[LLM] first chunk OK model=openai/gpt-oss-20b:free`. Найдено ТОЧНО, что тишину давал именно этот баг (плюс 402).
- **Скрипт `session_handoff.py`** (локально, /var/folders/.../opencode/lev/): сжимает сессии OpenCode в компактный handoff для умной модели (12KB вместо 53KB, фильтрует мусор tool/reasoning). Причина: ввод Sonnet 5 в старую сессию стоил $10 на контексте.

### Blocked / Грабли
- **SSH на VPS (217.149.23.113:22) снова Connection refused** (после успешной перезагрузки и теста). Ping идёт, SSH закрыт — временно. Нужна ещё перезагрузка в Timeweb либо ожидание.
- **`deepseek/deepseek-chat` НЕ РАБОТАЕТ**: ключ Levitan `sk-or-v1-63bf...` в минусе (-$0.44). Пополнение $10 ушло на покрытие старого долга. **Денег больше нет** → вернуться на free-роутинг (ling+gpt-oss).
- **Куда ушли $10 у пользователя**: НЕ Levitan! OpenCode-чат через `omni-auto-router/server.py` (Tier 3 → `anthropic/claude-sonnet-5`, ключ `sk-or-v1-8508f...` — ДРУГОЙ) сжёг $10.2 / 99.9% баланса. Ключ-перекрёстка нет: Levitan = `63bf...`, OpenCode/omni = `8508f...`.
- **Приветствие обрезано** (слышно с «...ранее вы заказывали»): TTS greeting = 2 сегмента по предложениям, lead-silence 5с prepended к первому — но клиент услышал только со второго предложения. НЕ исправлено.

### План на завтра (без денег)
1. Дождаться/перезагрузить VPS, SSH-проверка.
2. `LLM_MODEL=inclusionai/ling-3.0-tiny:free` (основная) + gpt-oss:free fallback (уже стоит fallback) → restart → тест-звонок. Проверить, что баг тишины ушёл (теперь gpt-oss выигрывает гонку за ~16с, ling за ~1.5-4с — нужен ling для скорости).
3. Если тишина ушла — правка приветствия: увеличить TTS_LEAD_SILENCE_SEC или приветствие одной фразой.
4. Скрипт session_handoff.py перенести в tools/ (полезен для всех).

### Сделано (Fixed)
- **LLM переведён на OpenRouter**: `LLM_MODEL=inclusionai/ling-3.0-tiny:free` (основная, TTFT ~1.4с) + `LLM_FALLBACK_MODEL=openai/gpt-oss-20b:free`. `LLM_BASE=https://openrouter.ai/api/v1`, `OPENAI_API_KEY=sk-or-v1-63bf...`, `LLM_PROXY=http://Q3NeJXTY:dsBaWh2L@172.120.21.141:64468` (HTTP 64468 ЖИВ с VPS! 403 без прокси).
- **Gemini free отброшен**: 429 «Quota exceeded... limit 20 req/день» (GenerateRequestsPerDayPerProjectPerModel-FreeTier). Мёртв для обзвона.
- **omniroute 20128 отброшен**: игнорирует model из запроса (всегда свой каскад), 502 с полным промптом/tools.
- **Конкурентный LLM-запуск** (новое в `DebugLLM`/`DebugLLMStream` в /opt/pipecat-agent/levitan_agent.py): на ход создаются стримы ОБЕИХ моделей параллельно, первый пришедший chunk побеждает, проигравший закрывается. Гейт ждёт первый chunk до 18с.
- **preemptive_generation ОТКЛЮЧЁН** (был в turn_handling): вызывал отмены стримов при final реплики пользователя → тишина.
- **faq_lookup_tool убран из tools** (лишний tool-цикл +2-4с на ход); остались [save_lead, end_call]. Промпт: «не вызывай инструменты поиска, отвечай по фактам».
- **Воронка: НЕ считать общую сумму** — только цена за голову по шкале (90/85/80/75₽) — по просьбе пользователя (ускорение диалога).
- **nemotron-3-ultra:free выброшен** — reasoning-модель озвучивала англоязычные размышления через TTS.
- **SSH**: прямой ssh на 217.149.23.113:22 СНОВА РАБОТАЕТ (был закрыт); туннель 22001 (ssh_tunnel.py) тоже жив. Использовать прямой.
- Задержка «Да»→ответ: **2-4с днём** (было 11-15с). Звонок 16:45 Verified пользователем: полный диалог, всё слышно.

### Blocked / Грабли
- ❌ **Free-тир OpenRouter нестабилен вечером** (16:49-16:55): ling 429 (Novita upstream rate-limit), gpt-oss очередь >18с. Днём работает (1-4с).
- ❌ Gemini free 20 req/день; omniroute каскад 502; nemotron reasoning-шум.
- ❌ прокси 64469 (SOCKS5) мёртв; 64468 HTTP жив только с VPS (с локалки CONNECT timeout).

### План на завтра (пользователь пополняет счёт OpenRouter)
1. **Проверить баланс OpenRouter** (`https://openrouter.ai/api/v1/auth/key` с ключом sk-or-v1-63bf..., usage/limit).
2. **Переключить на платный**: `LLM_MODEL=deepseek/deepseek-chat` (или grok-4.5 при желании) в /etc/systemd/system/levitan-agent.service → daemon-reload → restart → звонок. Цена: $5 ≈ 300+ звонков.
3. Проверить end-to-end: «Да» → 175 голов → цена за голову → save_lead в Bitrix (incubird.bitrix24.ru).
4. По желанию: убрать debug-печати [LLM]/[TTS]/[STT] из levitan_agent.py (много логов).
5. Зафиксировать эталонную конфигурацию в скилле `levitan-voice-agent`.

### Конфиг (актуальный)
- Файл: `/opt/pipecat-agent/levitan_agent.py` (DebugLLM+DebugLLMStream, гейт, конкурентный запуск)
- systemd: `/etc/systemd/system/levitan-agent.service` (LLM_MODEL/LLM_FALLBACK_MODEL/LLM_BASE/LLM_PROXY/OPENAI_API_KEY)
- tools=[save_lead, end_call] | TTS=Yandex | STT=deepgram nova-3 ru | лид-тишина 5с | allow_interruptions=False | preemptive OFF



### Сделано (Fixed)
- **LLM переключён на Gemini 2.5 Flash** (YandexGPT-lite УБИТ модерацией — подробности в граблях). Unit levitan-agent: `LLM_MODEL=gemini-2.5-flash`, `LLM_BASE=https://generativelanguage.googleapis.com/v1beta/openai`, `LLM_PROXY=socks5h://Q3NeJXTY:dsBaWh2L@172.120.21.141:64469`, `OPENAI_API_KEY=<GEMINI_API_KEY из /opt/levitan/projects/ai-eggs/.env>`.
- **Ключ Gemini найден** в `/opt/levitan/projects/ai-eggs/.env` (GEMINI_API_KEY), free tier 60 req/min.
- **Прокси-находка**: HTTP-прокси **64468 МЁРТВ** (connection timeout), живой — **SOCKS5 64469** (`socks5h://`). Для Gemini из РФ — ТОЛЬКО 64469.
- **Макетный тест ПО ДО деплоя** (изолированный скрипт /tmp, системный python): «Джанкой» → НЕТ фильтра (YandexGPT отказывал!), «Мой номер 8 923 456 78 90» + tool → Gemini сам вызвал `save_lead` с нормализованным телефоном `89234567890`, quantity `137 голов`, city `Симферополь`. TTFT 1.5-3.0с.
- **Звонок-15 (17:07, абонент 79859234644) — живьём на Gemini**: полный цикл. «Нет, не интересен» → end_call сработал (`[CALL] end_call requested` → звонок завершился). TTFT ~1.8с.
- **Пользователь Verified**: «звонок пришёл — всё ок». Приветствие полное (лид-тишина 5с работает). Завтра — серьёзное тестирование.

### Verified (звонок-15, 17:07)
- Gemini без фильтров: абонент ответил «Нет» → агент вежливо завершил (end_call). Задержка ответа ~1.8с.

### Грабли (коротко, полный список в скилле)
- ❌ **YandexGPT-lite**: content_filter на «Джанкой» («не могу обсуждать») и на ТЕЛЕФОНЫ (8 923...). Фильтр на уровне API, промпт не лечит. Пользователь живёт в Джанкое!
- ❌ **DeepSeek/OpenRouter**: 402 «only afford ~500 tokens» — баланса нет, системный промпт 2168+ токенов → 402 на каждой реплике → агент немой, абонент вешает трубку.
- ❌ HTTP-прокси 64468 мёртв; живой только SOCKS5 64469.

### ЭТАЛОННАЯ КОНФИГУРАЦИЯ (обновлена в скилле `levitan-voice-agent`, разделы 4, 7, 10, 11)
LLM=gemini-2.5-flash (generativelanguage.googleapis.com/v1beta/openai, SOCKS5 64469) | TTS=Yandex alena emotion=good | STT=Deepgram nova-3 ru | TTS_LEAD_SILENCE_SEC=5.0 | load_threshold=math.inf | tools=[faq_lookup_tool, save_lead, end_call] | воронка: Да→«сколько голов и город», Нет→end_call | save_lead: детерминированный парсер телефона + LLM-tool | Bitrix crm.lead.add.

### Открыто (на след. сессию — серьёзное тестирование)
1. **Тест сохранения лида end-to-end** (сегодня лид не создан: звонок-15 «Нет» → end_call).
2. Прогнать полную сделку на Gemini: «Да» → объём+город → телефон → save_lead → проверить лид в Bitrix (incubird.bitrix24.ru).
3. Сравнить качество диалога Gemini vs YandexGPT (там, где фильтр не мешал).
4. max_tokens: в тесте finish=length на коротком ответе — при необходимости увеличить с 512.

### План снижения задержек (решение 08.08, приоритет после серьёзного теста)
1. **Изолированный замер TTFT: Gemini Live API (real-time голосовая) vs текущий стек** — тот же тест-скрипт. Gemini Live: TTFT 0.1-0.3с (модель стримит речь, без TTS-контура), ожидание 10x выигрыш. Скилл `genai-live-quickstart` в копилке, pipecat поддерживает `GeminiLiveLLMService`, тот же free-ключ Gemini из `/opt/levitan/projects/ai-eggs/.env`. Если выигрыш подтверждается → переезд на Live.
2. **VPS-апгрейд Timeweb: 1 → 2-4 vCPU + RAM 2→4GB** (узкое место — CPU, не RAM; load 1.0=100%, STT+TTS+LLM конкурируют).
3. **Платная модель если нужно качество**: Grok 4.5 (`x-ai/grok-4.5`, $0.002/$0.006/M, контекст 500K) через SOCKS5 64469 (НЕ 64468 — мёртв), либо платный Gemini. Даёт качество диалога, НЕ задержку. OpenRouter — только при балансе (402 = немой агент).
4. Retell AI (<500мс из коробки) — только если Gemini Live не зайдёт.

## 2026-08-08 (день, 14:00-14:20) — ВНЕШНИЕ ОБЗВОНЫ РАБОТАЮТ: полный цикл INVITE→200 OK→ACK→диалог. Эталонная конфигурация зафиксирована

### Сделано (Fixed)
- **Полный SIP-цикл Манго→VPS восстановлен**: INVITE (81.88.86.11) → 200 OK → **ACK приходит** (14:03:37, раньше never). Звонки обзвона доходят, диспатч создаёт job, агент отвечает.
- **Входящий на 78612025110 — НЕ ПРОБЛЕМА**: номер настроен как ИСХОДЯЩИЙ, входящие не предусмотрены (подтвердил пользователь). «Appeared→Disconnected» на звонок с мобильного — штатное поведение АТС.
- **Корень обзвона найден**: 07.08 08:20 обзвоны упали из-за ребута VPS (10:27) — умерла регистрация user4. Регистрация восстановлена: pm2 `levitan-sip-register` (`/opt/livekit/sip_register.py`, REGISTER 60с → 200 OK).
- **Диспатч-правило починено**: SDR_yZuSLajiYrJF имел ПУСТЫЕ trunk_ids → job не создавался. Правка через Redis protobuf (Twirp 401): trunk_ids=[ST_D3x99V4JzSgy, ST_Tcn73hGzvoJS]. deploy.sh обновлён.
- **VoIP-сканеры заблокированы** (172.86.66.55 Grandstream, 46.19.137.194 Yealink, AbuseIPDB 15k+) — iptables + /etc/iptables.rules + crontab @reboot.
- **LLM переключён на OmniRoute**: OpenRouter кредиты кончились → 402 убивал AgentSession ДО приветствия (абонент слышал тишину, 14:11). Free-модели OpenRouter сняты (404). Unit levitan-agent: `LLM_MODEL=omni-auto`, `LLM_BASE=http://127.0.0.1:20128/v1`, `LLM_PROXY=`, `OPENAI_API_KEY=omni`. OmniRoute отвечает (deepseek-chat через StreamLake).
- **Лид-тишина 2.5с → 5.0с** (`TTS_LEAD_SILENCE_SEC=5.0` в /opt/pipecat-agent/.env): пользователь Verified «приветствие менее обрезанное» (звонок 14:14).

### Verified пользователем (14:14, звонок на 79859234644)
- Приветствие слышно (менее обрезанное), **диалог состоялся** (агент ответил «Вывод цыплят у нас проходит по понедельникам и четвергам...»), пользователь прервал из-за **задержки ответа** (LLM).

### LLM переключён на YandexGPT-lite (задержка ответа УСТРАНЕНА)
- Замеры TTFT (VPS): OmniRoute 2.3-3.2с (медленно, пользователь прерывал) vs **YandexGPT-lite 98-740мс** через OpenAI-совместимый endpoint `https://llm.api.cloud.yandex.net/v1` (тот же YC_API_KEY, что и SpeechKit).
- Unit levitan-agent: `LLM_MODEL=gpt://b1gaikufqg8b6i83he8n/yandexgpt-lite/latest`, `LLM_BASE=https://llm.api.cloud.yandex.net/v1`, `OPENAI_API_KEY=<YC>`, прокси пусто.
- Verified в живом звонке 14:33: вопрос → ответ ~1с, полный диалог продажи (цена 90р/голова, объём 56 голов, доставка в Джанкой).

### ЭТАЛОННАЯ КОНФИГУРАЦИЯ Манго голосового агента (глобально, для всех проектов)
Записана в скилл `levitan-voice-agent` (SKILL.md, раздел «ЭТАЛОННАЯ КОНФИГУРАЦИЯ») + claude-mem decision 5556218f. Кратко: (1) живая REGISTER-регистрация (pm2 sip_register.py) — без неё АТС не шлёт INVITE; (2) диспатч с trunk_ids (Redis protobuf, strip \n после redis-cli --raw); (3) кодеки PCMU+PCMA, G722 off; (4) **LLM: YandexGPT-lite (OpenAI-совместимый endpoint, TTFT <1с)**, OmniRoute fallback, OpenRouter запрещён; (5) TTS_LEAD_SILENCE_SEC=5.0; (6) callback только с VPS, ключи /opt/levitan/.env.

### Открыто (на след. сессию)
1. Оценить качество ответов YandexGPT-lite в продажах (есть отписки «Я не могу обсуждать эту тему» — при необходимости yandexgpt/latest или тюнинг промпта).
2. Контрольный полный прогон: обзвон → приветствие → диалог → закрытие сделки → сохранение лида (CRM/база).


## 2026-08-08 — Входящие звонки Манго: firewall найден и удалён, КОРЕНЬ no-ack установлен (перелом 02.08, кодеки PCMU→G722)

### Сделано (Fixed)
- **Убийца 5060 найден и удалён навсегда**: systemd `levitan-sip-drop.service` (nft-таблица `levitan_raw`, дроп всех UDP 5060 кроме {81.88.86.11, 10.170.19.206}, создан 07.08 19:58 UTC, enabled — возвращался при ребуте). `systemctl stop/disable` + `nft delete table ip levitan_raw`. Verified: внешний OPTIONS на 217.149.23.113:5060 → 200 OK, INVITE от Манго пошли (tcpdump подтвердил, первый 11:23:56).
- **Вторая причина — ws_url**: был `ws://localhost:7880/rtc` (WebSocket 404 → «Cannot create LiveKit participant»). Исправлено на `ws://localhost:7880` (без `/rtc`), pm2 restart. В `/opt/livekit/sip.yaml.bak` эталон уже был правильный.
- **Третья причина — КОДЕКИ (корень, главная находка!)**: наш SDP-ответ содержит только `G722` (плюс telephone-event), а Манго ждёт G711 (PCMU/PCMA) → отклоняет 200 OK → **не шлёт ACK** («Call accepted, but no ACK received» → media timeout → BYE).
  - **Перелом установлен по логу**: no-ack начались С 02.08 19:45 (старт процесса NE_N3iegM6CeQWX = бинарник из исходников `/opt/src/sip`, media-sdk@2026-07-23; docker pull livekit/sip:v1.8.0 падал с 429). До этого входящие работали (PCMU).
  - Доказательство из лога: в РАБОЧЕМ звонке 02.08 19:53:08 (66.96.91.238) `audio-codec: "PCMU/8000"` (audio-rtp 0) + Joining room + participant joined + published track + hangup. Сейчас (08.08): `audio-codec: "G722/8000"` (audio-rtp 9) + no-ack.
  - Попытка фикса через yaml `codecs: {G722/8000: false}` (опция `Codecs map[string]bool` в config.go:125, применяется в service.go:217 `DefaultCodecs().SetEnabledMap`) — залогирована «codec disabled G722/8000», НО SDP-ответ 11:43 всё равно содержал G722 → **для входящих по транку mconf.Codecs берётся НЕ из yaml** (inbound.go:1116 `mconf.Codecs` из trunk/API-диспатча). НЕ РАБОТАЕТ — вернуть/удалить блок `codecs:` из sip.yaml.
- **Посторонний трафик 5060**: no-ack массово с чужих IP — 185.213.155.242 (13655!), 66.96.91.238 (4095), 89.190.156.22 (1222), 51.178.122.178, 91.134.23.126, 153.75.91.175 (спам/сканеры + возможно часть пулов Манго: 66.96.91.238 давал рабочий звонок 02.08 19:53!). Реальный Манго сегодня: **172.86.66.55** (тестовые звонки на 0021442037699931@) и **46.19.137.194** (0073908351673002@, отвечает 400 sdp-error). Старый whitelist 81.88.86.11 — тоже Манго-пул (рабочий ранее).

### Блокеры
- Манго по-прежнему не шлёт ACK на наш 200 OK (8 сек → media timeout → звонок закрыт). Гипотеза: Манго принимает только G711-ответ; текущая сборка отвечает G722.
- На INVITE от 46.19.137.194 мы отвечаем **400 Bad Request** (`Cannot start media`, reason `sdp-error`) — вторичная проблема, копать после ACK.
- VPS 1 ядро CPU (load 1.0 = 100%), лог livekit-sip-error.log 1.9 GB (подъедает диск 83%).
- tcpdump на eth0 пуст при живых звонках → ловить `-i any` (видит всё).

### На следующую сессию (приоритет)
1. **Заставить inbound отвечать PCMU/PCMA**: пропатчить `/opt/src/sip/pkg/sip/media_codecs.go` — в `init()` убрать G722 (`g722.SDPNameAndRate: false`), пересобрать (go build в /opt/src/sip, `BUILD_EXIT=0` как в /root/sip_build.log), скопировать в /usr/local/bin/livekit-sip, pm2 restart, тест-звонок. (yaml-фикс `codecs:` НЕ влияет на inbound — проверено 11:43.)
2. Если Манго всё равно не шлёт ACK — сравнить наш 200 OK с работавшим (02.08): tcpdump-образцов нет, но можно сравнить SDP: 02.08 отвечали одним кодеком PCMU, сейчас — списком с opus/48000 первым (Mанго может брать ПЕРВЫЙ кодек = opus и не уметь его).
3. Разобраться с 400 sdp-error от 46.19.137.194 (после фикса кодеков).
4. Очистить лог livekit-sip (1.9 GB) и добавить logrotate/pm2 flush.
5. Проверить GOLDEN BASELINE (test42) после любых правок livekit-sip.

### Ключевые факты для диагностики (не потерять!)
- Входящий звонок Манго: INVITE → 100 Processing → 180 Ringing → 200 OK (SDP) → **Манго должен прислать ACK** → media. Сейчас ACK НЕ приходит вообще (ни одного за все тесты 08.08).
- Манго НЕ ретранслирует INVITE при отсутствии ACK-фазы — значит 200 OK до него доходит (иначе были бы ретрансмиссии T1×2ⁿ).
- Ответ мы шлём на source-порт INVITE (172.86.66.55:64468 при Via:5060;rport) — по стандарту; на 5060 отвечать не пробовали (если понадобится — `SymmetricRTP`/адресация в конфиге).
- SIP-каркас проверен: OPTIONS → 200 OK; WebSocket к livekit-server:7880 — 200; участник создаётся (SCTP-данные шли в 11:27), агент не виноват.
- Тестовая линия: звонить на **78612025110**, абонент 79859234644. Устройства для теста: `/opt/livekit/sip_test_ua.py`, `sip_test_ua2.py`, `sip_register.py` (исходящие), tcpdump `-i any port 5060`.

## 2026-08-07 (ночь) — Lead-silence fix + GOLDEN BASELINE восстановлен, VPS сеть нестабильна

### Сделано
- **Lead-silence 2.5с добавлен в YandexTTS.synthesize** — приветствие полное («Здравствуйте! Предлагаем вам...»), Verified пользователем на живом звонке (07:59)
- **GOLDEN BASELINE (test42) задокументирован** в chp.md и claude-mem (decision 14057e7d)
- **Новый ключ OpenRouter + модель**: `sk-or-v1-e45b...` + `google/gemini-2.5-flash-lite:free` (бесплатная)
- **`.env` и агент обновлены** на VPS (22:29), файл `/opt/pipecat-agent/levitan_agent.py` сохранён (md5 ab02e176...)
- **Бенчмарк латентности** написан (`bench_latency.py` на VPS, нужно исправить поле CreateAgentDispatchRequest)

### Не решено
- Исходящие звонки через Манго не доходят до VPS (Appeared→Disconnected мгновенно) — возможно, проблема на стороне Манго (баланс/линии)
- Сеть VPS 217.149.23.113 крайне нестабильна (ping 670-800мс, SSH 95% отказов, TCP порты закрываются) — вероятно, сетевой мост гипервизора Timeweb. Hard reboot не помог.

### На следующую сессию
1. Решить проблему сети VPS (поддержка Timeweb или переезд)
2. Проверить баланс/линии Манго (исходящие звонки)
3. Запустить latency-бенчмарк (исправить поле CreateAgentDispatchRequest)
4. Обновить SYSTEM_PROMPT (новый телефон +7(918)047-50-01, августовский график, сайт incubird.ru)
5. Прогнать несколько тестовых звонков на 79859234644 для стабилизации диалога

---

## 🔄 2026-08-07 (вечер) — Сервер ребутнулся; диагностировал, всё готово, НО Mango не дозванивается

**Handoff для следующей сессии. Кратко:**
- Сервер `217.149.23.113` перезагрузился ~10:27 UTC (uptime был 27 мин). Всё поднялось: livekit-server (pid 628, :7880, ручной), агент (systemd `levitan-agent`, pid 629, воркер `available`), livekit-sip v1.9.0 (:5060), OmniRoute (:20128).
- Мой IP (193.56.253.30) получил бан файрвола сервера → прямой SSH `:22` отвечает `Connection refused` (все порты RST). **Обход: локальный python-туннель через US-прокси** `127.0.0.1:22001` → CONNECT → `217.149.23.113:22`. Файл: `/var/folders/18/9bft2pp5595d231dd_zfs4240000gn/T/opencode/ssh_tunnel.py` (в /tmp, может не пережить перезагрузку сессии). Команда входа: `ssh -p 22001 root@127.0.0.1`. Если бан снят — вернуться на прямой `ssh root@217.149.23.113`.
- **Удалены лишние pm2-дубли** (после ребута pm2 resurrect поднял дубли, конфликтовали, падали в циклах рестартов, CPU 99.9%): `pm2 delete 14` (livekit-server), `pm2 delete 16` (levitan-agent), `pm2 save`. Живые: livekit-server pid 628 (вручную), агент systemd pid 629, sip pid 723, omniroute pid 1078.
- **Проблема осталась в Манго, НЕ у нас:** `test_callback` возвращает `result 1000` (принят), но вызов `Appeared → Disconnected` **в ту же секунду** (11:54:44), `CALL ENDED`, без дозвона до абонента 79859234644. Звонox идёт через линя `sip:user4@vpbx400161137.mangosip.ru`. Аналогично у другого звонка (79786258354, линия user1). Похоже на проблему исходящих линий/баланса Mango, НЕ на нашу конфигурацию.
- Важно: отчёт Манго-вебхука виден в `strings /root/.pm2/logs/levitan-webhook-error.log` (пример `events/call` + `Disconnected`).
- GOLDEN BASELINE (test42, ниже) НЕ тронут — эталон по-прежнему актуален.

**Самый удачный результат на текущий момент. ВСЕ будущие улучшения меряются от него — любой регресс (тишина, обрыв ответа, сброс генерации) = возврат к этой конфигурации.**

### Что было достигнуто (07:57-08:00, абонент 79859234644)
- Приветствие + ответы LLM озвучиваются, полный многоходовый продающий диалог: «Отлично, Симферополь — это по Крыму, туда у нас есть доставка!..», «Поняла вас! 100 цыплят РОСС-308», «Подскажите, пожалуйста, на какую дату вам нужна доставка?»
- Ответы начинают звучать за **~1.5-2с** после реплики абонента (LLM TTFT ~1с + TTS 0.3с)

### Эталонная конфигурация (плясать от неё)
| Компонент | Значение |
|-----------|----------|
| LLM endpoint | **напрямую** OpenRouter `https://openrouter.ai/api/v1` (НЕ OmniRoute :20128) |
| LLM model | `google/gemini-2.5-flash` |
| LLM | `max_completion_tokens=512`, `temperature=0.3` |
| LLM ключ | `sk-or-v1-63bf...` (~$9 баланс; НЕ 8508... — лимит $20 исчерпан) |
| Прокси | только в `httpx.AsyncClient(proxy=...)` для LLM; **НЕ** глобальные HTTP_PROXY в systemd (ломают ws://localhost:7880 воркер) |
| TTS | Yandex SpeechKit alena, 8kHz lpcm |
| STT | Deepgram nova-3 ru |
| livekit-sip | v1.9.0 (dev-билд в `/usr/local/bin/livekit-sip.dev-backup`), sip 5060, rtp 16384-32768 |
| Агент | `/opt/pipecat-agent/levitan_agent.py` + `DebugLLM`-обёртка (лог `[LLM] chunk#N`) |

### Уроки (не повторять)
- OmniRoute хранит ключ OpenRouter в SQLite `provider_connections` и env не читает — при «requires more credits» смотреть баланс ключа, а не конфиг
- Абонент вешает трубку через ~2с после вопроса → модель медленнее TTFT 2.5с = потеря звонка
- deepseek-chat с tools: TTFT ~2.3с; gemini-2.5-flash: ~1с

## 2026-08-02 — LiveKit SIP pipeline построен: real-time голосовой агент

**Сессия завершена. Тест real-time диалога — ЗАВТРА.**

### Сделано

1. **SIP активирован в LiveKit**: Redis установлен (apt), `livekit-server` v1.13.4 перезапущен через pm2 с redis-конфигом. Выяснено: SIP-стек — отдельный сервис, встроенный API не ходит в сеть.
2. **livekit-sip v1.8.0 собран из исходников** на VPS:
   - Go 1.26.5 скачан с go.dev (в apt только 1.18, а go.mod требует 1.26)
   - OOM при сборке → swap 2GB + `GOFLAGS=-p=2` → успех
   - buf.build 403 → сборка через US-прокси `172.120.21.141:64468`
   - Бинарник: `/usr/local/bin/livekit-sip`, конфиг `/opt/livekit/sip.yaml` (sip_port 5060, rtp 16384-32768 — уже открыт в UFW)
3. **SIP trunk** `mango-inbound` (ST_QYANmRqv5p4b):
   - Манго УЖЕ звонит на :5060 (INVITE от 172.110.223.114, username=1000)
   - Digest-auth не подходит (Манго шлёт свой внутренний номер) → trunk **без auth**, разрешён по IP `172.110.223.114`
4. **Dispatch rule** `levitan-inbound` (SDR_yZuSLajiYrJF): каждый входящий звонок → комната `levitan-sip-*` (individual, no_randomness)
5. **Pipecat-агент переписан** (`/opt/pipecat-agent/levitan_agent.py`, бэкап `.bak`):
   - LLM → **OmniRoute** (`omni-auto`, `http://217.149.23.113:20128/v1`) — обходит 403 OpenRouter
   - TTS → **Yandex SpeechKit alena** (кастомный класс `YandexTTS` pipecat, 8kHz PCM lpcm)
   - STT → Deepgram nova-3 ru (ключ есть)
   - **FAQ-tool** `faq_lookup_tool` (238 триггеров) — типовые вопросы без LLM, fast path
   - Цены актуализированы: 90/85/80/75₽ (до 100 / 101-300 / 301-999 / 1000+)
   - Greeting: «Здравствуйте! Предлагаем вам суточных цыплят Росс 308 по цене от 75 рублей за голову. Вам интересно?»
6. **pm2 процессы**: `livekit-server` (20), `livekit-sip` (21), `levitan-agent` (22) — все online, `pm2 save` выполнен
7. **baresip остановлен** (pm2 stop baresip-sip) — порт 5060 отдан livekit-sip. Конфиг сохранён, fallback возвращается `pm2 start baresip-sip`

### Известные проблемы
- Docker Hub 429 (rate limit) → сборка из исходников, docker не используется
- OmniRoute не поддерживает `/v1/audio/speech` (TTS через шлюз не выйдет — Yandex напрямую)
- Dispatch rule применяется ко ВСЕМ trunk (trunk_ids пустой) — ок для одного trunk
- livekit-server использует devkey/secret (6 символов) — warning про короткий secret

### На след. сессию
1. **Тест real-time диалога**: звонок через Манго → greeting → STT → FAQ/LLM → TTS (~1 сек вместо ~25 сек)
2. Проверить дедупликацию событий в `levitan_faq_agent.py` (watch_for_calls — старые события events.jsonl)
3. Решить судьбу baresip: оставить как fallback (конфиг готов) или удалить
4. Обновить `docs/adr/` и README_ANGELLA под LiveKit SIP

---

## 2026-07-21 — baresip auto-answer debug: root cause found

### Сделано
- **Регистрация работает**: baresip REGISTER → 200 OK (чистый конфиг без ctrl_tcp/aubridge)
- **Исследован механизм auto-answer**: логика в `ua.c:379-398` → `CALL_EVENT_INCOMING` → `ANSWERMODE_AUTO` → `call_answer(200)`. В `ua.c` v1.0.0 код есть
- **`answermode_decode`** вызывается в `account.c:421` с `&acc->laddr.params` — корректно
- **menu.so** использует `answermode` ТОЛЬКО для рингтона (НЕ для auto-answer) — `menu.c:850`
- **aubridge.so** загружается как `auplay` + `ausrc` — не помогает (180 Ringing, нет 200 OK)
- **Вероятная причина**: `answermode=auto` НЕ парсится из account params, аккаунт остаётся `ANSWERMODE_MANUAL`, поэтому `call_event_handler` шлёт `UA_EVENT_CALL_INCOMING` вместо `call_answer()`
- **План**: написать custom auto-answer модуль baresip (недоступны dev-хидеры, нужно скачать с GitHub)

### Issues / Blockers
- **answermode=auto не работает** — 180 Ringing есть, 200 OK нет
- **baresip-dev** не установлен — нет `baresip.h`, компиляция модуля невозможна
- **claude-mem 401** — не исправлен
- **OpenRouter 403** — LLM заблокирован

### Next Steps
1. Скачать хидеры baresip с GitHub → скомпилировать `autoanswer.so`
2. Загрузить модуль на VPS → протестировать auto-answer
3. После соединения звонка — протестировать RTP-захват

## 2026-07-21 — VPS baresip настроен, FAQ-кэш расширен до 230+ триггеров

### Сделано
- **baresip настроен** на VPS (217.149.23.113): accounts (`user4@vpbx400161137.mangosip.ru`) + audio config (PCMU, 8kHz, FIFO `/tmp/levitan_fifo`, auto-answer)
- **`levitan_faq_agent.py`** скопирован на VPS, зависимости установлены (`faster-whisper`, `edge-tts`, `python-dotenv`, `ffmpeg`)
- **FAQ-кэш расширен +20 триггеров**: Кобб-500 температура/уход (33-35°C → 20-22°C), Росс-308 температура, плотность посадки, свет, влажность, кормление, вес, сравнение пород
- **claude-mem**: сервер на :37878 не отвечает (401 Unauthorized) — память недоступна, запись невозможна

### Issues / Blockers
- **claude-mem 401** — сервер beta не принимает ключ, память сессии не записана
- **OpenRouter 403** — LLM по-прежнему заблокирован, агент работает на FAQ-кэш + local fallback
- **VPS baresip** — нужно проверить регистрацию на Mango SIP

### Next Steps
1. Проверить регистрацию baresip на Mango (SIP 200 OK)
2. Сгенерировать greeting WAV на VPS
3. Запустить `levitan_faq_agent.py` в режиме мониторинга звонков
4. Починить claude-mem (перевыпустить API ключ)


---

## 2026-07-17 — Анжелла (turbo-FAQ агент готов, локальный звонок заблокирован NAT)

### Done
- **turbo-FAQ агент Анжеллы** создан и протестирован: `deploy/levitan_faq_agent.py` (turn-based движок: Mango callback + baresip + faster-whisper STT → FAQ-кэш → мгновенный TTS [без LLM] ИЛИ LLM-fallback → TTS).
- **FAQ-кэш расширен до 210 триггеров** (`docs/ANGELLA_BROILERS_FAQ_CACHE.json`) — 100% HIT на типовых вопросах (12/12, 20/20).
- **Яндекс SpeechKit TTS** (`alena`, 8kHz PCM) — подключён и работает (200 OK, генерит WAV). Заменил edge-tts (тот падает с `NoAudioReceived`).
- **Локальный LLM-fallback** (`_local_fallback`) — агент не немеет при падении всех LLM (OpenRouter 403, Сбер timeout).
- **Генератор приветствия** `deploy/levitan_greeting.py` — пишет `/tmp/levitan_greeting_lead.wav` (12.1с, 8kHz).
- **Loopback-тест диалога** `deploy/test_angel_local.py` — проверен полный цикл: цена→заказ→доставка→телефон→прощание.
- **Деплой-скрипт** `deploy/deploy_angel.sh` — rsync + systemd + baresip + venv на VPS.
- **Дока** `docs/README_ANGELLA.md` + обновлён `docs/adr/ADR-001-yandex-realtime.md` (статус: Realtime 404, LLM 403, автономный режим работает).
- **Исправлен `mango_callback`**: `from.number=user1` (НЕ `extension:22`!) — `sip:22@...`→403, `sip:user1@...`→200 OK. Добавлено `MANGO_FROM_NUMBER=user1` в `.env`.
- **`BARESIP_RECORD_DIR`** исправлен на macOS-путь (`~/.baresip/recordings`).

### Issues / Blockers
- **Яндекс Realtime WebSocket** — endpoint НЕ СУЩЕСТВУЕТ (все варианты 404). Грабль из статьи 2026, не рабочий публично.
- **LLM (OpenRouter)** — ЗАБЛОКИРОВАН (403 "Access denied by security policy"). Сбер GigaChat — timeout. Агент работает на local-fallback.
- **baresip на macOS НЕ ПРИНИМАЕТ входящий SIP** (NAT/firewall) — регистрация 200 OK, но INVITE не доходит. Звонок не дошёл до клиента.
- **VPS `72.56.38.19` недоступен** (порт 22 closed) — деплой невозможен.

### Next Steps
1. Поднять VPS (порт 22) → `bash deploy/deploy_angel.sh` → реальный звонок через greeting_bridge (проверенная схема из STATUS.md).
2. Починить baresip auto-answer (VPS или STUN+firewall локально).
3. Восстановить LLM: рабочий OpenRouter ключ ИЛИ Yandex Foundation Models (`gpt://folder/yandexgpt`).
4. Поддержка Яндекса про Realtime endpoint (если нужен speech-to-speech).

---

## 2026-07-07

### Done
- **Mango S2T integration**: Создан `scripts/mango_s2t.py` — модуль для получения расшифровок через API Речевой аналитики Mango (расширенная статистика → recording_id → recording_transcripts)
- **CRM save fix**: Бот теперь сохраняет ВСЕ звонки в CRM (включая no_answer — статус `no_contact`), а не только отвеченные
- **Recording link**: В заметки CRM добавляется ссылка на запись Mango
- **Backfill command**: Добавлена команда `/backfill` — подтягивает конспекты из Mango для существующих контактов
- **CRM API**: Добавлены `crm_find_by_phone()` и `crm_update_contact()` для обновления контактов
- **Evening dial DB**: Сформирован `_for_evening_dial_2026-07-07.csv` — 73 контакта (35 свежих ping + 38 повтор no_answer)
- **Extended stats API**: Настроен поиск recording_id через расширенную статистику ВАТС (search_string filter)
- **Bot keyboard**: Все `ReplyKeyboardMarkup` теперь с `is_persistent=True`

### Issues / Blockers
- `recording_transcripts` API — жёсткий rate limit (429), не удалось получить текст расшифровки
- `s2t/queries/` (Speech Analytics) — возвращает 3128 (возможно не подключена услуга)
- VPS недоступны — CRM и бот работают только локально
- Whisper STT не используется (качество плохое из-за музыки ожидания)
- Бот не находит recording_id после звонка → думает "не ответил" → шлёт неверный отчёт

### Next Steps
1. Убрать автоотчёт результатов из Telegram (пользователь не нуждается)
2. Дозвонить оставшиеся контакты (73 в базе, ~60 после фильтра)
3. Настроить вебхук Mango для автоматического получения конспектов
4. Разобраться с rate limit recording_transcripts

---

## 2026-07-07 (вечер) — Ralph Phase-1 Production Readiness

### Done
- **US-001**: Настроен ruff linting в pyproject.toml, исправлено 392 нарушения + 3 E501 ручных. Создан pyproject.toml, requirements.txt обновлён
- **US-002**: Создан `src/levitan/utils.py` с `normalize_phone()` — поддержка +7/8/7XX, скобок, тире, пробелов. 10 тестов
- **US-003**: Создан `src/levitan/crm_enricher.py` — CrmProvider ABC + MockCrmProvider + CrmEnricher. 5 тестов
- **US-004**: CRM enrichment интегрирован в dialer_bot.py — обогащение контакта перед звонком + в `_process_summary`
- **US-005**: В `save_to_crm` добавлены `duration_sec` и `operator` поля
- **US-006**: Заменён class Config на model_config = ConfigDict во всех 8 settings классах. 0 deprecation warnings
- **US-007**: Создан `scripts/campaign_runner.py` — 100-звоночный baseline с метриками (connect_rate, avg_duration, calls_per_hour). 1 тест

### Metrics
- Тестов: 25 → 41 (все проходят)
- Ruff: 445 ошибок → 0
- Pydantic warnings: 8 → 0
- Все 7 US помечены passes=true в prd.json

### Next Steps
1. Запустить реальный обзвон через campaign_runner с валидным MANGO_API_KEY
2. Проанализировать метрики 100 звонков
3. Рассмотреть переход на реальный CRM API провайдер (вместо MockCrmProvider)

---

## 2026-07-09 — Eval-driven Dev (global) + FAQ upgrade

**Статус:** Global session — не относится напрямую к Levitan.

### Сделано (по другим проектам)
- TG токен @Angella26bot заменён в 6 файлах
- SkillSpector установлен (NVIDIA, 12K stars)
- FAQ negative synonyms + aliases для Angela
- Eval suite 47 тестов, pre-commit hook
- AGENTS.md: пункт 8 (дайджесты) + 9 (eval-driven dev)

### Блокеры Levitan
- VPS 185.39.206.145 — не отвечает (100% packet loss)
- SIP audio на Mac — тишина (Mango employee 22 timeout ~0 сек)
- recording_transcripts API — 429 rate limit

---

## 2026-07-09 — Real-time migration (Яндекс Realtime)

**Статус:** DRAFT-план и ADR готовы, ждут Яндекс Cloud ключи.

### Контекст (Habr-кейс 1057176)
Сравнены три подхода для РФ:
| Подход | Задержка | Русский | Доступ | Вердикт |
|---|---|---|---|---|
| **Яндекс Realtime** | **~330 мс** | Родной, без акцента | Нативно, рублями | **Выбор для прода** |
| OpenAI Realtime | ~740 мс (под VPN) | Акцент, мужской род у женского | Заблокирован, оплата — квест | Не для РФ-прода |
| Трёхзвенка (STT→LLM→TTS) | **~4.4 с** | Зависит от STT/TTS | Нативно | Слишком медленно |

### Решение
Перевести агента на **Яндекс Realtime API**, оставив трёхзвенку (`levitan_turnbased.py`) как fallback.

### Сделано в коде
- `docs/adr/ADR-001-yandex-realtime.md` — полный ADR с архитектурой, этапами, рисками.
- `deploy/levitan_turnbased.py` — класс `RealtimeDialog` (DRAFT, GA-форма):
  - `REALTIME_MODEL` = `gpt-realtime-2.1-mini` (переключим на `yandex-rt` при ключе)
  - `session.update`: `session.type:"realtime"`, `audio.output.format`, `reasoning.effort:"low"`
  - Убран `OpenAI-Beta: realtime=v1`
  - События GA: `response.output_audio.delta`, `response.output_audio_transcript.done`, `conversation.item.input_audio_transcription.completed`
  - Поток A: capture FIFO → `input_audio_buffer.append`; Поток B: audio.delta → playback FIFO
  - Запуск: `python3 levitan_turnbased.py realtime <phone>`

### Архитектура (из статьи)
```
Клиент (телефон) → Mango callback → baresip (ext 22, greeting_bridge)
    │ SIP/RTP 8kHz PCM
    ▼
FastAPI WS-прокси (deploy/levitan_realtime.py) — держит YC_API_KEY
    │ PCM 24kHz (auresamp 8k↔24k)
    ▼
Яндекс Realtime WS (wss://llm.api.cloud.yandex.net/.../realtime)
```
**Ключевое:** backend-прокси обязателен (нет ephemeral-токенов у Яндекса).

### Блокеры до теста
1. **Яндекс Cloud API-ключ** — сервисный аккаунт с ролями `ai.speechkit-stt.user`, `ai.speechkit-tts.user`, `ai.languageModels.user`, `ai.models.user` + `YC_API_KEY`, `YC_FOLDER_ID` в `.env`. **БЛОКЕР: аккаунта нет, задача на следующую сессию**.
2. `OPENAI_API_KEY` в `.env` (для OpenAI Realtime как fallback).
3. `pip install websockets` в venv.
4. **baresip audio-мост** — playback через `aufile` из FIFO, capture через `record` в FIFO; конверсия 8k↔24k через `auresamp.so`.

### Риски
- Доступы Яндекса: с 2026-06-01 обмен OAuth на IAM закрыт — роли назначать в консоли вручную.
- baresip AEC: на ноутбучных колонках может ловить эхо — `echoCancellation` или наушники.
- Промпт: JSON-формат ломает голос — только естественная речь + function calling.

### Next Steps
1. **Следующая сессия:** создать Яндекс Cloud аккаунт, выпустить ключ, добавить в `.env`.
2. Дописать `deploy/levitan_realtime.py` (FastAPI WS-прокси, TDD).
3. Настроить `greeting_bridge` baresip: PCM FIFO + `auresamp.so` 8k↔24k + `echoCancellation`.
4. Адаптировать `SYSTEM_PROMPT` под голос (убрать JSON, оставить роль/факты/запреты).
5. Интегрировать `save_lead` через function calling → CRM API.
6. Тест: latency <500мс, barge-in, эхо, лид в CRM.
7. Оставить трёхзвенку как fallback endpoint, обновить `active_tasks.md`.

---

## 2026-07-09 — Global: /goal & /code-review commands added

### Добавлено в корневой AGENTS.md
- **`/goal`** — декларативный цикл: метрика+target → baseline → итерировать (TDD) → стоп при достижении/эскалация → `memory_add kind=decision`.
- **`/code-review`** — выделенный Two-axis Review: `git status`+`diff` → чтение файлов целиком+вызовы → Standards+Spec → список рисков → не коммитить без подтверждения → `memory_add kind=bugfix` при блокере.

---

---

## 2026-07-21 — VPS восстановлен: новый сервер + миграция всех ботов

**Статус:** Новая VPS Timeweb (217.149.23.113, 1 vCPU/2GB/30GB NVMe, Ubuntu 22.04).

### Сделано
- **Новый VPS создан** в ЛК Timeweb (аккаунт Vezemcip@yandex.com, ~1775 ₽/мес)
- **Базовая настройка:** SSH-ключ, UFW (порты 22/5060/4321/5000/8085/8086/16384-32768), nginx, Python 3.10, Node.js 20, PM2 7.0.3
- **baresip 1.0.0** установлен (голосовой движок для SIP)
- **Код перенесён:** ai-eggs (agent + data), angel-backend, ecosystem.config.cjs
- **Виртуальные окружения:** `.server_venv` (ai-eggs) + `.venv` (angel-backend), все зависимости установлены
- **.env обновлён:** IP заменён с `72.56.38.19` на `217.149.23.113`

### Восстановлены 9 PM2-процессов

| Процесс | Статус | Порт | Назначение |
|---------|--------|------|-----------|
| **angela-bot** | ✅ online | — | TG бот @Angella26bot |
| **angela-server** | ✅ HTTP 200 | :5000 | REST бэкенд |
| **angela-autopilot** | ✅ online | — | Автопилот/A2A |
| **angela-scheduler** | ✅ online | — | Планировщик отчётов |
| **ptenchikova-bot** | ✅ online | — | VK/лента |
| **a2a-dispatcher** | ✅ online | — | A2A шина сообщений |
| **mango-webhook** | ✅ HTTP 200 | :8085 | Вебхук Mango Office |
| **dtmf-handler** | ✅ HTTP 200 | :8086 | DTMF обработчик |
| **vezem-web** | ✅ HTTP 200 | :4321 | Astro-сайт (каталог) |

PM2 автостарт (systemd) настроен, `pm2 save` выполнен.

### Нерешённое
- **vezemcip.ru (Laravel)** — код не найден (ни на Mac, ни на GitHub). Нужен бэкап из Timeweb аккаунта `wy07319` или исходники.
- **Архитектура:** turn-based FAQ-кэш (210 триггеров) + Яндекс SpeechKit TTS + local LLM-fallback. Яндекс Realtime отложен — текущая схема работает без него.
- **VPS SIP** — baresip установлен, нужно настроить на Mango SIP (аккаунт + регистрация).

## Next Steps
1. Найти код vezemcip.ru (Laravel) — бэкап Timeweb или исходники
2. Настроить baresip на Mango SIP (регистрация)
3. Получить Яндекс Cloud ключ для Realtime
4. Запустить `deploy/levitan_realtime.py`

---

## 2026-07-23 — Pricing tier update: 65₽ → tiered (90/85/80/75), FAQ cache + greeting updated

### Сделано
- **Цены обновлены** во всех документах: до 100 — 90₽, 101-300 — 85₽, 301-999 — 80₽, от 1000 — 75₽
- **FAQ-кэш** (`ANGELLA_BROILERS_FAQ_CACHE.json`): все ~20 блоков с упоминанием 65₽ заменены на новую сетку; пересчитаны суммы заказов (50/100/200/500 голов)
- **Greeting-скрипт** (`scripts/gen_angela_greeting.py`): обновлён текст (вместо "по 65 рублей" → "Цена от 75 рублей за голову в зависимости от объёма"), лид-тишина увеличена до 9с
- **KB** (`ANGELLA_BROILERS_KB.md`): цена от 75₽/шт, tiered
- **ADR-001**: цены обновлены
- **Коррекция смысла реплик**: вместо "фиксированные 65" теперь все ответы отражают ступенчатую скидку

### Issues / Blockers
- **Начало приветствия режется** — 9с лид-тишины может быть недостаточно (RTP стартует до ACK Mango). Возможные фиксы: увеличить до 12-15с, FIFO с задержкой, переставить ключевую фразу в конец

### Next Steps
1. Увеличить лид-тишину до 12-15с если обрезание продолжается
2. Перегенерировать greeting WAV на VPS (`python3 gen_angela_greeting.py`) после фикса тишины
3. Рестарт baresip на VPS: `sudo systemctl restart baresip-auto`

---

## 2026-07-30 — Greeting cut-off fix: lead-in silence, VPS deploy, Mango API 3128 verdict

**Сессия завершена.**

### Сделано

1. **Диагностика обрезания приветствия**: «…за голову. Чем могу помочь?» — первые ~8 секунд съедаются, т.к. baresip aufile стартует чтение раньше, чем RTP-канал открывается (RTP открывается через ~1-3 сек после 200 OK, но файл начинает читаться сразу при INVITE).

2. **Lead-in фикс на VPS**: добавлено 1.5 сек тишины в начало `/tmp/levitan_play.wav` и `/tmp/levitan_greeting_lead.wav` через Python wave (код в `_add_lead_silence()`).

3. **Код обновлён** (`levitan_greeting.py` — генерация greeting с lead-in; `levitan_faq_agent.py` — `set_baresip_audio()` принимает `lead_silence=1.5`), файлы загружены на VPS, FAQ-агент перезапущен (levitan-faq: online).

4. **Mango API 3128 финальный вердикт**: config/*, media/*, commands/call возвращают 3128 — **НЕ блокер!** FAQ-агент работает через baresip aufile напрямую, не используя Mango media/play API.

5. **Выявлены проблемы**:
   - FAQ-агент при рестарте читает ВСЕ события из `events.jsonl` → запускает старые диалоги заново (нужна фильтрация по timestamp)
   - `/root/.baresip/accounts` на VPS всё ещё на старой АТС `vpbx400161137` (должна быть `vpbx400374818`)
   - Запись аудио клиента не настроена (STT не работает)
   - baresip-auto сервис мёртв, baresip запущен вручную из `/root/.baresip/`

### Issues / Blockers
- **Mango API**: config/*, media/*, commands/call — 3128 (не влияет на работу через aufile)
- **FAQ-агент**: при рестарте воспроизводит старые события из events.jsonl
- **baresip accounts**: `/root/.baresip/accounts` → vpbx400161137 (нужно vpbx400374818)
- **baresip-auto**: сервис inactive (baresip запущен вручную)

### Next Steps
1. **Настроить LiveKit SIP для real-time диалога** (см. real-time verdict)
2. Доработать Pipecat агента: FAQ-кэш, русский TTS (edge-tts/Yandex)
3. Обновить `/root/.baresip/accounts` на vpbx400374818
4. Починить фильтрацию событий в `watch_for_calls()` — игнорировать события старше времени запуска

---

## 2026-07-31 — Real-time evaluation + LiveKit SIP plan

### Сделано
- **Greeting текст обновлён**: «Здравствуйте! Предлагаем вам суточных цыплят Росс 308 по цене от 75 рублей, вам интересно?»
- **Lead-in 2s**: тест пройден — клиент слышит полное приветствие
- **37s WAV**: 15s тишины (RTP) + greeting + 8s пауза + "спасибо до свидания"
- **STT (faster-whisper)**: работает на dec.wav (sndfile записывает голос клиента). Факт: клиент сказал «интересно, почём?» — распознано
- **Двухзвонковая схема доказана**: Call#1 (greeting+пауза) → STT → FAQ → TTS → Call#2 (ответ)
- **Play/start отключён**: перебивал baresip aufile, теперь только baresip
- **Фильтр-баг найден**: `"levitan_t" in cmd_id` блокировал тестовые звонки (levitan_test). Исправлен на `"levitan_turn_"`
- **FIFO bridge протестирован**: aufile не читает FIFO (ждёт WAV с RIFF заголовком)
- **aubridge.so** доступен, не протестирован

### Real-time verdict
| Подход | Статус | Задержка |
|--------|--------|----------|
| aufile WAV | ✅ Работает (текущий) | ~25 сек (2 звонка) |
| aufile FIFO | ❌ Не читает поток | — |
| aubridge UDP | ❓ Не тестирован | — |
| **LiveKit SIP** | 🏆 **Цель** | **~1 сек** |
| Pipecat | ✅ Установлен, агент работает | — |

### Архитектура real-time (LiveKit SIP)
```
Mango callback → SIP → LiveKit Server (v1.13.4) → Room
    → Pipecat Agent (STT+LLM+TTS) → Room → клиент
```
Требуется: установить `livekit-sip` плагин, настроить SIP trunk с Mango (vpbx400161137, user4), dispatch rule на room, доработать агента.

### PM2 процессы
- `baresip-sip` — online (WAV mode)
- `levitan-faq` — online (обработка событий, STT)
- `levitan-webhook` — online (приём Mango событий)
- `livekit-server` — online (pid 623, localhost:7880)
- Pipecat `levitan_agent.py` — online (pid 624)

### На след. сессию
1. Установить `livekit-sip` на VPS
2. Настроить SIP trunk с Mango
3. Настроить dispatch rule → room → Pipecat агент
4. Доработать агента: FAQ-кэш (238 триггеров), русский TTS
5. Тест real-time диалога

---

## 2026-08-09 — Диагностика «тишины после приветствия» (LiveKit SIP + Pipecat)

### Сделано
- **Найден корень 502 OmniRoute**: баланс/лимит OpenRouter исчерпан ($20 лимит ключа omniroute = 0 остаток; второй ключ — $0). Все платные модели падают «requires more credits».
- **OmniRoute 127.0.0.1:20128 работает на :free-каналах** (StreamLake/DeepInfra/Novita) при `max_completion_tokens ≤ 200`. При 300+ → 502 (каскад падает на платных каналах).
- **Патч агента**: `max_completion_tokens=300 → 200` в `/opt/pipecat-agent/levitan_agent.py` (строка 396). Бэкап `.bak3`. Синтаксис OK, сервис active.
- **Прямые тесты OmniRoute (все 200)**:
  - без tools, max_tokens=50/100/150/200 + stream → 200 за 1-2.9s
  - **с tools + stream + max_completion_tokens=200 → 200 за 1.5s (DeepInfra, tool_calls!)** — tools НЕ виноваты
  - Gemini AI Studio (ключ AIzaSyC9l5X...): простой запрос 200 за 1.16s, но **free tier лимит ~20 RPM** (429 на tools-запросе) — хрупко, не использовать как основной
- **Диагностика в DebugLLMStream** (levitan_agent.py): `import asyncio` добавлен, `__anext__` печатает `waiting`/`chunk#`, WATCH печатает состояние task через 10s.
- **Лог звонка 11:36-11:37**: приветствие сказано (16.3s), LLM-стримы создаются, `__anext__#1 waiting`, **WATCH 10s: task_done=False exc=None** — задача стрима висит без ответа. Ни одного chunk.

### Ключевой вывод
**LLM-запрос из livekit-агента зависает, хотя тот же запрос с tools через curl работает за 1.5s.** Значит проблема НЕ в OmniRoute и НЕ в tools — а в обвязке livekit openai.LLM внутри процесса агента (payload/параметры, event loop, или что-то ещё).

### Issues / Blockers
- **ГЛАВНЫЙ БЛОКЕР**: livekit-стрим LLM висит >10s без chunks; WATCH показывает task_done=False (задача жива, ответа нет). Пользователь слышит «приветствие → вопрос → тишина» и вешает трубку.
- Приветствие обрезается (livekit interruption / speech not done in time) — вторичная проблема после LLM.
- SSH на VPS нестабилен (Connection refused периодически) — патчи через grep-проверки.

### Next Steps
1. **Сравнить payload livekit-запроса vs рабочий curl**: tcpdump на 20128 во время звонка (только порт 20128, файл /tmp/cap2.pcap был пустой — tcpdump не стартовал из-за nohup+ssh). ИЛИ залогировать body в DebugLLMStream перед `_make_llm_call`.
2. Проверить подозрительные параметры livekit openai.LLM: `stream_options={"include_usage": True}`, `strict_tool_schema`, `max_completion_tokens` — какие из них omniroute/StreamLake не переваривает и вешает.
3. Если payload идентичен — копать event loop / uvloop / версию livekit-клиента внутри процесса.
4. Альтернатива: заменить livekit openai.LLM на собственный LLM-класс, который ходит напрямую (рабочий код из probe6/probe_live) — полный обход проблемной обвязки.
5. Потом: докрутить прерывание приветствия (allow_interruptions=False уже стоит).

### Файлы на VPS
- `/opt/pipecat-agent/levitan_agent.py` — диагностика в DebugLLMStream + max_completion_tokens=200
- `/opt/pipecat-agent/levitan_agent.py.bak2/.bak3` — бэкапы
- `/tmp/probe_live.py` — рабочий эталон прямого вызова (stream, без tools — 502 на 300 токенов; с tools на 200 — 200)
- `/tmp/tools.json` — успешный ответ с tool_calls
- OmniRoute: `/root/.omniroute/` (Node, 20128, ключ sk-or-v1-8508 лимит исчерпан) + `/opt/omni-auto-router/` (Python, 20129, ключ sk-or-v1-63bf без денег)

---

## 2026-08-13 (20:18-20:32) — Stage 2 FAILED → rollback + phone fix + Stage 3 deployed (тест завтра)

### Stage 2: answer-detection через participant attributes — FAILED (20:18)

**Попытка:** вместо слепой задержки 5с определять момент ответа абонента через `sip.callStatus` (LiveKit participant attributes) → greeting начинать СРАЗУ после ответа.

**Результат:** ПРОВАЛ. Лог: `[AGENT] callee answered (signal) -> greeting now` пришёл ПЕРЕД реальным ответом абонента. Приветствие обрезалось, номер телефона повторён как целое число «семь милиардов...».

**Причина:** Mango отправляет `sip.callStatus: "not ringing"` когда ещё НЕ передала аудио агента абоненту. Сигнал ненадёжен — приходит раньше реального подключения аудио-канала.

**Откат:** `cp /opt/pipecat-agent/levitan_agent.py.voice-stability /opt/pipecat-agent/levitan_agent.py` → restart (worker `AW_u3aws9Jmcgo6`).

**Ключевой вывод:** правильный API — `room.on("participant_attributes_changed", cb)` с сигнатурой `cb(changed_attributes, participant)` (НЕ `cb(participant, attrs)`). НО `sip.callStatus` unreliable для определения реального ответа.

### Phone bug fix (20:23)

**Проблема:** TTS произносил номер `79859234644` как «семь милиардов восемьсот...» (полное число).

**Решение:** функция `_fmt_phone_spoken()` форматирует по цифрам: `7 9 8 5 9 2 3 4 6 4 4`. Промпт: `caller_phone_spoken` подставляется.

**Статус:** VERIFIED (звонок 20:23). Деплой поверх voice-stability.

### Stage 3: streaming TTS + prompt rewrite + 7s delay (20:32) — ЗАДЕПЛОЕН, НЕ ПРОТЕСТИРОВАН

**Изменения:**
1. **SYSTEM_PROMPT**: убран повтор номера телефона — теперь только «Место доставки прежнее?» (пользователь: «мы и так знаем номер»)
2. **Задержка 5с→7с** в `on_enter` (insurance against slow answer)
3. **Lead-tone 0.3с→1.5с** (длиннее — до аудио абонента)
4. **Streaming TTS**: sentence-level chunking — каждый предложение синтезируется отдельным HTTP-запросом к Yandex TTS. Первое предложение отдаётся на воспроизведение СРАЗУ, не дожидаясь всего текста. `capabilities.streaming=True`
5. **Метрики возвращены**: `greet_dur`, `turns`, `resp_lat[]`, `avg_resp_lat` — печатаются в `[METRICS] summary` при завершении звонка.

**Ключевой insight по задержкам диалога:** TTFT LLM ~1с, но TTS блокирует 7-9с (потому что `streaming=False` + `await resp.read()` ждал ВЕСЬ аудио-ответ). Sentence-chunking решает это: первый ответ агента стартует после 1-ого предложения (~1-2с), а не после полного текста (~5-7с).

**Коммит:** voice-stability baseline обновлён на VPS (5с + phone fix). Git-тег `voice-stability` = `c85820ad`.

### Бэкап для отката
```
cp /opt/pipecat-agent/levitan_agent.py.voice-stability /opt/pipecat-agent/levitan_agent.py
systemctl restart levitan-agent
```

### ПЛАН НА ЗАВТРА
1. **ТЕСТ-ЗВОНОК** на `79859234644` — проверить: приветствие ЦЕЛИКОМ + задержки диалога меньше + номер НЕ повторяется + только «место доставки прежнее?»
2. При нестабильности → откат к `voice-stability`
3. При успехе → зафиксировать в git, обновить `voice-stability` tag
